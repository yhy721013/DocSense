# 大模型文件处理接口文档

## 目录

1. [大模型文件解析接口]
2. [大模型报告生成接口]
3. [检测大模型任务接口]
4. [大模型任务进度推送接口]
5. [大模型回调接口]

---

## 1. 大模型文件解析接口

**接口路径：** `/llm/analysis`  
**请求方法：** `POST`  
**功能说明：** 提交文件进行大模型解析，支持多维度业务参数配置

### 请求参数说明

| 参数 | 类型 | 说明 | 必填 |
|------|------|------|------|
| businessType | String | 业务类型标识，固定值："file" | 是 |
| params | Array | 请求参数列表，最多 32 项（项目兼容单项与多项；多项时按顺序串行处理） | 是 |

> 项目兼容扩展说明：`/llm/analysis` 支持 `mhtml`/`mht` 文件；服务端下载后会先提取正文文本，再复用现有大模型解析流程。
>
> Legacy Office 扩展说明：部署开启本地转换能力后，`/llm/analysis` 支持 Microsoft Office
> 97–2003 的 `.doc`、`.ppt`、`.xls`，服务端在下载后、创建远端 RAG Session 前分别转换为
> `.docx`、`.pptx`、`.xlsx`；正文读取、全文翻译和 RAG 均使用转换产物。转换失败时该文件按
> 既有文件失败合同收口，不会把原始二进制文件直接上传作为降级；同批后续文件继续处理。
> `.xls` 转换结果和原生 `.xlsx` 当前都只允许恰好一个可解析 Sheet，多 Sheet 会明确拒绝该文件
> 并尝试清理本次上传的完整临时 Folder，清理结果未知时保留恢复现场。该扩展不改变请求、202
> 响应、任务查询、Progress 或 Callback 的任何字段；`fileName`、`originalFileName`、`format`
> 和 `dataFormat` 继续使用业务请求值，内部转换名与 Sheet 名不对外返回。
>
> 整个 JSON 请求体最大 64 MiB；超限同步返回 HTTP `413`。`params` 中任一项格式或领域树无效时同步返回 HTTP `400`，整批不建任务、不下载文件。

### params参数结构

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| architectureList | Array | 完整领域树；请求结构不变，服务端在本地召回有界模型候选，但完整树仍用于合法性校验、数据标准判断、存储归并和 callback | 见下方示例 |
| architectureStandardList | Array | 数据标准额外解析范围；最终分类命中该范围或其子孙节点时，回调 `fileDataItem` 额外返回数据标准字段 | `[{"id":1768464916588441,"name":"国家军用标准"}]` |
| channel | Array | 资料来源机构候选列表（字典编码）；服务端仅使用请求提供的候选，不注入默认候选。未传、传空数组或没有有效候选对象时，模型和回调均输出空字符串 | `[{"value":"装发","key":"02"}]` |
| country | Array | 国家列表（范围）（字典编码） | `[{"value":"美国","key":"02"}]` |
| fileName | String | 文件对象名（业务主键） | `"bded228dc94440519d87f97cfb6b520b.pdf"` |
| originalFileName | String | 业务原始文件名，服务端保留请求原值；除辅助大模型理解文件主题外，还作为后续知识谱系回调来源展示名 | `"GJB 9001C-2017 质量管理体系要求.pdf"` |
| filePath | String | 文件下载地址（格式 application/octet-stream） | `"http://localhost:8080/file-download/..."` |
| format | Array | 原生格式列表（范围）（字典编码） | `[{"value":"文档类","key":"03"}]` |
| maturity | Array | 成熟度列表（范围）（字典编码） | `[{"value":"阶段成果","key":"02"}]` |
| security | Array | 密级列表（范围）（字典编码）；解析时根据文档开头内容判断密级，未见相关说明则默认为“公开” | `[{"value":"公开","key":"02"}]` |

### architectureList元素结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 领域体系唯一标识；兼容正数字符串，拒绝布尔值、非正数和重复 ID |
| name | String | 领域名称 |
| parentId | Long | 父节点 id；根节点可为 `0`、空或不传，请求子集缺少父节点时按有限树边界处理 |
| path | String | 从根节点到当前节点的 id 链 |
| pathName | String | 节点语义路径；非空值按不透明文本使用，不根据 `/` 反推树层级 |
| remark | String | 可选的节点名词概述；非空时用于召回和辅助模型理解节点含义 |

甲方正式调用继续在每个 `params[]` 中传完整 `architectureList`，无需在调用侧预先裁剪候选。服务端从完整树建立索引和 Top-K 候选；请求与 callback 的字段结构均未因此改变。

领域树输入边界：

- 单个 `architectureList` 最多 10,000 个节点，可见深度最多 128 层。
- `name`、`path`、`pathName`、`remark` 分别最多 256、2,048、2,048、4,096 字符；四个文本字段累计最多 2,000,000 字符。
- 包含调用方扩展字段在内的紧凑 JSON 最多 4,000,000 字符；非 JSON 可序列化值、重复 ID、非法父节点或父链成环均会被拒绝。
- 为兼容存量调用，`architectureList` 缺失、为 `null` 或空数组时继续使用服务端默认树；显式提供非空列表时，每个节点都必须合法，服务端不再静默过滤坏节点。

### architectureStandardList元素结构

`architectureStandardList` 使用与 `architectureList` 相同的节点结构和资源上限。该列表表示需要额外解析数据标准字段的分类范围，可以只传部分节点，也可以传包含“数据标准”树根节点的完整节点列表；缺失、`null` 或空数组表示不启用扩展字段。

领域分类规则：

- 默认 `topk_two_stage` 模式先根据文件名、原始文件名、标题、型号/标准号及有界正文片段，在完整树上执行 exact、字符 BM25、树路由和业务规则召回；AnythingLLM 标签向量召回不在本轮范围。
- 当 `architectureList` 只有一个合法节点时，服务端直接确定该节点 `id`，跳过分类并以字段抽取作为首次模型查询；其他信息提取仍正常执行。
- 当 `architectureList` 包含多个节点时，服务端先执行纯领域分类，再在同一文档 Session 的全新独立 Thread 中执行字段抽取，防止分类回答进入字段抽取上下文。抽取 Prompt 不包含完整领域树，只携带已确认 ID、语义路径和节点类型。
- 模型可见候选不超过 128 个，分类 Prompt 不超过 32,000 字符。分类与抽取阶段各最多执行 2 次实际模型查询，供应商重试和 repair 共享阶段预算，单文件总上限为 4 次。
- 多候选优先分类到证据充分的叶子节点。叶子证据不足时，只能从模型可见候选中选择最深的可靠父节点；该父节点必须属于完整树、深度至少为 2 且满足召回资格。完整树的八个根节点及“数据标准”父节点禁止返回。
- 分类返回空值、非数字、候选外 ID 或其他非法结果时，只在分类阶段剩余预算内使用完全相同的候选执行有限 repair；不得扩大候选或回退到完整树 Prompt。
- 当最终分类名称严格符合 `*-基础数据`、`*-战技指标`、`*-运用数据` 或 `*-效能数据` 时，回调中的 `data.architectureId` 仍返回该具体子分类 ID；服务端内部知识库统一按对应的武器装备父节点 ID 存储。该内部归并不会改变本接口的回调结构和分类结果。
- 当文档内容明确为 GJB、国军标、国家军用标准相关资料时，`architectureId` 只允许返回模型可见候选中“数据标准”分支下的六类叶子之一，禁止返回“数据标准”父节点；模型与有限 repair 仍无法确认专业类别时，定向归入“通用要求”（兼容节点名“通用要求标准”），不按完整 `architectureList` 的请求顺序选择其他叶子；候选树缺少该叶子时任务进入 `status=3`。
- 当最终 `architectureId` 命中 `architectureStandardList` 范围或其子孙节点时，回调 `fileDataItem` 额外返回 `militaryName`、`num`、`startTime`、`implTime`、`approvalDept`；未命中时不返回这 5 个字段。
- `architectureStandardList` 仅控制上述五个扩展字段，不替代完整 `architectureList`，也不改变领域分类候选。
- 无有效召回信号、候选或 Prompt 超过合同上限，或者分类及有限 repair 后仍无法确定合法 ID 时，文件任务进入 `status=3`；服务端禁止默认返回 ID `1`，不会发送成功 callback，也不会写入永久知识库。

运行与审计说明：

- `DOCSENSE_ANALYSIS_CLASSIFICATION_MODE=topk_two_stage` 为默认两阶段模式。
- `topk_single` 保留 Top-K 有界候选，但回滚为单阶段分类与字段抽取。
- `legacy` 仅允许完整树候选不超过 128 且最终完整 Prompt 不超过 32,000 字符的小树；任一上限超出时任务失败，不允许无界发送完整领域树。
- `DOCSENSE_ANALYSIS_FILENAME_CONSTRAINT_MODE=scope_guard` 为默认文件名约束模式，启用简氏作用域识别，以及 `originalFileName` 与首页真实标题的双源分支约束；设置为 `legacy` 可即时回滚到既有文件名硬约束。文件名不能在证据冲突或不足时单独决定最终分类；显式空值或未知值会使服务拒绝启动。
- `DOCSENSE_ANALYSIS_IDENTITY_RESELECT_MODE=enforce` 为默认装备身份受限重选模式。仅当描述性 `originalFileName` 与文档开头/标题独立确认同一唯一装备型号、并且完整的装备父节点与七类直属明细叶子均可见时，才允许对落在目标分支外或过粗祖先上的初次分类执行一次受限重选；空值、越界结果或调用失败均保留初次分类。设置为 `shadow` 可只记录而不采纳重选结果，设置为 `off` 可即时关闭。
- 服务端按任务 execution 幂等保存 tree fingerprint、query digest、Top-64、最终候选、通道排名、RRF、protected 原因、Prompt 字符数、返回 ID/rank、耗时及失败阶段。召回审计不保存正文；审计失败时不会创建远端 RAG Session。
- 以上改造只改变内部候选与模型调用流程，不改变请求或 callback shape。在人工 gold set 和上线门禁通过前，不据此宣称生产分类准确率已经提升。

`score` 评分规则：

- 闭源渠道或权威机构公开发布，如军政官方机构网站或正式出版物，评分 `95`。
- 专业科研单位，如兰德、简氏等知名智库及装备研制单位等，评分 `85`。
- 专业信息网站，如海上舰艇交通网、美舰艇历史与部署网、海军学院新闻网、防务新闻网等，评分 `75`。
- 普通信息网站，如综合性新闻网、百度百科等，评分 `65`。
- 未明确数据来源的资料，评分 `55`。

### 请求示例

以下请求用于展示节点字段和单节点兼容分支。甲方正式调用时，应以完整领域树替换示例中的 `architectureList`，无需在调用侧预先执行 Top-K 裁剪。

```json
{
  "businessType": "file",
  "params": [
    {
      "architectureList": [
        {
          "id": 1768464916588441,
          "name": "国家军用标准",
          "parentId": null,
          "path": "1768464916588441",
          "pathName": "数据标准/国家军用标准",
          "remark": "国家军用标准、GJB、技术标准和数据规范。"
        }
      ],
      "architectureStandardList": [
        {
          "id": 1768464916588441,
          "name": "国家军用标准"
        }
      ],
      "channel": [
        {
          "value": "装发",
          "key": "02"
        }
      ],
      "country": [
        {
          "value": "美国",
          "key": "02"
        }
      ],
      "fileName": "bded228dc94440519d87f97cfb6b520b.pdf",
      "originalFileName": "GJB 9001C-2017 质量管理体系要求.pdf",
      "filePath": "http://localhost:8080/file-download/bded228dc94440519d87f97cfb6b520b.pdf",
      "format": [
        {
          "value": "文档类",
          "key": "03"
        }
      ],
      "maturity": [
        {
          "value": "阶段成果",
          "key": "02"
        }
      ],
      "security": [
        {
          "value": "公开",
          "key": "02"
        }
      ]
    }
  ]
}
```

> 项目兼容扩展说明：除单文件请求外，也支持 `params` 中放入多个文件对象；后端会按数组顺序逐个处理，并为每个 `fileName` 分别维护状态、进度和回调。
>
> 同名文件已有 `status=0/1` 任务时返回 HTTP `409`。任务已进入业务终态但首次回调仍为 `callback_status=pending` 时也暂不接受同名重跑，避免覆盖尚未送达或尚未确认的旧结果；`failed` 或 `outcome_unknown` 任务通过新的 `/llm/check-task` 请求实际补发时会持有有界发送租约，租约释放前同样返回 `409`。发送租约过期后不会由后台自动重抢，而是先冻结为 `outcome_unknown`；调用方确认可以接受至少一次投递语义后，可再次调用 `/llm/check-task` 明确补发。该补发可能使接收方收到两次内容相同的业务回调，接收方应按 `fileName` 和业务结果幂等处理。回调成功、失败或明确跳过且没有在途补发后可再次提交。

### 受理成功响应（HTTP 202）

请求通过校验并成功受理后，响应体为空。调用方使用请求中原有的 `fileName` 通过进度 WebSocket、`/llm/check-task` 的恢复触发或最终回调跟踪任务，不从受理响应获取内部任务 ID。

```http
HTTP/1.1 202 Accepted
Content-Length: 0
```

HTTP 400、409 等错误响应仍按既有 JSON 错误体返回。

> 实施状态：202 空响应体契约已于 2026-07-15 明确确认，并在负责人于 2026-07-25 明确同意后于当前集成分支实现。成功响应不再返回任务快照、`execution_id` 或其他内部调度信息；此处描述的是待合并代码状态，不代表已经具备多实例生产运行能力。

---

## 2. 大模型报告生成接口

**接口路径：** `/llm/generate-report`  
**请求方法：** `POST`  
**功能说明：** 基于多文件内容生成结构化分析报告

### 请求参数说明

| 参数 | 类型 | 说明 | 必填 |
|------|------|------|------|
| businessType | String | 业务类型标识，固定值："report" | 是 |
| params | Array | 请求参数列表（仅使用第一个元素） | 是 |

> 项目兼容扩展说明：`/llm/generate-report` 的 `filePathList` 可包含 `mhtml`/`mht` 文件；服务端下载后会先提取正文文本，再参与报告生成。`templateOutline` 为 Word 模板文件下载地址，服务端会下载 `.docx` 模板并提取其中的文字内容作为报告大纲要求。
>
> Legacy Office 扩展说明：部署开启本地转换能力后，`filePathList` 可包含 `.doc`、`.ppt`、
> `.xls` 来源，服务端分别转换为 `.docx`、`.pptx`、`.xlsx` 后按原顺序参与报告生成；任一来源
> 转换失败时整份报告按既有失败合同收口，禁止原文件上传降级。`.xls` 转换结果和原生 `.xlsx`
> 当前都只允许恰好一个可解析 Sheet，多 Sheet 会使整份报告失败并尝试清理本次上传的完整临时
> Folder。`templateOutline` 仍只接受 `.docx`，不隐式转换 `.doc` 模板。本扩展不增加或改变
> `filePathList`、Callback 或其他公开字段，内部转换名、Sheet 名和 Folder 名不进入报告正文。

### params参数结构

| 字段 | 类型 | 说明 | 示例值 |
|------|------|------|--------|
| filePathList | Array | 文件下载地址列表（支持多文件） | `["http://.../file1.sql", "http://.../file2.sql"]` |
| reportId | Integer / String | 报告唯一标识（业务主键）；可传 JSON 整数或十进制整数字符串 | `132` 或 `"132"` |
| templateDesc | String | 模板说明信息 | `"1231231"` |
| templateOutline | String | Word模板文件下载地址（服务端下载后提取文字内容作为模板大纲） | `"http://.../template.docx"` |
| requirement | String | 业务需求描述 | `"要求"` |

`reportId` 不受 32 位或 64 位整数范围限制，但其十进制数字部分最多为 **128 位**。
字符串形式允许首尾空白、可选正负号和前导零；正负号不计入 128 位上限，前导零计入。
去除首尾空白后，字符串必须完全由可选的 `+`/`-` 与十进制数字组成；布尔值、浮点数和
含小数点、指数或其他字符的字符串均视为参数错误。服务端按整数值统一业务主键，因此
`132`、`"132"`、`"+132"` 和 `"00132"` 表示同一报告任务；但 129 个数字字符即使
主要由前导零组成也会被拒绝。调用方如无法保证其 JSON number 实现对大整数无损，应
优先使用整数字符串传入。

超过长度上限时，接口返回 HTTP `400` 和既有单字段错误体：

```json
{
  "error": "reportId不能超过128位十进制数字"
}
```

`templateDesc` 与 `requirement` 的参数集合和文档类型保持不变。为兼容旧执行链：字段缺失
时按空字符串处理；若调用方历史请求中传入了非字符串 JSON 值，服务端会先按旧逻辑转换
为字符串再冻结任务输入。新调用方仍应依照上表传入字符串，服务端不会在该兼容转换中
增加、删除或回写任何请求字段。

> 实施状态：上述 `reportId` 入站兼容、128 位上限及可选文本兼容转换已于 2026-07-16
> 获得确认；`reportId` 规则已在报告生成、报告类型 check-task 和报告类型 Progress 的
> 当前代码路径中统一实现。未改变任何字段集合或服务端输出类型。

### 请求体与数组元素校验

报告生成请求采用整次请求原子校验：

- 顶层 JSON 必须是对象；数组、字符串、数字、布尔值、`null` 或无法解析的请求体均返回
  HTTP `400`，错误体为 `{"error":"请求体必须是JSON对象"}`；
- `params` 必须是非空数组，且其中每个元素都必须是 JSON 对象。出现任意非对象元素时，
  整次请求返回 HTTP `400` 和 `{"error":"params元素必须是对象"}`，不得过滤后继续处理；
- 当前业务仍只使用 `params` 第一项，但严格对象校验覆盖数组中的所有位置；
- `filePathList` 必须是非空数组，且每个元素都必须是去除首尾空白后仍非空的字符串。
  第 `N` 项不符合要求时，返回 HTTP `400` 和
  `{"error":"filePathList中第N项不是有效字符串"}`，其中 `N` 从 `1` 开始；
- 上述参数错误必须在创建任务、发布初始 Progress 或启动后台执行前返回。

> 实施状态：上述三项严格校验已于 2026-07-16 明确批准并在当前 Flask 报告生成路径中
> 实现。该调整没有新增、删除或重命名任何请求参数。

### 请求示例

```json
{
  "businessType": "report",
  "params": [
    {
      "filePathList": [
        "http://localhost:8080/file/download/ad92572918564809bdeb61af1a06486a.sql",
        "http://localhost:8080/file/download/d472f05fa3344738953d027ad598ff67.sql"
      ],
      "reportId": 132,
      "templateDesc": "1231231",
      "templateOutline": "http://localhost:8080/file/download/report-template.docx",
      "requirement": "业务需求描述，报告xxxx要求"
    }
  ]
}
```

### 受理与重复提交

- 当同一 `reportId` 不存在生成中的任务时，服务端返回 HTTP `202`、空响应体并受理报告生成任务。
- 报告公开状态 `status="0"` 表示生成中。若同一 `reportId` 已存在该活动状态，重复提交返回 HTTP `409`，且不创建新任务、不启动后台执行、不覆盖原任务投影。
- 原任务进入 `status="1"`（已生效）或 `status="2"`（生成失败）后，通常允许再次提交同一 `reportId`。但若上一执行的回调 HTTP 请求可能已经送达、响应结果却无法确认，服务端会把该业务键标记为内部 `delivery_outcome_unknown`；在该状态收敛前，报告生成接口继续返回 HTTP `409`。调用方确认接收端能够按 `reportId` 和业务结果幂等处理后，可通过一次新的 `/llm/check-task` 请求显式授权至少一次补发；这不会改变报告生成接口的请求或错误结构。
- 上一执行的回调正在发送时，新提交立即返回同一 HTTP `409`，不在 HTTP 请求线程内轮询等待，也不在同一请求中重试受理。调用方可稍后按原接口重新提交；该规则不新增请求参数或错误字段。
- HTTP 400、409 等错误响应仍按既有 JSON 错误体返回。

#### 首次受理成功（HTTP 202）

```http
HTTP/1.1 202 Accepted
Content-Length: 0
```

#### 活动任务重复提交（HTTP 409）

```json
{
  "error": "任务正在处理中"
}
```

> 实施状态：202 空响应体与活动任务 409 已于 2026-07-15 明确确认；`delivery_outcome_unknown` 冻结及“发送中立即 409、不占用请求线程等待”已于 2026-07-16 确认。当前 Flask 报告路由已于 2026-07-17 完成 1C-6 切换并实现上述行为，不再为每次报告请求创建后台线程；任务受理事实进入 SQLite 持久积压，由单实例 Dispatcher 持续处理。该实现仍是开发分支的单实例兼容链路，尚未部署生产；阶段 3～6 再由 MySQL/Outbox/RabbitMQ/callback Worker 形成可靠、多实例生产实现。

---

## 3. 检测大模型任务接口

**接口路径：** `/llm/check-task`  
**请求方法：** `POST`  
**功能说明：** 检查任务是否需要回调恢复；对满足条件的终态任务触发一次回调补发

`params` 必须是非空 JSON 数组，且其中每个元素都必须是 JSON 对象。服务端会先完成全部元素的业务字段解析与规范化，再执行任何回调副作用；任一元素非法时，**整次请求**返回 HTTP 400 与既有单字段 JSON 错误体，不会过滤无效元素后继续处理，也不会先补发前面的合法项。

同一请求中，服务端按规范化业务键稳定去重：file 使用 `fileName`，report 使用规范化后的 `reportId`，weaponry 使用规范化后的 `architectureId`；每个唯一键最多进入一次检查和一次可能的发送权竞争。新的独立 `/llm/check-task` 请求可再次表达显式恢复意图，但同一轮 callback attempt 的并发请求仍只有一个 owner。单项 HTTP 404 与批量 HTTP 200 的判定继续依据调用方传入的原始 `params` 项数，而不是去重后的项数；不新增任何公开参数或响应字段。

### 文件类型请求参数

| 参数 | 类型 | 说明 | 必填 |
|------|------|------|------|
| businessType | String | 固定值："file" | 是 |
| params | Array | 仅含fileName参数的对象数组 | 是 |

### params元素结构（文件类型）

| 字段 | 类型 | 说明 |
|------|------|------|
| fileName | String | 文件对象名（业务主键） |

### 文件类型请求示例

```json
{
  "businessType": "file",
  "params": [
    {
      "fileName": "bded228dc94440519d87f97cfb6b520b.pdf"
    }
  ]
}
```

> 项目兼容扩展说明：`/llm/check-task` 支持一次传入多个 `fileName`；服务端按请求顺序处理规范化后的首次出现项，避免一次请求连续形成多轮回调尝试；成功响应体和状态码不变。
>
> file 回调为 `pending` 或 `failed` 时，可由当前请求同步尝试补发。回调为 `outcome_unknown` 时，后台 Worker 和维护线程均不得自动重发；新的 `/llm/check-task` 请求表示调用方明确接受 at-least-once 补发，因此可能产生重复业务回调。服务端仍使用 callback attempt、latest execution、租约和 fencing token 防止同一轮并发请求产生多个发送者，但接收方仍须按 `fileName` 和业务结果实现幂等。

### 报告类型请求参数

| 参数 | 类型 | 说明 | 必填 |
|------|------|------|------|
| businessType | String | 固定值："report" | 是 |
| params | Array | 仅含reportId参数的对象数组 | 是 |

### params元素结构（报告类型）

| 字段 | 类型 | 说明 |
|------|------|------|
| reportId | Integer / String | 报告唯一标识（业务主键）；可传 JSON 整数或十进制整数字符串，规范化规则与报告生成接口相同 |

报告类型 check-task 对 `reportId` 复用报告生成接口的完整规则，包括数字部分最多 128 位、
正负号不计入且前导零计入上限。超过上限时返回 HTTP `400` 与
`{"error":"reportId不能超过128位十进制数字"}`。

> report 回调为 `pending` 或 `failed` 时，可由当前请求同步尝试补发。回调为 `outcome_unknown` 时，正常 Worker 和维护线程均不得自动重发；新的 `/llm/check-task` 请求表示调用方明确接受 at-least-once 补发，因此接收方可能收到内容相同的重复业务回调，必须按规范化 `reportId` 和业务结果幂等处理。同一请求中的等价 `reportId`（例如 `132`、`"132"`、`"+0132"`）只处理首次出现项。

### 报告类型请求示例

```json
{
  "businessType": "report",
  "params": [
    {
      "reportId": "132"
    }
  ]
}
```

### 武器谱类型请求参数

| 参数 | 类型 | 说明 | 必填 |
|------|------|------|------|
| businessType | String | 固定值：`"weaponry"` | 是 |
| params | Array | 仅含 `architectureId` 参数的对象数组 | 是 |

### params元素结构（武器谱类型）

| 字段 | 类型 | 说明 |
|------|------|------|
| architectureId | Long / String | 知识谱系唯一标识；可传 JSON 正整数或只含 ASCII 数字的十进制字符串 |

武器谱类型 check-task 与 `/llm/weaponry` 共用 ArchitectureId 契约：取值范围为
`1..9223372036854775807`，字符串允许前导零但不允许空白、正负号、小数或指数形式。
`10502`、`"10502"` 和 `"00010502"` 规范为同一任务。缺失时返回
`{"error":"architectureId不能为空"}`；格式或范围非法时返回 HTTP `400` 与
`{"error":"architectureId必须为1到9223372036854775807之间的正整数"}`。

> weaponry 回调为 `pending` 或 `failed` 时，可由当前请求同步尝试补发。回调为 `outcome_unknown` 时，正常 Worker 和维护线程均不得自动重发；新的 `/llm/check-task` 请求表示调用方明确接受 at-least-once 补发，因此接收方可能收到内容相同的重复业务回调，必须按规范化 `architectureId` 和业务结果幂等处理。同一请求中的等价 `architectureId` 只处理首次出现项。

### 武器谱类型请求示例

```json
{
  "businessType": "weaponry",
  "params": [
    {
      "architectureId": "00010502"
    }
  ]
}
```

### 成功响应与副作用（HTTP 200）

请求参数有效且检查流程完成后，响应体为空。该接口不是纯查询：服务端可能在返回前同步尝试一次回调补发，并更新内部回调状态。调用方不得依赖响应体判断任务或回调结果，最终业务结果仍以回调为准。

```http
HTTP/1.1 200 OK
Content-Length: 0
```

- 单项任务不存在时，继续返回现有 HTTP 404 与 JSON 错误体。
- 参数错误继续返回现有 HTTP 400 与 JSON 错误体。
- 批量请求中的缺失任务不会使整批失败；其余存在任务仍执行检查，完成后返回 HTTP 200 空响应体。

参数错误体继续使用现有结构，不增加字段：

```json
{
  "error": "参数错误原因"
}
```

> 实施状态：200 空响应体与严格 `params` 元素校验均已于 2026-07-15 明确确认，并在负责人于 2026-07-25 明确同意后于当前集成分支实现。任一非对象 `params` 元素均拒绝整次请求；批量中缺失的任务不阻止其余存在任务完成必要的回调恢复，整批仍返回 200 空响应体。内部仍持久化并审计回调补发结果，但不通过本接口成功响应公开任务状态、进度、回调状态或恢复结果。本实现仍是单实例兼容链路，不代表 MySQL/Outbox/RabbitMQ/callback Worker 多实例生产链路已经就绪；没有增删任何请求参数、响应字段或错误结构。

---

## 4. 大模型任务进度推送接口

**接口路径：** `/llm/progress`  
**通信方式：** `WebSocket`  
**触发时机：** 调用文件解析/报告生成接口后，由客户端建立连接并发送订阅消息

### 消息请求参数（客户端→服务端）

客户端直接发送不含 `action` 的订阅 JSON。`params` 可包含一个或多个任务；服务端立即发送各任务当前快照，并在连接存续期间继续推送后续进度。关闭连接即取消该连接的全部订阅。

报告类型订阅的 `reportId` 采用与报告生成、check-task 相同的入站规则：允许 JSON
整数或十进制整数字符串，数字部分最多为 128 位，正负号不计入且前导零计入上限；
服务端按整数值规范化订阅键。参数格式错误（包括超过该上限）时只拒绝当前 WebSocket
消息、返回 error 并保持连接，其中超长错误消息为
`reportId不能超过128位十进制数字`。服务端到客户端的 `data.reportId` 仍保持既有
JSON number 类型，不改变响应字段或类型。

武器谱类型订阅的 `architectureId` 采用与 `/llm/weaponry`、check-task 相同的入站规则：
允许 `1..9223372036854775807` 的 JSON 正整数或只含 ASCII 数字的字符串，允许前导零并按
整数值规范化订阅键。缺失、格式或范围错误时只拒绝当前 WebSocket 消息，返回上述稳定错误
文本并保持连接；服务端到客户端的 `data.architectureId` 使用 JSON number。

`params` 必须是非空 JSON 数组，且其中每个元素都必须是 JSON 对象。只要出现任意非对象元素，服务端即拒绝**整条 WebSocket 消息**，不处理其中任何订阅项，返回错误消息并保持连接；客户端随后仍可发送合法订阅消息。

`action=subscribe/query/unsubscribe`、ack 控制消息属于项目曾经自行增加的内部扩展，没有甲方或生产前端需求依据，不属于目标公开契约，已在重构阶段 1B-2 下线。收到任何带 `action` 的消息时，服务端拒绝整条消息、返回错误消息、不发送 ack，并保持连接。调用方不得再依赖旧控制消息。

#### 文件类型请求结构

```json
{
  "businessType": "file",
  "params": [
    {
      "fileName": "bded228dc94440519d87f97cfb6b520b.pdf"
    }
  ]
}
```

#### 报告类型请求结构

```json
{
  "businessType": "report",
  "params": [
    {
      "reportId": "00132"
    }
  ]
}
```

#### 武器谱类型请求结构

```json
{
  "businessType": "weaponry",
  "params": [
    {
      "architectureId": "00010502"
    }
  ]
}
```

#### 参数或控制消息错误响应

错误消息沿用既有字段结构，不增加替代控制字段；`message` 的具体文字用于说明本次错误原因，不作为稳定枚举值：

| 字段 | 类型 | 说明 |
|------|------|------|
| type | String | 固定值：`"error"` |
| message | String | 参数或控制消息错误原因 |

```json
{
  "type": "error",
  "message": "参数或控制消息错误原因"
}
```

上述校验失败只拒绝当前客户端消息，WebSocket 连接保持打开，不返回 ack。

> 实施状态：严格 `params` 元素校验，以及显式 `action` 返回错误消息、保持连接且不发送 ack 的目标行为已于 2026-07-15 明确确认，并已于 2026-07-16 在重构阶段 1B-2 的当前代码运行路径中实现。

### 消息响应参数（服务端→客户端）

#### 文件类型响应结构

| 字段 | 类型 | 说明 |
|------|------|------|
| businessType | String | 固定值："file" |
| data.fileName | String | 文件对象名（业务主键） |
| data.progress | Number | 解析进度（0.0～1.0） |

```json
{
  "businessType": "file",
  "data": {
    "fileName": "bded228dc94440519d87f97cfb6b520b.pdf",
    "progress": 0.69
  }
}
```

#### 报告类型响应结构

| 字段 | 类型 | 说明 |
|------|------|------|
| businessType | String | 固定值："report" |
| data.reportId | Long | 报告唯一标识 |
| data.progress | Number | 生成进度（0.0～1.0） |

```json
{
  "businessType": "report",
  "data": {
    "reportId": 132,
    "progress": 0.89
  }
}
```

#### 武器谱类型响应结构

| 字段 | 类型 | 说明 |
|------|------|------|
| businessType | String | 固定值：`"weaponry"` |
| data.architectureId | Long | 规范化后的知识谱系唯一标识 |
| data.progress | Number | 解析进度（0.0～1.0） |

```json
{
  "businessType": "weaponry",
  "data": {
    "architectureId": 10502,
    "progress": 0.42
  }
}
```

---

## 5. 大模型回调接口

**接口路径：** `/llm/callback`  
**请求方法：** `POST`  
**功能说明：** 大模型处理完成后回调业务系统，返回结构化结果

> 可靠投递目标规则（2026-07-16 首次确认，2026-07-29 扩展显式恢复语义）：file/report/weaponry 的旧执行在外发前必须
> 复核同一业务键的最新内部任务身份；若新执行已经先受理，旧回调判定为过期并跳过。
> 如果旧回调已经发出、但因读取超时、连接中断或响应丢失而无法判断接收方是否处理，
> 服务端不自动重试，并冻结同业务键的新任务受理，复用对应提交接口既有 HTTP 409
> 单字段错误结构。三类业务均允许调用方通过新的 `/llm/check-task` 请求明确授权
> at-least-once 补发；同一请求先完整校验并按规范化业务键去重，每个键最多竞争一次发送权。
> 该补发可能产生重复业务回调，接收方必须分别按 `fileName`、规范化 `reportId` 或规范化
> `architectureId` 与业务结果幂等处理。旧 Worker/旧进程必须已经停止或被隔离，
> latest execution、attempt、租约和 fencing 仍会在每次外发前复核。上述内部状态、
> 确认信息和任务身份均不会加入回调载荷或任何公开请求参数。

### 文件解析回调参数

| 参数 | 类型 | 说明 |
|------|------|------|
| businessType | String | 固定值："file" |
| data.fileName | String | 文件对象名（业务主键） |
| data.country | String | 国家（解析结果） |
| data.channel | String | 资料来源机构（解析结果）；只能返回请求 `params[].channel` 候选中的 `value`，请求未提供有效候选时返回空字符串 |
| data.maturity | String | 成熟度（解析结果） |
| data.security | String | 密级（解析结果）；从请求 `params[].security` 候选 value 中选取，文件开头未见相关说明时默认为“公开” |
| data.format | String | 原生格式（解析结果） |
| data.status | String | 解析状态（0未解析 1解析中 2已解析 3解析失败）（解析结果） |
| data.architectureId | Long | 领域体系ID（解析结果） |
| data.fileDataItem | Object | 文件解析详细数据（见下表） |
| msg | String | 回调消息（如"解析成功"） |

### fileDataItem字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| fileName | String | 文件名(对象名) |
| dataTime | String | 资料年代（ISO8601）yyyy-MM-dd |
| keyword | String | 关键词；使用英文逗号分隔 5～10 个关键词，分类路径关键词在前、内容关键词在后；内容关键词优先使用 `summary` 中的原词，数量不足时可使用正文明确出现的核心术语补足 |
| summary | String | 中文摘要；对明确识别为新闻、报告或科普资讯文稿的材料，分别生成 100～300 字、300～500 字或 100～300 字摘要，并添加对应的固定材料类型前缀（前缀计入字数）；新闻和科普资讯使用单段，报告可在同一字符串中换行分层；标准、规范、手册、目录、表格等不强制归入上述三类，不添加材料类型前缀，只遵守通用的客观、非空和最多 500 字要求 |
| score | Integer | 资料来源权威性评分，必填且仅返回 `95`、`85`、`75`、`65`、`55` |
| fileNo | String | 文件编号 |
| source | String | 资料来源 |
| originalLink | String | 原文链接 |
| language | String | 语种 |
| dataFormat | String | 资料格式 |
| associatedEquipment | String | 所属装备 |
| relatedTechnology | String | 所属技术；使用英文逗号分隔、最多 10 项，返回中文规范技术名称；外文资料允许将正文技术术语准确翻译为中文，只要中文名称与原文技术证据语义对应；无合格项时返回空字符串，不得输出装备名称、装备型号等非技术域内容 |
| equipmentModel | String | 装备型号 |
| documentOverview | String | 文件概述 |
| originalText | String | 文件原文 |
| documentTranslationOne | String | 文件翻译单语 |
| documentTranslationTwo | String | 文件翻译双语 |
| militaryName | String | 国军标名称；仅当最终分类命中 `architectureStandardList` 范围时返回 |
| num | String | 编号；仅当最终分类命中 `architectureStandardList` 范围时返回 |
| startTime | String | 发布时间，格式 `yyyy-MM-dd`；仅当最终分类命中 `architectureStandardList` 范围时返回 |
| implTime | String | 实施时间，格式 `yyyy-MM-dd`；仅当最终分类命中 `architectureStandardList` 范围时返回 |
| approvalDept | String | 批准部门；仅当最终分类命中 `architectureStandardList` 范围时返回 |

`keyword` 必须保持 5～10 个的数量范围，不得为满足数量加入与材料无关的词。若模型未生成摘要、已标注类型的摘要长度越界、关键词少于 5 个，或 `relatedTechnology` 缺少可核验的原文术语映射，该文件解析任务仍按成功处理；服务端会丢弃无法核验的所属技术，并对可检测到的不合格内容记录 warning 日志。外文所属技术的原文术语映射仅用于服务端内部核验，不新增回调字段。

### 文件回调请求示例

#### 解析成功

```json
{
  "businessType": "file",
  "data": {
    "fileName": "string",
    "country": "string",
    "channel": "string",
    "maturity": "string",
    "security": "公开",
    "format": "string",
    "status": "2",
    "architectureId": 1768464916588441,
    "fileDataItem": {
      "fileName": "string",
      "dataTime": "2026-02-09",
      "keyword": "string",
      "summary": "string",
      "score": 85,
      "fileNo": "string",
      "source": "string",
      "originalLink": "string",
      "language": "string",
      "dataFormat": "string",
      "associatedEquipment": "string",
      "relatedTechnology": "string",
      "equipmentModel": "string",
      "documentOverview": "string",
      "originalText": "string",
      "documentTranslationOne": "string",
      "documentTranslationTwo": "string",
      "militaryName": "GJB 9001C-2017 质量管理体系要求",
      "num": "GJB 9001C-2017",
      "startTime": "2017-05-18",
      "implTime": "2017-07-01",
      "approvalDept": "中央军委装备发展部"
    }
  },
  "msg": "解析成功"
}
```

#### 解析失败（更新状态）

```json
{
  "businessType": "file",
  "data": {
    "fileName": "string",
    "status": "3"
  },
  "msg": "解析失败"
}
```

### 报告生成回调参数

| 参数 | 类型 | 说明 |
|------|------|------|
| businessType | String | 固定值："report" |
| data.reportId | Long | 报告唯一标识 |
| data.details | String | 报告内容（HTML富文本格式） |
| data.status | String | 报告状态（0生成中 1已生效 2生成失败） |
| msg | String | 回调消息（如"生成成功"） |

> 空结果兼容语义（2026-07-16 已确认）：如果报告 RAG 返回 `null`、空字符串或仅包含空白
> 字符，服务端仍按生成成功处理，将 `data.details` 规范化为
> `<div class="report-content"><pre></pre></div>`，并发送 `data.status="1"`、
> `msg="生成成功"`。该规则不增加任何回调字段；空结果质量仅在服务端内部记录和监控。

### 报告回调请求示例

#### 生成成功

```json
{
  "businessType": "report",
  "data": {
    "reportId": 132,
    "status": "1",
    "details": "<div class=\"report-title\">\n  <span style=\"color: #ff6b6b;\">📄</span>\n  <span style=\"margin-left: 8px; font-size: 20px;\">报告内容</span>\n</div>"
  },
  "msg": "生成成功"
}
```

#### 生成失败（更新状态）

```json
{
  "businessType": "report",
  "data": {
    "reportId": 132,
    "status": "2",
    "details": ""
  },
  "msg": "生成失败"
}
```

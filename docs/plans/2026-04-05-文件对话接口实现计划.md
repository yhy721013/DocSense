# 文件对话接口后端实现计划

基于接口文档和后端设计文档实现三个接口：`POST /llm/chat`（SSE）、`GET /llm/chat/history`、`POST /llm/chat/delete`。

## Proposed Changes

### 环境配置

#### [MODIFY] [.env.example](file:///c:/Files/Codes/DocSense/.env.example)

新增 `DOCSENSE_CHAT_DB` 环境变量示例。

#### [MODIFY] [.env](file:///c:/Files/Codes/DocSense/.env)

在现有 `.env` 中追加 `DOCSENSE_CHAT_DB`（若文件不存在则不创建，由用户自行管理）。

---

### 核心基础层

#### [MODIFY] [settings.py](file:///c:/Files/Codes/DocSense/app/services/core/settings.py)

新增 `CHAT_DB_PATH` 路径常量（从环境变量 `DOCSENSE_CHAT_DB` 读取，默认 `.runtime/chat_sessions.sqlite3`）。

---

#### [MODIFY] [database.py](file:///c:/Files/Codes/DocSense/app/services/core/database.py)

新增 `ChatDatabaseService` 类（连接独立数据库），包含：
- `_init_db()` — 建 `chats` 表（schema 见设计文档）
- `create_chat / get_chat / update_file_names / delete_chat`

---

### 工具与外部边界层

#### [MODIFY] [anythingllm_client.py](file:///c:/Files/Codes/DocSense/app/services/utils/anythingllm_client.py)

**重命名**：`create_workspace` → `create_rag_workspace`（原方法用于文件解析/谱系提取，prompt 和 chatMode 均为 RAG query 专用）。

需同步更新调用方：
- [ensure_workspace](file:///c:/Files/Codes/DocSense/app/services/utils/anythingllm_client.py#L106)
- [rag_pipeline.py L52](file:///c:/Files/Codes/DocSense/app/services/utils/rag_pipeline.py#L52)
- [analysis_service.py L591](file:///c:/Files/Codes/DocSense/app/services/llm_service/analysis_service.py#L591)

**新增方法**：

| 方法 | 说明 |
|------|------|
| `create_chat_workspace(name)` | 创建对话 Workspace（`chatMode: "chat"`，通用对话 prompt） |
| `stream_chat_to_thread(workspace_slug, thread_slug, message, mode="chat")` | 流式 chat，yield 文本片段 |
| `get_thread_chats(workspace_slug, thread_slug)` | 获取 Thread 历史消息 |
| `delete_workspace(workspace_slug)` | 删除整个 Workspace |
| `update_embeddings_batch(workspace_slug, adds, deletes)` | 批量增删文档嵌入 |

---

### 业务层

#### [NEW] [chat_service.py](file:///c:/Files/Codes/DocSense/app/services/llm_service/chat_service.py)

对话业务主流程：
- `handle_chat_stream()` — SSE 生成器（新建/续接对话 → 引用文档 → 流式推送）
- `get_chat_history()` — 读 AnythingLLM Thread 历史
- `delete_chat()` — 清理 Workspace + Thread + 数据库记录

---

### 接口层

#### [MODIFY] [llm.py](file:///c:/Files/Codes/DocSense/app/blueprints/llm.py)

新增 `chat_db` 实例化，注册 3 条路由：
- `POST /llm/chat` — 参数校验 → SSE Response
- `GET /llm/chat/history` — 查询历史
- `POST /llm/chat/delete` — 删除对话

---

## Verification Plan

### 自动化测试

新建 `tests/test_chat.py`，覆盖参数校验（400/404）、删除（404/200）、历史查询等场景。

```bash
python -m pytest tests/test_chat.py -v
```

### 回归测试

```bash
python -m pytest tests/ -v
```

> [!IMPORTANT]
> 根据用户要求，不启动 Flask 主服务（`run.py`），仅通过单元测试验证正确性。

# 阶段 2-0 完成验收记录

## 1. 结论

阶段 2-0 的十个步骤已按顺序完成。每一步均在进入下一步前通过定向测试、静态合同或只读诊断，
并完成“是否需要商讨”的检查。未发现需要修改公开接口文档、增删前后端参数或新增公开状态的事项；
直接父节点专项仍按自身门禁保持阻塞，但不阻塞阶段 2 通用任务内核。

最终复核基准为 `feat/weaponry-chat` 分支、HEAD
`9ed1281601a285e37093eaa8ea0b1aa620b709a0`。相对初次冻结点新增的提交只包含重构计划文档，未触及
运行代码、测试、脚本、环境样例、`run.py` 或接口文档；它正是本轮采用的阶段 2 设计权威。

本阶段只新增内部文档、机器可读黄金资产、离线测试和只读预检脚本，没有修改运行时代码、
`.env.example`、`run.py` 或 `docs/接口文档/`，也没有启动项目主进程和后台服务。

## 2. 十步验收

| 步骤 | 交付与验收结果 | 商讨检查 |
| --- | --- | --- |
| 1 | 冻结当前 Git、Schema、调用链、运行拓扑与过渡所有权。 | 无公开契约问题。 |
| 2 | 冻结 Task/Attempt/Step/Event、Authority、状态机、序号、恢复协议与 Canonical JSON。 | 无未决状态映射。 |
| 3 | 固定接口目录精确文件集及逐文件规范化 SHA-256。 | 接口零差异。 |
| 4 | 固定 v2 路径/身份、旧库只读预检、新空库初始化、文件集归档和回滚算法。 | 旧库无活动/unknown/未清理阻塞事实。 |
| 5 | 覆盖全部 `app/services` 文件，并为当前 58 张显式 SQLite 表冻结唯一目标 Writer。 | 无双写或循环迁移需求。 |
| 6 | 冻结 73 个 Runtime Config 键的唯一所有者、默认值、校验、日志与样例状态。 | 新 Task 键保持 planned，未提前改环境样例。 |
| 7 | 冻结 Callback History 非权威语义、Knowledge 阶段 3A 输入和 Web Adapter 边界。 | 允许连接局部 `RLock`，禁止后台线程/基础设施构造。 |
| 8 | 将直接父节点专项的 5 个未决事项独立隔离，保留 Weaponry v3。 | 专项需另行接口确认，但不阻塞通用内核。 |
| 9 | 冻结 Report/Weaponry/Analysis 共 42 个最终 Step、六类 Schema、六张恢复矩阵和输入 Profile。 | 复合外部边界均 fail closed，无未登记模型操作。 |
| 10 | 冻结一内核三实例、公平 limiter、12 个原子 UoW、Clock/owner/租约不等式和受理解锁矩阵。 | 无需新增公开状态或参数。 |

## 3. 自动验证证据

执行命令：

```text
venv\Scripts\python.exe -B -m unittest \
  tests.test_stage2_baseline_assets \
  tests.test_stage2_task_control_preflight -v
```

结果：发现 37 项，执行 37 项，失败 0，错误 0，跳过 0。

覆盖范围包括：接口哈希、Task/Attempt/Step 分层、Authority、Reaper、Canonical Profile、Services 与
SQLite Writer、Runtime Config、Callback/Knowledge/Web 边界、直接父节点专项隔离、三业务 Step 与
Recovery Registry、Executor/UoW/Clock/租约/受理解锁拓扑，以及临时 SQLite 故障注入预检。

两个新增大型 JSON 资产另经 `python -m json.tool` 解析通过。`git diff --name-only --
docs/接口文档 run.py` 无输出。

## 4. 旧库只读预检复核

2026-08-12 总体验收时，标准 `mode=ro` 在当前目录访问条件下返回 `unable to open database file`；该次
失败没有降级为可写连接，也没有创建或修改数据库。随后只读确认旧库主文件存在、没有 WAL/SHM，且
当前未发现 `python/pythonw` 进程，再按冻结协议显式启用 immutable 离线快照模式复核：

- 状态：`safe_for_empty_v2_initialization`；
- 14 条 blocker rule 计数全部为 0；
- Schema 错误 0；
- 新 v2 数据库不存在；
- 旧路径摘要：`1EF33B617F39BE4AE8B32EE73CDF3C5964D94128540FC22B9FE6B8786F5734BE`；
- 新路径摘要：`8A3641D42F9ED6FFD1C8B285D2C8105DA0DD818F82BEFAF0B51E5FF47A4D1B19`；
- 主库大小和 mtime、WAL/SHM/journal 文件集在预检前后保持不变。

该结论只证明本次 Windows/SQLite 离线现场满足“未来可初始化空 v2”的编码前门禁；它不等于已经
创建新库，也不证明多实例、可靠队列、真实供应商或生产容量。

## 5. 阶段 2-1 入口条件

进入 2-1 时必须继续遵守：

1. 只实现纯 Domain、Port 与 Fake，不创建新数据库、不切换生产 Store；
2. 三业务 Step/Recovery Registry 和 Runtime 拓扑作为编码权威，变化必须先更新资产并评审；
3. 公开接口哈希必须持续为零差异；任何可能改变公开契约的需求先中断并确认；
4. 外部 unknown、Profile 不匹配、时钟异常和归属不明始终 fail closed；
5. Fake/临时 SQLite 结果不得表述为多实例、供应商容量或生产验收。

## 6. 最终商讨检查

阶段 2-0 门禁要求的公开状态映射、业务 Step、Profile 字段和 SQLite 切换动作均已确定。当前没有需要
中断并与用户商讨的阶段 2-0 遗留项；后续若要进入阶段 2-1，应作为新的、逐步验证的实施波次执行。

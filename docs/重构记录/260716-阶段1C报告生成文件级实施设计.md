# 阶段 1C：报告生成垂直切片文件级实施设计

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 编写日期 | 2026-07-16 |
| 适用基线 | `refactor/concurrency@52da041` |
| 文档层级 | L3 文件级实施设计 |
| 所属波次 | 阶段 1C：`POST /llm/generate-report` |
| 上游文档 | `260716-阶段1C至11滚动实施计划.md`、`260715-统一任务最小契约与Progress语义预设计.md` |
| 当前状态 | **阶段 1C 已关闭**：1C-0～1C-7 已完成并通过契约、并发、故障、架构、静态旧引用与扩大离线回归。当前开发分支的 Flask 报告路由已切换为 Parser → Submit → Presenter，并装配 SQLite 持久积压、一条报告执行 Worker、两条隔离维护线程、跨进程单实例锁、Callback Guard 与资源恢复；尚未部署生产，RabbitMQ 可靠队列和多实例能力仍待阶段 3～6 |
| 接口影响 | 已授权：成功改为 HTTP 202 空响应体；活动任务及 callback outcome unknown 冻结均返回既有 HTTP 409 单字段错误体；reportId 数字部分最多 128 位；`templateDesc`/`requirement` 兼容字符串化。不得增删任何请求或回调参数 |
| check-task 最终口径 | **2026-07-17 修订**：甲方规定保留报告回调的请求内同步恢复。阶段关闭后的审查补强已让该入口与报告 Worker 共用 latest-wins、lease/fencing 和 Callback Guard；未来可靠队列只增加后台兜底，不替换同步入口 |
| 数据策略 | 当前 SQLite 测试数据无需迁移；1C 使用兼容 SQLite Adapter，阶段 3 再切同步 SQLAlchemy/MySQL Repository |
| 运行限制 | 不启动 `run.py`；测试不得连接真实 AnythingLLM、文件服务器或回调方 |

---

## 1. 目标与非目标

### 1.1 目标

1. 把报告生成从 Flask 路由和巨型 Service 拆成 Report Domain、Application、Ports 和 Adapters。
2. 提交用例原子完成活动任务判断、不可变 execution ID/请求快照创建和最新投影更新。
3. 同一 `reportId` 并发提交时只允许一个 HTTP 202，其余返回既有 HTTP 409。
4. 执行入口只接收内部 `task_id/execution_id`，从持久化快照恢复全部业务输入。
5. 所有进度、结果、回调和公开投影写入都携带 expected execution ID，旧执行不得覆盖新执行。
6. 路由不创建 daemon thread；正常积压保存在数据库，内存只使用常量空间的有界唤醒。
7. 把下载、模板提取、文件规范化、多文档 RAG、Progress、Callback 和 Artifact 变成显式 Port。
8. 保持既有报告 Prompt、HTML、进度、公开状态和回调载荷；只应用已批准的 202/409 差异。
9. 为阶段 2 统一任务隔离、阶段 3 SQLAlchemy/MySQL、阶段 4 Outbox 和阶段 5 Worker 提供
   可替换边界，不把阶段 1C 描述成可靠队列已经完成。

### 1.2 非目标

- 不部署或接入 MySQL、RabbitMQ、Redis、MinIO、Celery 或 FastAPI。
- 1C 原始波次不迁移 file/weaponry 的 check-task 实现；报告类型已在 2026-07-17 阶段关闭
  审查后接入共享 Guard 的同步恢复应用服务，且未改变公开请求参数或响应契约。
- 不给 analysis、weaponry、chat 或 reassign 顺带做业务迁移。
- 不实现多实例执行、跨进程租约或 Redis 通知。1C 只在 SQLite Guard 内预演 lease token
  与单调 fencing token，证明迟到 Worker 无法覆盖新租约；它不等同于阶段 3～5 的跨实例
  MySQL/Worker 正确性。
- 不新增 URL 白名单、DNS 重绑定、重定向校验或其他 URL Policy。
- 不冻结 MinIO 7/30/180 天等具体保留周期，也不启用自动 TTL。
- 不迁移 SQLite 历史测试数据，不删除旧 Service 或旧表。
- 不新增、删除、重命名或改变任何前后端请求参数、回调字段或 Progress 消息。

---

## 2. 当前代码事实与问题

### 2.1 1C 开工时调用链（历史基线）

> 下列调用链用于说明 1C 的起始缺陷。1C-6 已在当前开发分支移除报告路由的 daemon
> 线程和遗留执行调用，实际代码现为 Parser → SubmitReportTask → 持久化 accepted →
> LocalReportTaskDispatcher → RunReportTask；遗留服务只保留兼容测试用途。

```text
POST /llm/generate-report
  → app/blueprints/llm.py::llm_generate_report
      → 解析/修改 request dict
      → LLMTaskService.create_report_task(reportId, payload)
      → LLMProgressHub.publish(...)
      → threading.Thread(daemon=True)
          → UploadTaskLimiter.run(run_report_task, request_payload=整份请求)
              → 下载源文件/模板
              → normalize_file_for_llm / extract_text_from_word
              → 直接创建 AnythingLLMClient
              → run_anythingllm_rag
              → 按 reportId 更新任务/进度/结果
              → post_callback_payload
```

### 2.2 已核实的结构和并发缺陷

| 事实 | 风险 |
| --- | --- |
| 路由在 `app/blueprints/llm.py` 直接创建 daemon thread | 请求量会转化为线程量；Web 进程退出时任务丢失；路由承担执行策略 |
| `run_report_task` 接收整份可变请求字典 | 无法由队列仅凭 task ID 恢复；调用方与执行器共享数据形状 |
| `llm_tasks` 以 `(business_type, business_key)` 为主键并 upsert | 新执行覆盖旧执行行；没有追加式 execution 事实 |
| `create_report_task` 无活动状态原子检查 | 两个并发请求都可能受理，且后一次覆盖前一次 execution ID |
| 进度、结果和 callback 更新只按 `reportId` | 旧线程可写入新任务投影，破坏 latest-wins |
| `get_task_by_execution_id` 仍从最新投影表读取 | 同业务键创建新执行后，旧执行输入无法再按 ID 恢复 |
| 报告 Service 直接创建旧 `AnythingLLMClient` 和读取全局配置 | 业务层依赖供应商与运行环境，难以测试和替换 |
| 旧 Client/RAG 流程没有由报告执行器显式关闭任务级 Transport，workspace/thread/上传文档清理也不完整 | 长时间运行可能泄漏连接或外部临时资源，失败后遗留现场不可追踪 |
| RAG 返回 `None`/空字符串时仍会被包装为空 HTML 并按成功回调 | 2026-07-16 已确认这是必须保留的兼容契约；1C 只增加可观测性，不得改成失败 |
| 文件名包含 reportId/index，缺少 task 级命名空间 | 同业务键重跑、崩溃清理和并发下载容易相互覆盖 |
| callback Client 把所有 RequestException 折叠成 `False` | 无法区分明确未送达、非 2xx 和发送后结果未知 |
| 当前 limiter 只限制实际执行，不限制等待线程数量 | 大量积压时可能创建大量阻塞线程，内存与调度不可控 |

### 2.3 当前行为必须保留的部分

- `businessType` 必须为 `report`。
- `params` 必须是非空数组；2026-07-16 已确认任一非对象元素使整次请求返回 HTTP 400
  `params元素必须是对象`，不得过滤后继续处理。业务仍只读取第一个对象。
- `reportId` 仍接受 JSON 整数或十进制整数字符串，不受 32/64 位机器整数范围限制，
  但数字部分最多为 128 位；正负号不计入、前导零计入该上限，并按整数值规范化。
- `templateDesc` 与 `requirement` 缺失时使用空字符串，历史非字符串 JSON 值在 Web 边界
  按旧执行链语义转换为字符串；领域层和持久化快照只接收冻结后的文本。
- 顶层请求必须为 JSON 对象，否则返回 HTTP 400 `请求体必须是JSON对象`。
- `filePathList` 必须为非空数组，每一项必须是非空字符串；非法第 N 项返回 HTTP 400
  `filePathList中第N项不是有效字符串`。`templateOutline` 必须为非空字符串。
- MHTML/MHT 规范化失败时继续回退到原下载文件。
- Word 模板提取不到有效文字时任务失败，不调用 RAG。
- 报告纯文本结果仍由既有 HTML 规则包装。
- 成功回调 `status="1"`、`msg="生成成功"`；失败回调 `status="2"`、`details=""`、
  `msg="生成失败"`。
- Progress 仍使用 `businessType="report"`、既有 `reportId` 和 0～1 `progress`，不新增字段。

---

## 3. 已确认契约和停止条件

### 3.1 请求与响应

| 场景 | 目标行为 |
| --- | --- |
| 合法且可靠受理 | HTTP 202，响应体零字节；不公开内部 task ID |
| 同 reportId 最新任务公开状态为 `0` | HTTP 409，`{"error":"任务正在处理中"}` |
| 同 reportId callback 为 `delivery_outcome_unknown` | HTTP 409，复用同一既有错误体，直至内部人工解除 |
| 旧 callback 正在发送 | 新提交最多等待当前 callback timeout；期间释放则继续受理，超时仍占用则返回同一 409 |
| 参数错误 | 保持接口文档既有 HTTP 400 和错误文本 |
| 持久化受理失败 | 不得返回 202；保持既有单字段服务端错误边界，不新增公开字段 |

### 3.2 202 的准确含义

阶段 1C 当前开发适配下，202 表示：

1. execution ID 和规范化请求快照已在 SQLite 事务中提交；
2. 最新 report 投影已指向该 execution ID；
3. 本地数据库扫描执行器能够发现 accepted 任务；
4. 不表示源文件已下载、输入字节已冻结、RAG 已开始、报告已生成或 callback 已完成。

阶段 4 以后，202 还必须表示任务事实与 Outbox 同事务提交。1C 不得提前声称这项能力。

### 3.3 必须立即停止并确认

- 实现需要改变任一请求字段、字段类型、必填性或回调字段。
- 需要为 stale、cancelled、outcome unknown 增加新的公开报告状态。
- 不能复用既有 409 错误体，需要新增错误字段或新状态码。
- 多文档 RAG 迁移无法保持当前 Prompt、上传顺序或最终 HTML/回调。
- 可靠受理无法在不启动无界线程/内存队列的前提下完成。
- Callback Guard 无法在不改变公开参数的前提下实施已确认的 unknown 冻结语义。
- 除 D0-23 已确认的三项严格校验外，还需要改变报告参数错误优先级、错误文本或状态码；
  任何进一步变化仍须先确认并同步接口文档。
- 发现本轮必须迁移真实历史数据或必须启动外部后台服务才能继续。

---

## 4. 目标模块和依赖方向

```text
Flask Report Adapter
  → SubmitReportTask
      → Report Domain
      → Task Submission/Unit of Work Port
      → Progress Publisher Port
      → ReportTaskDispatcher Port
  → Report Presenter

Local Report Worker Adapter
  → RunReportTask(task_id)
      → Task Execution/Input Port
      → SourceFile Port
      → ArtifactStore Port
      → TemplateExtractor Port
      → ReportRag Port
      → CallbackDelivery/Guard Port
      → Progress Publisher Port
```

依赖规则：

- `report/domain` 只依赖 Python 标准库和本模块纯类型。
- `report/application` 只依赖 report domain、report ports 和 tasks 对外 Port。
- report application 不导入 Flask、SQLAlchemy、sqlite3、requests、Celery、AnythingLLM 或旧 Service。
- Adapter 可以依赖遗留实现，但必须把供应商/数据库异常映射为稳定内部错误。
- Flask Adapter 不访问 SQLite、不创建线程、不执行下载/RAG/callback。
- tasks 模块拥有通用任务身份与执行事实；report 拥有报告输入规则、Prompt、结果和回调载荷规则。

---

## 5. 文件级变更设计

以下是计划落点。实施中若发现文件职责需要微调，可以在不改变依赖方向和契约的前提下
调整文件名；涉及数据所有权、事务或接口行为的调整必须重新评审。

### 5.1 新增 Report 模块

| 文件 | 职责 |
| --- | --- |
| `app/modules/report/README.md` | 数据所有权、依赖方向、公开应用入口和当前能力边界 |
| `app/modules/report/domain/models.py` | `ReportId`、`ReportSubmission`、`ReportInputSnapshot`、`ReportResult`、`ReportCallbackPayload` 等不可变类型 |
| `app/modules/report/domain/errors.py` | 参数已由 Web 校验后的内部错误、活动冲突、stale、输入/模板/RAG/产物错误 |
| `app/modules/report/domain/rules.py` | HTML 规范化、回调载荷、任务级名称和不含 I/O 的报告规则 |
| `app/modules/report/application/submit_report.py` | 原子受理、callback Guard 即时冲突、初始 Progress 和 Dispatcher 唤醒 |
| `app/modules/report/application/run_report.py` | 只按 task ID 恢复并执行下载、模板、RAG、结果、callback 和清理 |
| `app/modules/report/ports/files.py` | 源文件下载、规范化和模板提取能力；不暴露 requests/Flask |
| `app/modules/report/ports/artifacts.py` | 任务对象写入、读取/物化、校验和、删除资格与隔离语义 |
| `app/modules/report/ports/rag.py` | 报告专用多文档 RAG 请求/结果，不复用单文档 `DocumentRagSession` 的错误语义 |
| `app/modules/report/ports/audit.py` | 原子持久化模型调用 trace、外部资源生命周期和审计门禁结果；没有成功审计就不能进入业务成功路径 |
| `app/modules/report/ports/callbacks.py` | 精确 callback outcome、Guard 获取/完成/人工解除协议 |
| `app/modules/report/ports/dispatcher.py` | `dispatch(task_id)` 唤醒契约；任务事实不得仅保存在实现内存中 |
| `app/modules/report/adapters/legacy_files.py` | 包装现有 downloader、MHTML normalizer、Word extractor 和 `prepare_upload_files` |
| `app/modules/report/adapters/local_artifacts.py` | 当前任务级本地目录 Adapter；阶段 3 替换 MinIO Adapter |
| `app/modules/report/adapters/anythingllm_rag.py` | 包装当前多文件 RAG 流程，使用任务级 Client/Factory，不在业务层创建旧 Client |
| `app/modules/report/adapters/interaction_audit.py` | 复用 AnythingLLM 总计划定义的原子交互审计入口，不在报告应用层直接写审计表 |
| `app/modules/report/adapters/http_callback.py` | HTTP callback、历史载荷保存和错误阶段分类，不改变出站 JSON |
| `app/modules/report/adapters/local_dispatcher.py` | SQLite 扫描 + 常量空间唤醒的单实例兼容执行器 |
| `app/modules/report/__init__.py` 及各子包 `__init__.py` | 只导出正式应用入口、Port 和 DTO，不导出具体遗留 Adapter |

### 5.2 扩展 Tasks 模块

| 文件 | 变更 |
| --- | --- |
| `app/modules/tasks/domain/models.py` | 增加阶段 1C 所需 submission/input/execution 最小 DTO；不提前塞入 Celery/RabbitMQ 字段 |
| `app/modules/tasks/ports/task_commands.py` | 新增原子受理、按 task ID 读取、条件领取/进度/终态和 latest 检查 Port |
| `app/modules/tasks/ports/progress.py` | 增加 `ProgressPublisherPort`，与现有 Snapshot/Subscription Port 共享稳定 DTO |
| `app/modules/tasks/adapters/legacy_task_commands.py` | 把兼容 SQLite `LLMTaskService` 映射为上述 Port；明确单实例能力 |
| `app/modules/tasks/README.md` | 记录 report 成为首个生产 command 消费方，但 MySQL/Outbox/Worker 尚未实现 |

`task_commands.py` 的抽象由 tasks 模块表达；不得在 report 模块创建一套只服务报告、以后
再复制到 analysis/weaponry 的通用任务状态机。报告专有输入与规则仍保留在 report 模块。

### 5.3 兼容持久化和组合根

| 文件 | 变更 |
| --- | --- |
| `app/services/llm_service/task_service.py` | 增加追加式 execution/input 事实、原子 report 受理、按 expected execution ID 条件写、待执行扫描和 callback Guard 兼容方法；旧查询继续可用 |
| `app/container.py` | 显式装配 Submit/Run、Task Port、File/Artifact/RAG/Callback Adapter 和唯一 Report Dispatcher；测试可注入 Fake，不自动连接外部服务 |
| `app/adapters/web/flask/report_requests.py` | 把 Flask JSON 转成不可变 Report command；复用现有 `normalize_report_id` |
| `app/presenters/report.py` | 集中呈现 202 零字节、400/409 和既有错误 JSON；不返回 task ID |
| `app/blueprints/llm.py` | `/llm/generate-report` 只做解析、调用和 Presenter；删除本路由线程创建与业务编排 |
| `app/services/llm_service/report_service.py` | 实施期保留兼容 Facade/纯规则转发；切换稳定且无调用证据后在 1G 删除，不长期维护双实现 |
| `app/modules/README.md` | 增加 `report/` 模块职责与实施状态 |

### 5.4 测试文件

| 文件 | 覆盖范围 |
| --- | --- |
| `tests/test_report_domain.py` | ReportId、HTML、回调载荷、任务名称和纯规则 |
| `tests/test_report_application.py` | Submit/Run 的成功、失败、stale、Fake Port 调用顺序与补偿 |
| `tests/test_report_task_adapter.py` | SQLite 原子受理、追加 execution、按 ID 重载、条件写和回滚 |
| `tests/test_report_dispatcher.py` | 常量空间唤醒、FIFO 扫描、停止/重启、重复唤醒和异常隔离 |
| `tests/test_report_interaction_audit.py` | 完整 trace 原子落库、幂等、审计失败阻断成功及失败现场保留 |
| `tests/test_report_callback_guard.py` | 提交/发送竞态、等待、超时、unknown 冻结、stale 跳过和人工解除审计 |
| `tests/test_report_contract.py` | 202 空体、400、活动/unknown 409、请求与 callback 黄金样例 |
| `tests/test_report_concurrency.py` | 50 同键提交、50 不同键积压、旧 execution 与新 execution 写竞争 |
| `tests/test_architecture_boundaries.py` | report/tasks domain/application 禁止依赖 Web、SQLAlchemy、sqlite3、requests、Celery 和旧 Client |

---

## 6. 内部契约

### 6.1 ReportId

- Web 边界继续接受 JSON `int` 或十进制整数字符串，拒绝 bool、float 和非十进制字符串。
- 十进制数字部分最多为 128 位；可选正负号不计入，前导零计入输入长度。129 位输入即使
  规范化后数值很小也必须拒绝，避免请求解析、日志、索引和外部命名资源放大。
- 内部业务键使用规范化十进制文本，例如 `"+00132" → "132"`、`"-0002" → "-2"`。
- 公开回调和 Progress 中继续输出既有 JSON number；不得把内部规范化字符串泄露为新类型。
- 不受 32/64 位限制意味着阶段 3 MySQL 不能用 `BIGINT` 作为唯一业务表示；Repository
  使用至多 129 字符（负号 + 128 位数字）的规范化文本键，并在领域/Web 双层校验。

### 6.2 不可变报告输入

```text
ReportInputSnapshot
├── schema_version
├── task_id
├── report_id_key          # 规范化内部文本
├── public_report_id       # 公开 JSON number 所需值
├── source_urls[]          # 保持请求顺序和重复语义
├── template_outline_url
├── template_desc
├── requirement
├── accepted_at
└── trace_id
```

请求快照在 202 前持久化，但源文件字节在 Worker 执行时下载。URL 在排队期间内容变化、失效
或权限变化时，本次执行使用执行时结果；下载失败按既有报告失败语义收敛。

### 6.3 任务命令 Port

概念签名如下，实际命名可在保持语义的前提下调整：

```python
class TaskCommandPort(Protocol):
    def create_report_if_allowed(self, submission: ReportSubmission) -> TaskSnapshot: ...
    def get_execution(self, task_id: TaskId) -> TaskExecutionSnapshot | None: ...
    def claim(self, task_id: TaskId) -> ClaimResult: ...
    def update_progress_if_current(self, update: ExpectedProgressUpdate) -> bool: ...
    def finish_if_current(self, completion: ExpectedTaskCompletion) -> bool: ...
    def is_latest(self, task_id: TaskId, business_ref: TaskBusinessRef) -> bool: ...
    def list_accepted(self, task_type: TaskType, limit: int) -> tuple[TaskId, ...]: ...
```

契约要求：

- 所有写入按 task ID 定位 execution，并按 latest projection owner 条件更新公开投影。
- 返回 `False/stale` 是正常并发结果，Application 必须停止后续公开写和 callback。
- Repository/Adapter 不执行下载、RAG、callback、Progress 通知或线程创建。
- 一次业务事务由 Unit of Work 统一提交/回滚，Repository 方法不得私自 commit。

### 6.4 多文档 Report RAG Port

```text
ReportRagRequest
├── task_id
├── trace_id
├── ordered_source_files[]
├── prompt
├── context_name
└── conversation_name

ReportRagResult
├── raw_content
├── execution_trace
│   ├── trace_id/context refs/final_call_id
│   ├── attempts[]
│   │   ├── sequence/operation/attempt/call_id
│   │   ├── prompt_kind/prompt_digest/query_mode
│   │   ├── raw_response/sources/source verification counts
│   │   └── failure_stage/error_message
│   └── lifecycle_events[]
└── cleanup_reference
```

报告是多文档一次生成，不能强行套用当前单文档 `DocumentRagSession.analyse(file_path)` 并
改变上传/查询语义。Report Port 由 report 业务表达“按顺序准备多文档并生成一次报告”，
AnythingLLM Adapter 再复用现有 Client/Gateway 原子能力。

RAG Adapter 必须返回完整 `execution_trace` 和仍待处置的外部资源引用；打开 Session 的部分
成功、上传失败、查询失败和清理失败均不能只抛一个丢失现场的普通字符串异常。报告模块沿用
AnythingLLM 总计划中的审计硬门禁：完整 trace 原子落库成功之前，不得写任务成功、发布成功
Progress 或发送成功 callback。

成功 trace 至少包含一次模型调用；模型调用前失败允许 attempts 为空，但仍须携带任务
`trace_id`、失败阶段、错误信息和已经发生的 lifecycle events。Application 必须校验响应
trace 的 `trace_id`、Context 名称和最终响应对应关系，禁止把其他任务或不完整轨迹落入本任务。

### 6.5 交互审计 Port

```python
class ReportInteractionAuditPort(Protocol):
    def persist_trace(self, command: PersistReportRagTrace) -> AuditReceipt: ...
```

约束如下：

- `PersistReportRagTrace` 使用 execution ID 生成幂等键，并携带业务引用、冻结后的完整
  Prompt、任务 trace ID、最终调用以及完整 attempts、lifecycle events 和 trace 摘要。
- 主交互、attempts 和初始 lifecycle events 必须由一个原子入口提交；只拿到主记录 ID 不代表
  审计成功。
- 审计失败时任务进入失败语义，禁止成功 callback，并保留 Context、Conversation、全局文档
  和本地 Artifact 现场；不得先清理再尝试补审计。
- 审计成功后才允许按结果执行资源清理；close 新产生的 lifecycle/cleanup 事件以幂等追加方式
  写入。
- Port 不暴露 SQLAlchemy Session、SQLite connection 或 AnythingLLM 的供应商字段。
- 1C-4 的生产 Audit Adapter 必须把 `trace_id` 与每次 `call_id` 一并持久化并纳入摘要；若
  既有共享审计 Schema 无法无损保存这些字段，先做只增不删的内部 Schema 扩展，不能在
  Adapter 中丢弃字段或退回只有最终响应的摘要记录。

### 6.6 Callback outcome

内部至少区分：

| Outcome | 含义 | 自动重试 | 新提交 |
| --- | --- | --- | --- |
| `success` | 收到 2xx | 否 | 允许 |
| `rejected` | 收到非 2xx | 普通 Worker 不自动重试；甲方显式 check-task 可重新授权一次 | 允许；不会再有迟到 2xx |
| `definitely_not_sent` | 能确认请求未送达 | 后台 Worker 可有限重试；甲方显式 check-task 可重新授权一次 | 当前发送权释放后允许 |
| `delivery_outcome_unknown` | 请求可能已送达，但响应丢失/读取超时/连接中断 | 否 | HTTP 409 冻结，人工解除后才允许 |
| `skipped` | 未配置 callback URL | 否 | 允许 |
| `stale` | 外发前发现已不是最新 task ID | 否且禁止网络调用 | 新任务已拥有业务键 |

`delivery_outcome_unknown` 是内部结果，不新增公开 callback 状态。报告类型 check-task 已按
甲方要求保留请求内同步恢复：它读取最新终态候选后仍必须通过 Guard 原子取得发送租约，
没有发送权就不得发出 HTTP。阶段 4～6 的可靠 recovery command/Worker 是后台兜底触发源，
不能接管或改变同步入口的返回时点。
`success`、`skipped`、`rejected` 和 `definitely_not_sent` 一旦由 Guard 完成即视为本次投递
已形成明确终局，普通 Worker 重放返回 `already_completed`，不得隐式再次发送。只有甲方
check-task 显式恢复或未来可靠 recovery command 才能按既定策略重新授权；两者必须记录
触发来源并竞争同一个 execution 级 Guard。

### 6.7 已确认：空 RAG 结果仍然成功

当前 `run_anythingllm_rag` 返回 `None` 或空字符串时，旧实现会调用
`ensure_report_html("")`，得到 `<div class="report-content"><pre></pre></div>`，并发送
`status="1"`、`msg="生成成功"` 的既有成功回调。负责人于 2026-07-16 确认继续采用该语义。

实施约束：

1. `None`、空字符串以及仅包含空白字符的 RAG 结果均沿用空 HTML 成功语义。
2. 不得因此发送 `status="2"`，也不得新增“空报告”等公开状态或字段。
3. 增加 `empty_rag_result=true` 的结构化内部日志和计数指标，便于发现模型质量问题；该标记
   不进入 callback、Progress 或 HTTP 响应。
4. 仍须先完成完整 RAG trace 审计，才能写成功终态和发送成功 callback；“允许空结果”
   不豁免审计硬门禁。

---

## 7. 兼容 SQLite 数据设计

阶段 1C 需要解决旧行覆盖和按 task ID 恢复，不能只给现有 `llm_tasks` 增加几个方法。
建议以增量表保存最小追加事实，同时保留旧表作为现有查询/Progress 的最新投影：

### 7.1 `llm_task_executions`

| 字段 | 语义 |
| --- | --- |
| `execution_id` PK | 一次不可变执行身份 |
| `business_type/business_key` | report 和规范化 reportId 文本 |
| `input_schema_version/input_payload` | 202 前提交的不可变请求快照 |
| `dispatch_sequence` | 在受理写事务中分配的稳定 FIFO 序号，不依赖应用时钟 |
| `dispatch_failure_count/next_dispatch_at/last_dispatch_error` | 领取前故障次数、冷却时间和已清洗错误分类 |
| `execution_state` | accepted/running/succeeded/failed/stale |
| `progress/message/result_payload` | 本 execution 的内部事实，不因新提交被删除 |
| `callback_status/callback_outcome` | 回调兼容状态与精确内部 outcome |
| `created_at/started_at/completed_at/updated_at` | 排序、耗时和恢复审计 |

当前兼容索引覆盖 ready accepted 扫描所需的
`(business_type, execution_state, next_dispatch_at, dispatch_sequence)`，并保留业务键、
历史时间查询索引。`dispatch_sequence` 在 `BEGIN IMMEDIATE` 受理事务中生成；阶段 3 使用
Alembic 和数据库原生序列/等价机制重新建立 MySQL Schema，本表不是生产迁移脚本的替代品。

终态结果必须分为两份独立编码：execution 行只保存 `schema_version`、报告标识、状态、
空结果标记和最终 Artifact 元数据等内部恢复事实；`llm_tasks.result_payload` 只保存与既有
回调完全相同的公开投影。check-task/回调恢复只能重放公开投影，禁止把内部 wrapper、路径、
trace 或 Artifact 结构发送给甲方；大型 HTML 由公开投影和最终 Artifact 按各自责任持有，
不得再复制进内部 execution wrapper。

### 7.2 `llm_tasks` 最新投影

- 继续服务现有 check-task、Progress 和其他尚未迁移业务。
- 增加/复用 `execution_id` 作为最新投影 owner。
- Report 公开写入使用 `WHERE business_type='report' AND business_key=? AND execution_id=?`。
- 创建新 report execution 时，在同一事务插入 execution 并更新投影。
- 旧 execution 终态/进度只能更新自己的 execution 行；若不再拥有投影，不能更新公开行。

### 7.3 `callback_delivery_guards`

| 字段 | 语义 |
| --- | --- |
| `business_type/business_key` PK | 同一业务键唯一 Guard |
| `owner_execution_id` | 当前取得发送权的 task ID |
| `state` | idle/sending/outcome_unknown |
| `lease_token/lease_version` | 每次发送的随机租约身份与单调 fencing token；迟到完成必须 CAS 失败 |
| `lease_started_at/deadline_at` | 新提交等待边界和超时审计 |
| `last_outcome/error_stage` | 已清洗结果，不保存敏感响应正文 |
| `released_at/released_by/release_reason` | 人工解除审计；普通释放为空操作者 |

SQLite 通过短 `BEGIN IMMEDIATE` 事务串行化同键受理/Guard 变更；网络请求绝不在数据库事务
内执行。Guard acquire 在同一事务内权威复核 latest 和终态，不依赖事务外预检查；完成时
同时匹配 owner、lease token 和 fencing token。发送租约到期不能证明请求未到达，因此直接
冻结为 `outcome_unknown`，不得自动重抢。多实例最终正确性由阶段 3 MySQL 条件写和阶段 5
Worker Guard 实现。

### 7.4 历史数据

- 当前历史数据仅用于测试，无需把旧 `llm_tasks` 行推导成完整 execution 历史。
- 旧 file/weaponry/check-task 查询继续读取 `llm_tasks`，不能因 report 增量表而中断。
- 新测试使用空库初始化；Schema 创建必须幂等。
- 不删除、不重命名旧表或列，回滚时允许保留未使用的增量表。

---

## 8. 原子受理和并发规则

### 8.1 受理事务

```text
SubmitReportTask.handle(command)
  1. 开启短写事务
  2. 在事务内检查 callback Guard
     - sending：立即冲突
     - outcome_unknown：立即冲突
  3. 读取最新 report 投影
     - status=0：冲突
  4. 分配 dispatch_sequence 并生成 execution ID
  5. 插入 llm_task_executions accepted + input snapshot
  6. upsert llm_tasks 最新投影 status=0/owner=execution ID
  7. commit
  8. 发布初始 Progress（带 task ID）
  9. dispatcher.dispatch(task_id) 仅做常量空间唤醒
 10. Presenter 返回 202 空体
```

步骤 2～6 必须同一事务；不能 Web 层“先 get_task 再 create”。初始 Progress 和唤醒发生在
提交后。唤醒失败不删除已提交任务；周期扫描会恢复，必须记录 ERROR 和最老 accepted 年龄。

### 8.2 50 同键并发

- 50 个线程使用 Barrier 同时提交相同 reportId。
- SQLite Adapter 使用独立连接和短写事务；不得共享 Connection/Session。
- 恰好一个返回 accepted，其余全部是相同领域冲突；不允许数据库锁异常泄露为随机 500。
- 数据库只有一个新 execution、一个最新投影 owner、一个初始 Progress owner。
- 任何失败事务不得留下孤儿 execution 或已改变的投影。

### 8.3 旧执行写保护

每次进度、结果、callback 和清理提交都携带 task ID：

```sql
UPDATE llm_tasks
SET ...
WHERE business_type = 'report'
  AND business_key = :report_id
  AND execution_id = :expected_execution_id;
```

rowcount 为 0 时必须记录 stale 并停止公开写；不得回退成只按 reportId 重试。

若旧 execution 当时仍处于 `accepted/running`，条件写发现 latest owner 已变化时还必须在
同一短事务把该 execution 收敛为 `stale` 并记录完成时间，避免它永久污染运行中数量、恢复
扫描和容量指标。已经终态或身份不匹配的行不得被该补偿覆盖。

---

## 9. 无积压数量上限的本地 Dispatcher

### 9.1 设计

阶段 1C 仍没有 RabbitMQ，但不能继续“每请求一个线程”或把积压放入
`ThreadPoolExecutor` 的无界内存队列。兼容 Adapter 使用：

- 数据库 `llm_task_executions.execution_state='accepted'` 作为待处理事实；
- 一条固定 Report 执行 Worker（当前 AnythingLLM 上传安全基线仍为并发 1）；
- 一个 `threading.Event` 或等价常量空间 wake signal，不保存 task 列表；
- 周期按受理事务内的 `dispatch_sequence` 扫描已到重试时间的 accepted 任务；
- 条件 `accepted → running` 领取，rowcount=1 才执行；
- 领取前异常通过 `next_dispatch_at` 持久冷却，不能让毒任务热循环或阻塞后续页；
- 资源恢复与队列诊断使用两条固定维护线程，不被重型执行阻塞；
- 每次完成/异常后继续扫描，直到当前积压排空。

`dispatch(task_id)` 只校验 task ID 并设置 wake signal。重复唤醒可以合并，唤醒丢失由周期
扫描兜底。因此积压数量增长只增加 SQLite 行，不增加等待线程或 Python Queue 元素。

### 9.2 能力边界

- 当前只支持一个应用实例；组合根必须声明并校验 `single_instance`，生产 Local Dispatcher
  必须取得同一运行目录下的跨进程内核文件锁，并禁止 preload/fork。
- accepted 任务可在进程重启后由扫描发现。
- running 任务在进程崩溃后的 lease/reaper 恢复仍属于阶段 2；1C 不冒充完整可靠队列。
- callback check-task 不使用任务执行 Dispatcher；报告类型在请求线程内调用同步恢复应用
  服务。未来 MySQL Outbox + RabbitMQ callback Worker 只能作为后台兜底，并复用同一 Guard。
- 不配置业务 backlog 上限或正常积压 503；基础设施无法可靠写入时不得返回 202。

### 9.3 生命周期

- Dispatcher 在 Bootstrap 中创建、显式 `start()` 一次，并提供幂等 `stop()/close()`。
- 单元测试默认注入 Fake/Manual Dispatcher，不启动真实后台线程。
- 进程退出时停止领取新任务；等待许可者可取消；以一个总超时等待执行/维护线程并记录
  未完成 execution ID。超时保持 `stopping`，只有全部线程真实退出后才能标记 `closed`。
- 不使用 `run.py` 测试生命周期；通过直接构造 Bootstrap/Dispatcher 完成离线验证。

---

## 10. RunReportTask 执行步骤

### 10.1 主路径

1. 按 task ID 读取 execution 和输入快照，校验类型为 report。
2. 条件领取 `accepted → running`；重复派发、终态或 stale 直接幂等返回。
3. 创建 `runtime/tasks/{task_id}/` 任务级目录和 Artifact 上下文。
4. 按 `source_urls` 顺序在执行时下载源文件；文件名使用 task ID + sequence，不能只用 reportId。
5. 调用规范化 Port；保持 MHTML 规范化异常回退原文件的既有行为，并记录 warning。
6. 下载 Word 模板，提取文字；空文字抛出稳定输入错误，不调用 RAG。
7. 使用提取文字替换内部 Prompt 输入中的 templateOutline；不改变原始持久化快照。
8. 构建与当前等价的报告 Prompt；完整 Prompt 不写普通日志。
9. 通过任务级 `ReportRagPort` 上传有序文档并生成一次报告，取得完整 trace 和外部资源引用。
10. 通过 Audit Port 原子持久化交互、attempts 和初始 lifecycle events；审计失败立即阻断成功路径并保留现场。
11. 使用纯规则生成 HTML 和既有成功 callback payload。
12. 先条件提交 execution 结果和最新公开投影；若 stale，停止 Progress/callback。
13. 发布 task ID 归属的最终 Progress。
14. 获取 callback Guard；获取前再次检查 latest。stale 时禁止网络调用。
15. 保存不可变 callback payload，执行 HTTP 投递并持久化精确 outcome。
16. 根据 outcome 释放 Guard 或进入 outcome_unknown 冻结。
17. 审计已经成功后，清理可安全删除的 scratch 和临时 RAG 资源；清理失败写 cleanup pending/audit，不反向改写业务成功。

### 10.2 失败路径

- 捕获应用已分类错误和未知异常，日志使用 `logger.exception`，不得吞掉类型和 task 上下文。
- 生成既有报告失败 payload，并按 expected task ID 条件提交 status=2。
- RAG/业务失败也要先尝试持久化失败 trace；审计成功后才能清理未转交的外部资源。
- 审计本身失败时禁止调用正常 close/cleanup，保留外部资源和 Artifact 现场并记录 critical 日志。
- 如果失败提交发现 stale，不更新最新公开投影，也不发送旧失败 callback。
- 只有失败结果成功成为最新事实后，才发布最终 Progress 和尝试失败 callback。
- 进度或终态条件写若抛出异常，视为“数据库提交结果不确定”而不是普通业务失败：包装为
  `ReportTaskPersistenceError`、保留现场并向上抛出；不得再补写第二个失败终态，也不得
  继续 Progress、callback 或清理。只有 Port 明确返回 stale 时才按已确定过期幂等停止。
- 业务终态明确提交后，latest 查询、Callback Guard 获取/完成和网络投递均属于回调故障
  维度；异常只记录 `port_error`，不得重新进入业务失败路径或触发第二次终态条件写。
- 清理/审计失败与业务失败分维度保存，不允许一个异常覆盖前一个更重要的失败事实。

---

## 11. Callback Guard 时序

### 11.1 新任务先提交

```text
new Submit commit(owner=new task)
old task tries acquire callback Guard
  → latest task ID mismatch
  → record stale/skipped
  → no HTTP request
```

### 11.2 旧 callback 先取得发送权

```text
old callback Guard = sending
new Submit sees sending
  → transaction outside wait/poll, no DB lock held
  → callback completes with known outcome: continue submit
  → still sending at deadline: HTTP 409
```

最大等待使用当前 callback timeout；不能无限阻塞 Web 请求。等待过程记录耗时但不输出新字段。

### 11.3 结果未知

```text
old HTTP request may have arrived
response timeout/lost
  → Guard = outcome_unknown
  → no automatic retry
  → subsequent same-key submit = HTTP 409
  → internal operator reviews and explicitly releases
```

人工解除不通过新前端参数或公开路由实现。阶段 1C 先提供 Application/Repository 能力和
测试入口；阶段 5/11 再提供受控运维命令、权限、审计和 Runbook。

---

## 12. 文件、Artifact 与保留语义

### 12.1 任务级命名空间

```text
runtime/tasks/{task_id}/
├── input/
│   ├── source-0001{suffix}
│   └── template{suffix}
├── normalized/
├── rag/
├── output/
│   └── report.html
└── quarantine/
```

对象 key/路径不得使用未清洗 URL 或只使用 reportId。日志记录 task ID、对象类别、大小和
校验值，不记录带凭据的完整 URL、Authorization、文件正文或完整 Prompt。

### 12.2 当前本地 Adapter

- 下载目标必须位于已解析的 task root 内，禁止路径穿越。
- 写入采用临时文件 + 原子替换，避免把半文件当作有效输入。
- 产物持久化成功后返回不透明 ArtifactRef；Application 不拼接真实路径。
- 1C 不新增 URL host 安全校验，继续使用当前受控离线 URL 和既有超时行为。

### 12.3 保留期暂不确定

- 持久输入引用、最终报告、callback payload 和审计元数据不启用自动过期。
- 只有任务终态且无执行/回调/恢复引用后才可产生删除资格。
- scratch 仅在输入/结果已持久化、数据库提交已确认且没有调用方使用时清理。
- 崩溃、提交不明或清理异常进入 quarantine/cleanup pending，不直接递归删除。
- 阶段 3 MinIO Adapter 复用相同 ArtifactRef 和 retention class；具体天数后置确认。

---

## 13. 失败矩阵

| 失败点 | 对外/任务结果 | 持久化与补偿 | 重试 |
| --- | --- | --- | --- |
| 请求参数错误 | HTTP 400 既有错误 | 不创建 execution | 不重试 |
| 活动 task/unknown Guard | HTTP 409 既有错误 | 不创建 execution、不发 Progress | 人工解除 unknown 后可重提 |
| 受理事务失败 | 不返回 202 | 整体回滚，无孤儿 execution | 调用方可重提 |
| commit 后唤醒失败 | HTTP 202 | accepted 事实保留，ERROR 告警，周期扫描恢复 | 自动扫描 |
| 下载失败/URL 失效 | task status=2 | 失败 payload 条件提交，清理已下载文件 | 1C 不自动重跑整个任务 |
| MHTML 规范化失败 | 继续执行 | warning，回退原文件 | 无 |
| 模板提取为空 | task status=2 | 不调用 RAG | 无 |
| RAG 部分资源创建失败 | task status=2 | Adapter 补偿并保存 trace/cleanup pending | 遵循既有 RAG 策略 |
| RAG/业务失败 trace 审计成功 | task status=2 | 保存完整失败证据后清理未转交资源 | 无业务重试 |
| 交互审计失败 | task status=2；禁止成功 callback | 保留 Context/Conversation/全局文档和 Artifact，critical 告警 | 仅对短暂数据库锁有限重试 |
| 结果条件写 stale | 不改变最新公开状态 | 旧 execution 标记 stale，禁止 callback | 不重试公开写 |
| callback 2xx | 业务状态不变 | success + attempt 审计，释放 Guard | 无 |
| callback 非 2xx | 业务状态不变 | rejected/failed + 审计，释放 Guard | 不自动重试 |
| callback 明确未送达 | 业务状态不变 | definitely_not_sent/failed，释放 Guard | 1C 不新增自动重试；阶段 5 有限重试 |
| callback 发送后结果未知 | 业务状态不变 | outcome_unknown，Guard 冻结 | 不自动重试，人工处理 |
| cleanup 失败 | 不反向改写业务终态 | cleanup pending/quarantine + 告警 | 后续 cleanup Worker |
| Worker 进程崩溃 | accepted 可恢复；running 可能滞留 | 阶段 2 lease/reaper 处理 | 1C 明确能力限制 |

---

## 14. 日志和指标

### 14.1 日志字段

所有报告应用/执行日志至少包含：

- `trace_id`
- `task_id/execution_id`（仅内部）
- 规范化 `report_id` 业务键
- `task_type=report_generation`
- `step`
- `execution_state`
- `artifact_type`（涉及文件时）
- `callback_outcome/error_stage`（涉及回调时）
- `duration_ms`

普通日志不得记录完整请求、带 query/credential 的 URL、文件正文、完整 Prompt、完整模型
响应、Authorization 或密钥。错误信息进入数据库前也必须清洗。

### 14.2 指标

- report 受理总数、202/400/409/500 分类。
- 活动冲突、callback sending 等待、unknown 冻结冲突数量和等待时长。
- accepted/running/terminal 数量、最老 accepted 年龄、每分钟排空数。
- 下载/模板/RAG/产物/callback 各步骤耗时和失败分类。
- 交互审计成功/失败、有限重试、trace 完整性和待恢复资源数量。
- stale 条件写拒绝数量；大于 0 必须可追踪到 task ID。
- callback success/rejected/definitely_not_sent/outcome_unknown。
- scratch/quarantine/cleanup pending 数量和最老年龄。
- Dispatcher wake 合并、扫描轮次、领取冲突和停止超时。

---

## 15. 分子波次实施步骤

### 1C-0 契约与现状基线（已完成）

1. 扩展阶段 0 黄金资产，区分当前 JSON 202 与已批准目标空体。
2. 固定 400 文本、reportId 规范化、回调成功/失败载荷、Progress 和 MHTML/Word 行为。
3. 增加当前缺陷证明：重复受理、旧 execution 覆盖、路由线程、按业务键回写，以及任务级
   Client/外部资源未完整清理；另以黄金用例冻结“空 RAG 结果仍按成功回调”的兼容契约。
4. 初次基线建立不改生产路径；负责人随后确认三项严格请求校验，作为 1C-0 契约收口
   立即同步当前 Flask 入站路径，但不改变成功受理、任务、线程或执行链。

**门禁**：所有当前行为与目标差异都有显式断言，未授权差异为零。

**完成证据**：`tests/contracts/stage0_contracts.json` 已建立 report 的 `current/target`
双基线；`tests/test_report_contract.py` 固定当前 202 JSON、重复覆盖、旧 execution 回写、
daemon 线程和资源生命周期缺口，并单独保存已批准的 202 空体、409 与 expected task ID
目标。400 文本、128 位上限内的大整数 `reportId`、Progress、MHTML、Word、成功/失败及空 RAG 成功
回调均有离线断言；顶层对象、params 全对象及 filePathList 非空字符串校验已通过独立
Web Adapter 和当前路由测试。其余当前缺陷测试是迁移坐标，不是目标行为认可。

### 1C-1 Domain/DTO（已完成）

1. 创建 report 包和 README。
2. 迁移 HTML、callback payload、任务名称等纯规则。
3. 建立不可变 submission/input/result DTO 和稳定错误。

**门禁**：纯规则无 Flask/数据库/网络依赖；旧测试与新 Domain 测试双通过。

**完成证据**：已建立 `app/modules/report/domain/` 的不可变 `ReportId`、submission、input
snapshot、result、callback DTO，稳定错误分类，以及 HTML、callback 和执行级名称纯规则；
架构门禁已覆盖 report 包。遗留 `report_service.py` 仅转发上述纯规则以校验兼容性，没有
改变路由、线程、SQLite 或外部资源生命周期。

### 1C-2 Ports 和 Fake（已完成）

1. 定义 Task Command、Progress Publisher、File、Artifact、Report RAG、Interaction Audit、Callback 和 Dispatcher Port。
2. 建立严格 Fake，记录调用顺序、输入快照和失败注入。
3. 完成 Submit/Run Application 的无框架测试。

**门禁**：Application 在无 Flask context、无 SQLite、无真实文件/AnythingLLM/callback 下通过。

**完成证据**：已新增通用 `TaskCommandPort`、`ProgressPublisherPort` 和携带泛型输入的
`TaskExecutionSnapshot`；report 已建立 File、Artifact、Report RAG、Interaction Audit、
Callback 与 Dispatcher Port，以及 `SubmitReportTask`/`RunReportTask(task_id)`。严格 Fake
共享全局调用记录，覆盖成功、受理冲突、通知失败、重复派发、各阶段异常、normalization
回退、stale 条件写、callback sending 在数据库锁外等待/释放重试/超时/unknown、完整 trace
审计门禁、任务进度/终态写入结果不确定、终态后的 callback latest/Guard/投递故障、
cleanup，以及跨 TaskId/trace/Artifact/Receipt 和 Artifact 类别/顺序非法返回值。
Application 测试不创建 Flask、SQLite、真实文件或网络对象。

本子波次只冻结编排协议：Task Command、File/RAG/Audit/Callback/Dispatcher 均没有生产
Adapter，Flask 路由仍调用遗留 `report_service.py`。因此 1C-2 不代表原子 409、持久化
execution、按 TaskId 生产执行或可靠队列已经落地。

### 1C-3 SQLite execution 与原子 409（已完成）

1. 增量创建 execution/Guard 表和索引。
2. 实现一次事务的 create-if-allowed 和最新投影 owner。
3. 实现按 task ID 重载和 expected task ID 条件写。
4. 运行 50 同键 Barrier 并发和事务回滚测试。

**门禁**：恰好一个 accepted；无锁错误泄露、无孤儿行、无旧写覆盖。

**完成证据**：已增量建立 `llm_task_executions`、`callback_delivery_guards` 和扫描索引；
`LLMTaskService` 使用独立连接与短 `BEGIN IMMEDIATE` 事务实现 Guard/活动状态检查、execution
插入和 `llm_tasks` 最新投影的原子提交，并实现领取、latest、accepted 扫描以及 expected
TaskId 进度/终态双表条件写。通用 `LegacyTaskCommandAdapter` 通过注入 Codec 隔离业务
DTO，report Codec 严格保存 128 位上限内的 reportId 和完整不可变输入。50 同键 Barrier 得到
1 accepted/49 active conflict，50 并发领取得到 1 claimed/49 already running；触发器故障
验证受理与进度双表回滚，无锁错误、孤儿行或旧写覆盖。上述结果是内部 Adapter 门禁，
在 1C-3 完成时，公开 HTTP 202/409 尚待 1C-6 路由切换。全面审查又补充了 WAL/短事务设置、旧运行 execution
原子收敛为 stale、内部/公开结果分离，以及带 lease token/fencing token 的 SQLite Guard
核心；这些能力当时尚未装配到报告路由，现已由 1C-6 完成装配。执行证据见
`../更新记录/260717-阶段1C报告生成整合执行记录.md`。

### 1C-4 RunReportTask 和 I/O Adapter（已完成）

1. 包装 downloader/normalizer/Word extractor。
2. 建立任务级本地 Artifact Adapter。
3. 建立多文档 AnythingLLM ReportRag Adapter，Client/Transport 为任务级。
4. 接入原子 Interaction Audit Adapter，先保存完整 trace，再允许业务成功或清理临时 RAG 资源。
5. Run 只接收 task ID 并覆盖成功/失败/stale/audit/cleanup。

**门禁**：旧报告服务黄金样例等价，源文件在执行时下载，输入与目录隔离；审计失败时
禁止成功终态/成功 callback 且保留外部现场。

2026-07-16 已完成以下落地与离线验收：

1. `LocalReportArtifactAdapter` 使用 TaskId 的 SHA-256 命名空间隔离任务目录；scratch 与
   final artifact 均以不透明相对引用、长度和 SHA-256 摘要表示，最终 HTML 通过同目录
   临时文件、`fsync` 和原子替换发布。50 个并发任务未出现路径串扰，伪造的跨类别引用
   会被拒绝；成功后只清理 scratch，保留最终报告。
2. `LegacyReportFileAdapter` 把既有下载、文件规范化和 Word 文本提取收口为执行时调用，
   所有遗留工具返回路径都会重新发布到当前任务命名空间，避免把共享临时路径泄漏给
   Application。当前按已确认决策不增加 URL 安全策略。
3. `AnythingLLMReportRagAdapter` 为每次调用创建独占 Transport/Client，按输入顺序上传并
   绑定多份文档，只发起一次报告查询；每份文档携带唯一来源标记。成功、部分上传失败、
   查询失败、空结果和清理失败均产生带 `trace_id`、`call_id`、来源统计及资源生命周期的
   完整 trace；cleanup token 为版本化不透明值，清理时重新获取独立调用租约。
4. `SQLiteReportInteractionAuditAdapter` 通过一个事务原子保存主交互、全部 attempts 与初始
   lifecycle，并可幂等追加 cleanup lifecycle。共享审计 Schema 只增不删升级为 v3，新增
   `llm_interactions.trace_id` 与 `llm_interaction_attempts.call_id`；旧表通过幂等补列兼容。
5. 真实 SQLite Task Adapter、本地 File/Artifact Adapter、RAG Adapter 和 Audit Adapter 已在
   无 Flask、无真实后台服务的组合测试中驱动 `RunReportTask(task_id)` 完成成功链路；强制
   审计事务失败时，主审计与明细全部回滚，任务不得成功、不得发送成功 callback，并保留
   本地和 AnythingLLM 现场供恢复。

详细文件、测试结果和未切生产边界见
`../更新记录/260717-阶段1C报告生成整合执行记录.md`。

### 1C-5 Callback Guard 和资源收口（已完成）

1. 将已完成单元验证的 SQLite Guard Adapter 装配到报告执行链；HTTP Adapter 区分精确
   outcome，网络调用始终位于事务外。
2. acquire 在事务中权威复核 latest/终态，真正 HTTP 前再次复核 latest、lease token、
   fencing token 和期限，complete 使用相同身份 CAS；提交遇到 sending/unknown 立即映射
   409，不占用 Web 线程等待。人工解除必须确认旧 Worker 已停止或隔离并追加审计。
3. 完善 callback 公开投影、任务级 Artifact 引用和 cleanup/quarantine；旧 execution 的
   未提交最终 Artifact 不得 retain，审计成功后的 cleanup 追加失败必须进入可恢复事实。
4. 覆盖提交与 callback 的确定性竞态、明确结果不自动重试、迟到完成和崩溃过期测试。

**门禁**：所有时序下旧 callback 不会在新任务先提交后外发；unknown 不自动重试且阻止
新提交；Guard 写闭环、Artifact 所有权和清理恢复均先于公开路由切换完成。

> **2026-07-16 执行结果**：1C-5 已完成。SQLite Guard 已进入新报告离线执行链；人工解除
> 以 `(business_type, business_key, lease_version)` 唯一追加审计，普通成功释放不会被误判
> 为人工解除。任务资源记录以 version CAS 持久化 RAG/Audit/Artifact 引用、待审计事件和
> 外部调用占用。全面复查后资源 payload 升级为 v3：每次清理持有 token、开始时间和逐步
> 心跳，每个 lifecycle event 在下一项 DELETE 前 CAS 落库；过期/中断后先审计已保存步骤，
> 再利用远端 404 的幂等删除语义以连续序号恢复，不再把整批永久误隔离。
> conversation/context/global document 删除均参与完成判定；审计追加失败只重放已持久事件，
> 不重复已经有明确检查点的删除。详细证据见
> `../更新记录/260717-阶段1C报告生成整合执行记录.md`。

#### 1C-5 全面审查后的强制补强（已完成）

1. 最终报告即使属于 retain，也会复核路径、大小和 SHA-256；文件丢失或内容变化保持
   pending，资源记录不得进入 CLEANED。
2. AnythingLLM Workspace 创建响应不确定时按任务唯一名称精确查回；无法唯一查回或
   文档上传后拿不到全局 location 时，失败 trace 先审计，资源记录再进入明确 quarantine。
3. 新增 `ReportResourceRecoveryService.sweep(limit)`：范围固定为 1～1000，一项失败不
   中断同批其他任务，并返回 cleaned/pending/quarantined/not-ready/missing/failed 汇总。
4. 128 位 reportId 触发供应商名称上限时使用稳定摘要，完整业务键不截断；回调历史使用
   排他创建，50 并发同名写入不覆盖；历史落盘不再位于发送前权威复核和 HTTP 之间。
5. 资源恢复的每步检查点只适用于幂等 DELETE；AnythingLLM 创建/上传等写操作响应不确定
   时仍必须隔离，不能把“清理可重放”错误扩张为“所有外部副作用都可重放”。

### 1C-6 Dispatcher、Container 和路由切换（已完成）

1. 实现数据库扫描 + Event 唤醒的单实例 Dispatcher。
2. 在启动后和固定周期调用 `ReportResourceRecoveryService.sweep(limit)`；每次批量、轮询
   间隔、超时和指标均由内部配置控制，不新增公开参数。单任务异常不得杀死扫描循环。
3. Container 显式装配全部依赖；测试注入 Fake，并先以不接收公开流量的方式完成组合验收。
4. 切换前停止 report 新受理，盘点并排空或明确处置遗留 daemon 线程与新 execution，证明
   不存在会在切换后继续发送旧回调的旧执行。
5. 蓝图改为 Parser → Submit → Presenter，删除该路由线程创建；确认回调、Guard、Artifact
   和 Dispatcher 均已使用同一新组合根后，再切换 202 空体和活动/unknown 409。
6. Dispatcher 只扫描并领取 accepted；running 仅输出数量、最老年龄和 task ID 供人工处置，
   在阶段 2 Attempt/Step/Checkpoint/lease 就绪前禁止直接重置为 accepted。
7. 路由切换不得与旧 Service 双写、双执行或双 callback；失败时按第 17 节先停受理再回滚。

**门禁**：50 不同键 accepted 不创建 50 线程/Queue 项；全部按执行许可持续处理；路由暴露
前 1C-4 I/O/Audit Adapter 与 1C-5 Guard/资源闭环全部通过，旧在途任务已排空或隔离。

**2026-07-17 完成证据**：

1. `LocalReportTaskDispatcher` 以 SQLite `accepted` execution 作为唯一积压事实，内存只有
   一个 `threading.Event`；50 个不同业务键只创建一条 `docsense-report-worker` 执行线程，
   不保存 Python Queue/list/Future 积压，按事务内 `dispatch_sequence` 有界扫描并连续排空。
2. Dispatcher 构造不启动线程，`start/stop/close` 显式且幂等；等待共享上传许可时可响应
   停机，`close` 超时保持 `stopping`，只有线程真实退出后才进入 `closed`。生产组合根装配
   跨进程文件锁并禁止 preload/fork，Werkzeug debug reloader 保持关闭。
3. 资源恢复和队列诊断分别由固定维护线程运行；启动时及每 30 秒执行 `sweep(limit=50)`。
   坏资源记录通过独立 `next_recovery_at` 冷却，不饿死后续页；`running` 只输出数量、最老
   年龄及最多 20 个 TaskId，队列告警按 30 秒节流且不自动重置任务。
4. 资源清理使用独立的 60 秒 HTTP 超时和 90 秒清理租约，配置启动时强校验
   `lease > timeout`；报告生成/模型查询继续沿用 `ANYTHINGLLM_TIMEOUT` 原语义，不被强制
   改成 60 秒。两个数值均为内部基础设施配置，没有新增公开参数。
5. 组合根装配 Task Codec/Command、latest Guard Progress、File/Artifact/RAG/Audit/
   Callback/Resource、Run/Dispatcher/Submit 的同一对象图；应用工厂只启动自行拥有的生产
   容器，显式注入的测试容器不会产生后台线程，退出时通过有限关闭收口。
6. `/llm/generate-report` 已切换为 Parser → `SubmitReportTask` → Presenter：成功严格返回
   HTTP 202 零字节体，活动任务、callback sending 和 outcome unknown 返回既有 HTTP 409
   单字段错误体；路由不再导入或调用 `run_report_task`，不双写、双执行或双 callback。
7. 持久化 latest owner 的复核与内存 Progress latest 更新已合并到 Hub 同业务键发布临界区；
   Guard 不占用全局状态锁，不同业务键可以并发，同时封闭旧 accepted 迟到覆盖新任务的竞态。
8. 本轮未启动 `run.py`，开发环境不存在需要排空的在线旧报告线程；真正部署时仍必须按
   第 17 节停受理、盘点旧进程并单版本切流，该生产动作不得用本次离线结果代替。

详细修改、验证命令和剩余边界见
`../更新记录/260717-阶段1C报告生成整合执行记录.md`。

### 1C-7 全量验收与清理证据（已完成）

1. 运行定向、阶段 1A/1B 合并、路由、任务、Progress、AnythingLLM 和安全扩大回归。
2. 运行架构导入门禁、AST、`git diff --check` 和静态旧引用搜索。
3. 记录未连接真实服务、未启动 run.py、未进入生产以及阶段 2～6 后置责任。
4. 新增 `docs/更新记录/` 执行记录并更新本计划进度。

**完成证据**：358 项报告/1A/1B/路由/Progress/AnythingLLM/架构合并定向测试通过；74 个
安全模块共 872 项全仓回归通过；240 个 Python 文件编译、契约 JSON、AST 和
`git diff --check` 通过。运行与配置路径已无遗留报告 Worker 调用，但兼容测试仍直接验证
旧入口，因此按门禁保留 `report_service.py`，并增加生产代码不得重新导入它的永久 AST
规则。未启动 `run.py`、未连接真实服务、未部署生产，阶段 2～6 输入清单已落档。

详细证据见 `../更新记录/260717-阶段1C报告生成整合执行记录.md`。

---

## 16. 验收矩阵

### 16.1 Domain/Application

- reportId 的 128 位边界、负数/前导零规范化、129 位拒绝和公开值保持。
- HTML 原样/纯文本转义、成功/失败 callback payload 精确等价。
- `None`、空字符串和纯空白 RAG 结果均生成空 HTML 成功回调，并产生内部可观测记录。
- Submit 成功、活动/sending/unknown 立即冲突、持久化失败、唤醒失败；断言不调用同步等待。
- Run 成功、各步骤失败、stale、重复派发、终态重复、任务事实写入结果不确定、终态后
  callback 控制面故障不二次写终态、cleanup 失败。
- 完整 RAG trace 审计成功才允许成功终态；审计失败、原子回滚和幂等重复均有负断言。
- 所有 Fake 调用顺序和“失败后不得继续”的负断言。

### 16.2 Adapter/事务

- Schema 重复初始化。
- create execution + projection 原子提交/回滚。
- 50 同键并发唯一受理；50 不同键全部持久化。
- expected task ID 条件进度/终态/callback。
- Dispatcher 重复唤醒合并、周期扫描、异常隔离、停止和 accepted 重启恢复。
- 资源恢复启动/周期 `sweep(limit)` 有界、单任务异常隔离、逐删除事件检查点、心跳过期和
  连续序号幂等恢复；running 不得被 Dispatcher 或资源扫描器盲目重置。
- Callback HTTP 分类、历史载荷写失败不阻断网络语义、敏感日志脱敏。

### 16.3 Web 契约

- HTTP 202 `Content-Length: 0` 且零字节正文。
- 活动/sending/unknown 均立即返回 409 和既有 JSON，不同步等待回调。
- 既有 400 场景、文本和字段不变。
- 不返回 task/execution ID，不新增响应头业务语义。
- Progress 和 callback 字段、类型、顺序、状态不变。

### 16.4 架构和并发

- report/tasks domain/application 禁止导入 Flask、FastAPI、Celery、SQLAlchemy、sqlite3、requests。
- Blueprint 不出现 `threading.Thread`、SQLite SQL、AnythingLLM Client 或业务步骤。
- 50 个积压任务只增加持久化行，进程内 wake state 保持常量空间。
- 旧/new task 竞争、callback/submit 竞争通过 Barrier/Event 精确调度，不依赖随机 sleep。

### 16.5 实际关闭验收

2026-07-17 使用 Windows 项目解释器 `venv\Scripts\python.exe` 完成：

1. 报告全链、1A/1B、路由、Task、Progress、AnythingLLM 契约、容器及架构合并定向：
   **358 项通过**；
2. 排除会启动主进程或依赖真实资源/缺失 fixture 的四个既有环境敏感模块后，74 个安全
   模块共 **872 项通过**；
3. `app/`、`tests/` 共 240 个 Python 文件编译通过；
4. 契约 JSON、静态旧引用、架构 AST 与 `git diff --check` 通过。

完整命令、排除原因和未获得能力见阶段 1C-7 执行记录。全程未运行 `run.py` 或连接真实
后台服务。

---

## 17. 灰度与回滚

阶段 1C 仍是开发分支改造，不进入生产。路由切换时仍需保持可回滚：

1. 新旧实现通过组合根绑定切换，不在同一请求中双写、双执行或双 callback。
2. 增量 SQLite 表只新增不删除；回滚旧绑定时可以保留，无需破坏性降级。
3. 旧 `report_service.py` 在 1C 观察期保留，但新路由不能同时调用它。
4. 若新 Dispatcher/执行器需回滚，先停止 report 新受理，列出 accepted/running execution，
   排空或明确处置后再切旧路径；禁止让旧线程重复执行已被新路径领取的任务。
5. 202 空体和已确认 409 属于目标接口契约，不因内部实现回滚恢复成旧成功 JSON。
6. 任何无法安全排空的任务保留 execution/input/Artifact/Guard 审计，不删除现场。

---

## 18. 后置责任

| 阶段 | 仍需完成 |
| --- | --- |
| 阶段 2 | 通用 Task/Attempt/Step/Event、running 崩溃 lease/reaper、统一本地执行器和 50 任务隔离 |
| 阶段 3 | 同步 SQLAlchemy/MySQL Repository、Alembic、MinIO、文本 reportId 索引和共享事务 |
| 阶段 4 | task/input/Outbox 同事务、可靠 Relay、publisher confirm |
| 阶段 5 | RabbitMQ/Celery、late ACK、DLQ、callback Worker、跨进程 Guard 和人工运维命令 |
| 阶段 6 | report 从本地 Dispatcher 切可靠队列；check-task 保留同步恢复，并启用共用 Guard 的可靠队列后台兜底 |
| 阶段 7 | MySQL Progress 事实 + Redis 唤醒，多实例连接通知 |
| 阶段 8 | FastAPI Adapter 和同步 Repository 受控线程池调用 |
| 阶段 10 | 真实 50+ 在途/长连接/短请求、积压恢复和故障注入验收 |

---

## 19. 完成清单

- [x] 当前/目标报告契约和黄金样例完整（1C-0）。
- [x] Report 模块、Domain/DTO 和依赖边界建立，纯规则可脱离 Flask 测试（1C-1）。
- [x] Report Application 可脱离 Flask、SQLite 和真实外部服务测试（1C-2）。
- [x] SQLite 原子活动冲突及 callback sending/outcome unknown 分类已实现；公开 HTTP 409 已由 1C-6 路由映射。
- [x] 50 同 reportId 的内部受理只有一个 accepted、其余 active conflict，无锁错误或孤儿 execution；HTTP 202/409 路由门禁已通过。
- [x] 新 `RunReportTask` Application 只接收 task ID，并从不可变快照恢复；I/O/Audit/Callback/资源恢复、Dispatcher 和组合根已完成装配。
- [x] 新 Application 的进度、结果、callback 和投影命令携带 expected task ID；当前开发分支报告路由已切新链。
- [x] 路由不创建线程；积压持久化，内存唤醒常量空间且持续排空。
- [x] 文件 Adapter 在任务执行时下载；50 个并发 TaskId 的任务级目录/Artifact 无串扰；本地一条执行 Worker 与两条隔离维护线程已装配。
- [x] 新 Application 的 ReportRag/Callback/File/Artifact/Progress 均通过 Port 访问；生产形态 Adapter、恢复 Application、Dispatcher 和组合根已完成当前单实例装配。
- [x] SQLite Guard 已证明新任务先提交后旧 callback acquire 必定 stale、50 并发仅一方取得发送权、迟到 fencing token 无法完成新租约；追加式人工解除审计也已通过 50 线程唯一写入及跨下一租约保留测试。
- [x] outcome unknown 不自动重试并冻结同业务键；人工解除包含旧 Worker 停止/隔离确认；
  发送前二次权威复核、Artifact 完整性、外部写结果未知隔离、逐事件 cleanup 检查点、心跳
  租约和有界 `sweep(limit)` 均已完成。
- [x] 202 空体、400、409、Progress 和 callback 契约测试通过，未增删参数。
- [x] 阶段 1C 已由 1C-7 最终关闭；358 项合并定向、74 个模块/872 项安全回归、AST、240 文件编译、契约 JSON 与差异检查通过。
- [x] 2026-07-17 阶段关闭后全面审查已补强同步 check-task/Guard、维护扫描、下载、审计、
  资源恢复和 Dispatcher；最终 75 个安全模块/889 项回归通过，详见对应更新记录。
- [x] 阶段 1C 全程未启动 `run.py`、未连接真实后台服务、未部署或进入生产。
- [x] 1C-0～1C-7 实际执行记录已写入 `docs/更新记录/`，偏差、遗留引用与阶段 2～6 后置责任已回链。

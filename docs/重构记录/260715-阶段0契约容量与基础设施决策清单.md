# 阶段 0：接口契约、容量基线与基础设施决策清单

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 编写日期 | 2026-07-15 |
| 所属计划 | `260715-低耦合高并发任务隔离与可靠队列改造总计划.md` 阶段 0 |
| 文档层级 | L3 执行设计与决策清单 |
| 文档状态 | **阶段 0 已有条件完成并关闭**；真实性能基线经批准延期，转为首次完整集成环境补测和阶段 10 硬门禁 |
| 接口影响 | 已确认定向契约调整：report 活动任务及 callback outcome unknown 冻结返回 409；三类受理成功 202 空响应体；check-task 成功 200 空响应体；check-task/Progress 严格 `params` 元素校验；Progress 显式 action 返回错误消息、保持连接且无 ack；reassign 的远端 `false`/目标创建失败/本地条件更新失败按业务失败返回既有 500 结构。参数集合、错误字段结构、回调和 chat 契约不变，代码按对应阶段实现 |
| check-task 队列补充决策 | 2026-07-16 已确认直接采用 MySQL Outbox + RabbitMQ + callback Worker，不建设同步/并行过渡；目标 200 表示恢复命令可靠登记或复用，不等待外部回调完成，生产切换后移至阶段 6 |
| 运行限制 | 不启动 `run.py`；不连接真实 AnythingLLM、模型、回调或生产基础设施，除非另行明确授权 |

本文把阶段 0 从原则性门禁细化为可交付清单。阶段 0 的目标不是实现新架构，而是建立后续改造所需的契约、工具和决策基线，并把无法从代码和现有文档确定的业务、容量、部署和迁移选择交由负责人确认。2026-07-15 已明确批准暂不采集改造前真实性能数据；该批准只允许延期，不取消最终容量验收。

---

## 1. 当前已核实基线

以下内容已经能够从当前代码、README 和更新记录中确认：

- Web 入口当前为 Flask，正式 LLM 路由集中在 `app/blueprints/llm.py`。
- analysis、report、weaponry 当前仍由进程内 daemon thread 执行；Web 进程退出会中断执行。
- file/report 共用上传类并发限制，AnythingLLM Document Processor 当前安全基线为并发 1。
- 任务、知识库映射、文件对话分别使用三个 SQLite 文件。
- 文件对话当前只允许 `single_instance`，没有事务 Outbox、可靠队列、跨实例通知或 fencing。
- `/llm/check-task` 不只是查询：它会对满足条件的终态任务补发回调；当前代码还返回任务和补发结果，但目标成功响应已确认改为空响应体。
- 未配置 `CALLBACK_URL` 时，已完成且仍为 `pending` 的任务会被幂等标记为 `skipped`；以后再配置 URL 也不会由 check-task 自动补发。
- `/llm/progress` 当前代码支持不带 action 的订阅，以及项目自行扩展的 subscribe/query/unsubscribe 与 ack；仓库未发现甲方或生产前端对显式 action 的需求证据，目标契约只保留不带 action 的订阅消息。
- `/debug/*` 始终注册，`APP_DEBUG=false` 不会自动关闭，生产环境必须在网络或反向代理层限制。
- 目标生产环境已确认为单台物理服务器；MySQL、RabbitMQ、Redis、MinIO 与应用组件最终使用 Docker Compose 部署。完整计划实施前，开发阶段继续使用当前环境，不提前切换基础设施。

---

## 2. 必须确认的决策登记表

所有 `待确认` 项必须在其“最迟确认阶段”之前得到明确结论。结论应追加到本表，不得只保存在聊天记录中。

| ID | 决策问题 | 当前可确认事实 | 状态 | 最迟确认阶段 |
| --- | --- | --- | --- | --- |
| D0-01 | 目标生产环境仍是否只有一台物理服务器 | 已确认为单台物理服务器 | **已确认** | 阶段 0 |
| D0-02 | “不少于 50 并发”的正式业务含义和 SLO | 已确认采用分层口径：50+ 在途任务、50 条长连接、短请求稳定并发；重任务按资源限流排队。改造前真实性能基线与 P95/P99 等数值阈值经批准延期到首次完整集成环境，并作为阶段 10 生产验收硬门禁 | **已决策（延期）** | 首次完整集成环境/阶段 10 |
| D0-03 | MySQL、RabbitMQ、Redis、MinIO 是否允许部署及由谁运维 | 四项基础设施均允许部署；目标生产使用 Docker Compose，开发阶段暂不切换。备份、监控和责任人仍需形成 Runbook | **部分确认** | 阶段 0/11 |
| D0-04 | 三套 SQLite 历史数据是否全量保留、允许停机多久 | 历史数据仅用于测试，无需迁移；阶段 3 可初始化空 MySQL Schema 和测试种子数据 | **已确认** | 阶段 0/3 |
| D0-05 | 报告、上传文件和中间产物最终使用 MinIO 还是受保障共享文件系统 | 已确认统一使用 MinIO；开发阶段仍可由兼容本地 Adapter 支撑当前环境 | **已确认** | 阶段 1C/3 |
| D0-06 | 外部回调接收方能否按现有业务键和结果状态去重，是否支持结果查询 | 已明确无法确认接收方幂等与查询能力；按“不具备保证”设计保守投递策略，不新增回调参数 | **已决策** | 阶段 1C/4 |
| D0-07 | 非 chat 接口过载时允许采用什么现有兼容响应 | 已确认 analysis/report/weaponry 不设置业务积压数量上限，不因正常积压返回 503/429；任务事实持久化后持续排队，重任务执行并发仍按资源限流。数据库/Broker/磁盘无法完成可靠写入属于基础设施失败，不能返回 202 | **已确认** | 阶段 2/6 |
| D0-08 | 同一 `reportId` 活动任务重复提交是否继续接受 | 已确认改为 HTTP 409 拒绝，复用现有错误结构；已授权修改接口文档，不增删参数。代码在波次 1C 实现 | **已确认** | 阶段 1C |
| D0-09 | `/llm/reassign` 可接受的最长同步时间和上游超时 | 接口必须保持同步，具体时限未知 | **待确认** | 阶段 1E |
| D0-10 | MySQL Repository 采用统一同步模型还是 Web/Worker 双适配 | 已确认第一版统一使用同步 SQLAlchemy Repository：Flask/Celery 直接调用，FastAPI 使用受控线程池执行短事务；只有阶段 8 压测证明必要时才增加异步查询 Adapter | **已确认** | 阶段 3/8 |
| D0-11 | 文件下载与回调目标允许的域名、网段和重定向策略 | 已确认当前生产为受控本地离线环境，暂不建设 URL 安全策略；1C 不增加白名单、DNS 重绑定或重定向校验，也不作为当前上线门禁。若部署拓扑或 URL 信任边界改变，必须重新评审 | **已确认（风险接受）** | 拓扑变化时重审 |
| D0-12 | 生产发布采用 Docker Compose、宿主机服务还是其他编排方式 | 目标生产采用 Docker Compose；完整计划实现前，开发阶段保持当前环境 | **已确认** | 阶段 0/3 |
| D0-13 | analysis/report/weaponry 成功受理是否需要返回任务 JSON | 甲方不会处理三个接口的成功响应内容；已确认保留 HTTP 202、删除成功 JSON 响应体，错误响应不变 | **已确认** | 阶段 1C/1D/1F |
| D0-14 | `/llm/check-task` 是否需要公开任务/回调恢复结果 | 甲方不会处理成功响应内容；已确认保留 HTTP 200、成功响应体为空，内部回调恢复和审计语义保留，400/404 错误响应不变 | **已确认** | 阶段 6 |
| D0-15 | `/llm/progress` 显式 action 与 ack 是否属于外部需求 | Git 历史显示其来自 2026-03-16 后端多任务兼容扩展；仓库调用方仅为内部脚本/测试，没有甲方或生产前端需求证据。已确认目标契约下线显式 action/ack；收到 action 时拒绝整条消息、返回 error 消息、保持连接且无 ack | **已确认** | 阶段 1B |
| D0-16 | check-task/Progress 的混合 `params` 数组是否继续过滤非对象元素 | 已确认采用严格校验：任一非对象元素使整次 HTTP 请求或 WebSocket 消息报参数错误，不过滤后部分处理；HTTP 400，WebSocket error 消息后保持连接 | **已确认** | 阶段 1B |
| D0-17 | check-task 回调恢复采用请求内同步、同步并行还是可靠队列 | **已确认直接异步**：MySQL 事务创建/复用 recovery request 并写 Outbox，RabbitMQ callback Worker 执行；不实现同步 Adapter、有界同步并行、本地线程/内存队列或总超时过渡方案。目标 200 表示可靠登记，不表示 callback 已完成 | **已确认** | 阶段 4～6 |
| D0-18 | `/llm/reassign` 如何处理 AnythingLLM 返回 `false`、目标 workspace 创建失败或本地条件更新失败 | 已确认全部视为业务失败，执行补偿并返回既有 HTTP 500 `businessType + msg + data` 结构；不新增参数或错误字段 | **已确认** | 阶段 1E |
| D0-19 | callback 已取得发送权或进入 `delivery_outcome_unknown` 时是否允许同业务键新任务 | 已确认使用同键 Guard：新任务先提交则旧 callback stale；旧 callback 先发送则新提交最多等待当前 callback timeout；outcome unknown 或等待到期仍被占用时返回既有 409，直至内部人工解除 | **已确认** | 阶段 1C/4～6 |
| D0-20 | report 的源文件字节在 202 前还是 Worker 执行时固定 | 已确认 202 只表示任务可靠受理；源文件由 Worker 执行时下载并写入当前本地/未来 MinIO。排队期间 URL 内容变化时，以执行时版本为本次输入 | **已确认** | 阶段 1C |
| D0-21 | MinIO 输入、中间产物、最终产物、callback 载荷和审计元数据保留多久 | 具体 7/30/180 天等数值批准延期；未确认前不启用自动 TTL。阶段 1C～3 先建立对象类别、引用、可空删除资格和审计，排队/运行/恢复/待 callback 数据不得按创建时间删除 | **已决策（延期）** | 首条非测试自动删除规则启用前/阶段 10 |

除已逐项批准的定向调整外，禁止新增、删除或改变请求字段、响应字段、回调字段、SSE/WebSocket 消息和错误结构。D0-16 只收紧既有 `params` 数组元素校验；D0-18、D0-19 只改变已确认失败判定/409 适用条件，均不增删参数或错误字段。

### 2.1 2026-07-15 决策纪要

- 单服务器是已知约束，因此首期只承诺进程隔离、可靠重启、数据可恢复和多应用实例验证，不宣称基础设施节点级高可用。
- “50 并发”不等于 50 个 AnythingLLM/模型重任务同时执行。重任务必须进入独立队列并受资源配额、速率和下游容量限制。
- SQLite 仅保存测试数据，不做历史行迁移；仍需用测试证明 MySQL 新 Schema 能表达全部现有业务状态和查询。
- MinIO 是目标唯一共享产物存储；当前本地目录通过 `ArtifactStorePort` 兼容，直至相应阶段切换。
- report 活动任务的判定沿用公开状态：`status="0"` 表示生成中；同一 `reportId` 再提交时返回 HTTP 409 和既有单字段错误体，不创建线程或新任务。
- analysis/report/weaponry 的业务键均由调用方提供，成功受理无需返回内部任务记录；目标分别在 1F/1C/1D 返回 HTTP 202 空响应体。
- `/llm/check-task` 原定继续同步执行必要恢复；该实施方式已被 2026-07-16 的 D0-17
  取代。最终请求只可靠登记恢复命令并返回 HTTP 200 空响应体，callback attempt 和
  outcome 由 Worker 异步形成并作为内部事实、日志和运维数据保存。
- check-task/Progress 的 `params` 任一元素不是对象时，分别拒绝整次 HTTP 请求或整条 WebSocket 消息，不过滤后部分处理。
- Progress 正式协议只保留不带 `action` 的订阅 JSON 和既有进度推送；显式 subscribe/query/unsubscribe 与 ack 在波次 1B 删除，收到 action 时返回 error 消息并保持连接。
- Docker Compose 只描述最终生产编排方向，不授权当前开发阶段启动或接入这些基础设施。
- 改造前真实性能基线经批准延期。阶段 1 可以按各自门禁推进，但任何阶段都不得把离线 Fake 测试解释为 50+ 生产容量；首次具备完整集成环境时必须补测，阶段 10 未完成数值 SLO、稳态和故障压测则禁止生产验收。

### 2.2 2026-07-16 阶段 1C～11 细化决策纪要

- 阶段 3 起允许使用仅供集成测试的 Docker Compose 启动 MySQL、RabbitMQ、Redis 和
  MinIO；应用继续从 `/venv` 运行，不进入容器，也不启动 `run.py`。
- analysis/report/weaponry 不设业务积压数量阈值。待处理事实必须持久化，内存只允许
  有界唤醒；AnythingLLM/模型执行并发继续限流，资源释放后持续排空。
- MySQL 第一版采用同步 SQLAlchemy Repository/Unit of Work。Repository Port 不导入
  SQLAlchemy；Session 不跨线程/任务共享，事务中禁止下载、RAG 或 callback。
- report 的请求快照在 202 前保存，文件字节在 Worker 执行时下载；这项选择不承诺排队
  期间源 URL 内容不变。
- callback Guard 采用“先取得所有权者优先”的同键串行化：新 task 已提交则旧 callback
  stale，旧 callback 已发送则新提交有限等待；发送后结果未知冻结业务键并返回 409。
- 当前不建设 URL Policy；该风险接受只适用于已确认的受控本地离线拓扑，拓扑变化必须重审。
- MinIO 数值保留期后置。TTL 不得从对象创建时间直接起算，否则无上限积压可能在执行前
  误删输入；未确认前所有持久对象不自动过期。

### 2.3 D0-06：回调幂等约束与保守投递策略

可靠回调采用“至少一次尝试”时，会出现以下无法仅靠发送方判断的情况：

1. DocSense 向业务系统发送报告成功回调。
2. 业务系统已经按 `reportId` 保存结果并触发后续处理。
3. 业务系统返回的 HTTP 2xx 在网络中丢失，或发送方等待响应超时。
4. DocSense 只能看到“超时”，无法知道对方是否已经处理；若自动重试，对方可能收到完全相同的第二次回调。

现有回调不能新增 delivery ID，因此接收方需要基于既有字段实现幂等：file 可使用 `businessType + fileName + status`，report 可使用 `businessType + reportId + status`，weaponry 可使用 `businessType + architectureId + status`。如果接收方是按业务键执行 upsert，重复载荷只覆盖同一结果，通常安全；如果每次回调都会新增记录、重复通知或再次触发下游任务，则不安全。

接收方无法确认上述能力，因此后续实现不得假定重复投递安全，也不得依赖接收方结果查询。采用以下保守策略：

- 每次投递都按内部 task ID 持久化 attempt、开始/结束时间、错误分类、HTTP 状态和脱敏响应摘要；不向公开回调增加 delivery ID。
- 只有能确定 HTTP 请求尚未送达的错误才允许有限自动重试，例如 DNS 解析失败、建立连接前超时或连接被拒绝；重试必须有指数退避、次数上限和告警。
- 请求可能已经发出后发生的读取超时、连接中断或响应丢失，内部标记为 `delivery_outcome_unknown`，不由 callback Worker 自动重试。
- 接收到非 2xx 响应时记录为明确失败并进入人工/死信处理，不在未知幂等前提下自动重复发送。
- `/llm/check-task` 当前是已有的显式恢复入口；目标实现允许调用方触发一次可靠恢复命令，
  记录 `trigger=check_task`。同一 TaskId 的活动命令必须复用，不能被重复请求或后台循环
  放大；真正外发由 callback Worker 完成。
- 内部 callback 状态继续使用 `pending/success/failed/skipped`；发送结果未知使用内部 `delivery_outcome_unknown` outcome，并兼容落为 failed。check-task 成功响应改为空后，这些内部状态不再通过该响应公开，业务任务自身的成功/失败状态不受影响。

---

## 3. 阶段 0 的执行分解

### 0A：接口契约资产化

**目标**

把接口文档和当前代码的可观察行为转成自动化契约资产。

**工作内容**

1. 建立正式接口清单、路由方法和协议类型。
2. 为每个接口记录合法请求、边界请求、非法请求、状态码、JSON 类型和错误文本。
3. 对 `/llm/progress` 冻结不带 action 的订阅、批量顺序、快照和关闭行为；把显式 action/ack 记录为“当前实现存在、波次 1B 改为 error 后保持连接且无 ack”的差异，而不是目标黄金契约。
4. 对 `/llm/chat` 记录响应头、SSE 行格式、事件顺序、终态和断连行为。
5. 对 `/llm/check-task` 记录成功 200 空响应体、回调补发、`skipped`、单项/批量和缺失任务的内部处理差异；任一非对象 params 元素整次 400，不得再把内部状态序列化为目标成功响应。
6. 对出站回调保存脱敏黄金载荷和字段类型。
7. Debug 接口单独建立“非生产协议”清单，不混入甲方正式接口。

**产物**

- 接口契约矩阵。
- JSON/SSE/WebSocket/Callback 黄金样例。
- 路由测试与 Presenter 测试清单。
- 接口文档与代码差异清单；发现差异先停止并确认，不直接修改接口文档。

### 0B：当前单实例性能基线

**目标**

记录改造前真实瓶颈，避免仅比较框架名称。

**采集范围**

- 短请求吞吐、P50/P95/P99、错误率。
- 活跃 WebSocket/SSE 数量、连接建立和释放时间。
- file/report/weaponry 的排队、运行和总耗时。
- SQLite 锁等待、失败和慢操作。
- 线程数、内存、CPU、文件句柄、临时磁盘增长。
- AnythingLLM 429/5xx/超时、Document Processor 失败和上传等待。
- 回调耗时、成功率和 check-task 补发次数。

本步骤的采集器、数据模板和指标定义已建立。2026-07-15 已批准不在当前阶段连接真实后台服务，实测转移到首次完整集成环境；届时仍须单独确认目标环境和负载范围，且不由采集器启动 `run.py`。

### 0C：目标容量与 SLO 决议

以下分层并发范围已经确认；成功率、P95/P99、最大排队时间等数值 SLO 经批准延期，在首次完整集成环境取得结果后评审确定，并最迟在阶段 10 冻结：

| 维度 | 已确认验收范围 | 仍需确认 |
| --- | --- | --- |
| 短请求 | 100 并发测试，50 并发稳定基线 | 请求混合、延迟阈值、成功率 |
| 长连接 | 50 条同时在线 SSE；WebSocket 按现有接口补测 | 连接持续时间、消息频率、断连率 |
| 在途任务 | 50 个 accepted/queued/running 混合任务 | 各业务比例和最大队列等待 |
| AnythingLLM 摄取 | 初始并发 1，压测后尝试 2、4 | 提升门槛、错误率和数据完整性阈值 |
| 重任务 | 按模型/GPU/AnythingLLM 实测限流 | 是否要求 50 个同时执行 |

正式 SLO 至少写明：成功率、P95/P99、最大队列等待、任务恢复时间、回调最终成功时间和允许积压。

### 0D：基础设施与部署能力确认

每个目标组件必须确认：

- 部署位置、版本、CPU/内存/磁盘、端口和网络路径。
- 生产账号、最小权限、密钥保存和轮换方式。
- 备份、恢复、监控、日志、升级和责任人。
- 单机故障边界；同机多实例不宣称基础设施高可用。
- 测试环境与生产环境隔离方式。
- Web、Worker、Relay、Scheduler 的进程数量和优雅退出要求。

目标生产已确认为单服务器，首期目标是进程级隔离、可靠重启和可恢复，不虚构 RabbitMQ/Redis/MySQL 多节点高可用。

### 0E：数据与产物基线

在阶段 3 设计迁移前完成：

- 三套 SQLite 的表、行数、索引、触发器、Schema 版本和文件大小盘点。
- 非法历史状态、孤儿引用、重复业务键和缺失文件统计。
- 任务、交互审计、知识库映射、chat 消息、租约和清理任务的对象类别、引用关系与删除资格；
  具体保留天数按 D0-21 批准延期。
- 下载缓存、OCR/MinerU 缓存、回调历史和报告产物的 retention class、终态起算点和清理
  所有权；未确认前不启用自动 TTL。
- SQLite 备份、JSON 导出和恢复抽样验证。

已确认当前 SQLite 历史数据仅用于测试，可以清空且无需迁移。阶段 3 仍必须保留 Schema 映射、种子数据和切换验证，不能因无历史数据而跳过数据库一致性测试。

### 0F：安全基线

至少确认：

- 文件下载和 Callback 的资源超时、日志脱敏及部署信任边界；URL 域名/IP 白名单、DNS
  重绑定和重定向策略已按 D0-11 决定在当前受控本地离线拓扑下暂不建设。
- Debug 路由在反向代理/防火墙层的拒绝规则。
- 上传文件扩展名、MIME、恶意文件扫描和解压/路径穿越防护。
- 临时目录和对象存储 key 的任务隔离。
- 日志中密钥、Authorization、文件正文、Prompt 和回调正文的脱敏。
- MySQL/RabbitMQ/Redis/MinIO/AnythingLLM 使用独立最小权限账号。

D0-11 是负责人对当前受控本地离线拓扑作出的风险接受，而不是“所有离线 URL 天然安全”
的通用结论。当前不要求主机白名单或重定向复核；一旦部署拓扑、URL 来源或信任边界改变，
必须重新打开该决策，不能把本次豁免自动扩展到新环境。

---

## 4. 契约测试矩阵

| 接口/协议 | 阶段 0 必测内容 | 不得改变 |
| --- | --- | --- |
| `/llm/analysis` | 单文件、批量、重复 fileName、活动任务、文件路径错误 | 成功 202 空响应体；400/409 错误结构、批量处理顺序和副作用保持 |
| `/llm/generate-report` | params、reportId、filePathList、templateOutline、活动 reportId 重复提交 | 首次 202 空响应体；活动任务重复提交返回 409，且不创建新任务 |
| `/llm/weaponry` | 字段、表格、选中文件、活动任务、404/409 | 成功 202 空响应体；当前字段验证和错误结构不变 |
| `/llm/check-task` | 文档已列 file/report 的单项、批量、混合 params、缺失任务、回调补发和 skipped；weaponry 仅作当前内部兼容回归 | 阶段 6 目标为可靠登记后 200 空响应体，不等待外部回调；非对象 params 整次 400；其他 400/404 与显式恢复触发语义保持，不新增公开 businessType |
| `/llm/progress` | 不带 action 的单项/批量订阅、混合 params、快照、缺失任务、非法 JSON、显式 action 和关闭清理 | 既有进度/error 字段；action/非对象 params 整条失败后保持连接，无 ack、无公开事件游标 |
| `/llm/reassign` | 参数错误、记录不存在、分类不一致、远端异常/`false`、目标 workspace 创建失败、本地条件更新失败、成功 | 保持同步；失败使用既有 HTTP 500 `businessType + msg + data` 结构，不增删字段 |
| `/llm/chat*` | 既有专项计划全部冻结契约 | chatId、SSE、状态码和断连语义 |
| 出站 callback | file/report/weaponry 成功和失败载荷 | 现有字段和业务键 |
| Debug | 路由注册、返回内容和生产访问限制 | 不升级为正式接口 |

---

## 5. 阶段 0 交付物

- [x] 决策表 D0-01～D0-21 均有结论或明确的后置门禁；D0-03 的具体运维责任人随部署 Runbook 补齐。
- [x] 正式接口、WebSocket、SSE 和出站回调契约矩阵完成。
- [x] 契约测试能在不启动 `run.py`、不连接真实后台服务时运行。
- [x] 当前性能采集脚本、测试数据集和指标定义完成；真实结果待已有测试服务授权后采集。
- [x] 50+ 并发工作负载定义得到确认；数值 SLO 待基线后补齐。
- [ ] 目标基础设施拓扑、资源、运维和故障边界得到确认。
- [x] SQLite 历史数据无需迁移，目标产物统一使用 MinIO。
- [ ] 安全基线和 Debug 访问控制得到确认。
- [x] 所有未决项均以决策 ID 跟踪，没有隐藏假设。

### 5.1 阶段 0 关闭判定

2026-07-15 已批准阶段 0 **有条件完成并关闭**。未勾选的生产资源、运维责任与安全部署项已经转入其明确后置阶段，不阻塞阶段 1；真实性能基线不是取消，而是转为以下两个强制检查点：

1. 首次具备完整 DocSense、AnythingLLM/模型和专用测试数据的集成环境时，立即建立首个可测基线并冻结数值 SLO。
2. 阶段 10 必须完成 50+ 混合负载、稳态、积压恢复和故障注入验收；未通过不得进入生产灰度或宣称容量目标完成。

---

## 6. 当前可执行的离线核对命令

```bash
/venv/bin/python -B -m unittest \
  tests.test_routes \
  tests.test_progress_and_check_task \
  tests.test_task_service \
  tests.test_chat \
  tests.test_chat_id_contract \
  tests.test_chat_stream_presenter \
  tests.test_dependency_container \
  tests.test_stage0_contract_assets \
  tests.test_stage0_baseline_tools \
  tests.test_stage0_sqlite_inventory -q

git diff --check
```

上述命令是当前离线回归入口，不代表真实容量成绩。开发阶段继续使用当前环境；真实性能采集已按批准转为首次完整集成环境和阶段 10 门禁，执行时仍需使用专用数据并明确授权目标服务。

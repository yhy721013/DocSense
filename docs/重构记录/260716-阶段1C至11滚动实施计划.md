# 阶段 1C～11 滚动实施计划

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 编写日期 | 2026-07-16 |
| 适用基线 | `refactor/concurrency@52da041` |
| 文档层级 | L2/L2.5 滚动实施计划；承接总计划阶段 1C～11 |
| 上游计划 | `260715-低耦合高并发任务隔离与可靠队列改造总计划.md`、`260715-其余业务分层统一改造实施计划.md` |
| 当前进度 | 阶段 0 已有条件关闭；1A、1B、1C 和 1D-0/0R～1D-7 的开发分支代码与验收已完成。1D 已完成 create 前持久意图/唯一查回/崩溃隔离、HTTP 双阶段租约预算、内部 readiness、Schema v2 有时效机器证明、诊断传播、孤儿资源隔离、永久 AST/配置门禁及遗留引用清单；关闭后又补强真实 Source URL/唯一入库文件身份、共享 FIFO limiter、生产 fail-fast 和人工处置审计。localhost 只读 score/来源身份已验证 8/8，但四类生产指纹未冻结、完整证明文件仍未生成，因此 production ready 明确为 false。analysis/file 的原子受理与 callback latest-wins 只归入 1F 计划，本轮未改其运行时。已批准的 `/llm/check-task` 200 空响应体不属于本波次，当前 JSON 响应保持不变。共享文档处理平级阶段 1H 尚未实施 |
| 近期 L3 | `260716-阶段1C报告生成文件级实施设计.md`、`260717-阶段1D武器谱文件级实施设计.md`、`260718-阶段1H共享文档处理模块文件级实施设计.md` |
| check-task 最终口径 | **2026-07-17 修订**：甲方规定保留请求内同步恢复；可靠队列作为后台兜底。同步入口与 Worker 必须复用同一恢复用例和 Callback Guard，阶段 6 不删除同步副作用 |
| 运行限制 | 开发阶段继续使用当前环境；不得启动 `run.py`。1D-0R 经明确批准仅创建并清理随机临时 AnythingLLM workspace/文档副本，四次执行后既有资源快照精确恢复且无 token 残留；没有批准修改现有 workspace/文档或连接其他真实后台服务 |

本文把阶段 1C～11 细化到可滚动执行、评审和验收的子波次。本文不替代每个阶段开工前
的 L3 文件级设计：越靠后的阶段越依赖前一阶段的 Schema、压测和故障注入结果，若现在
提前冻结所有类名、表结构和队列参数，反而会制造无效返工。

公开接口始终以 `docs/接口文档/` 为准。本文记录的已批准变化只包括既有确认范围；任何
新增、删除或重命名前后端参数的方案一律禁止。实现中若还需改变状态码、错误文本、回调
字段、WebSocket 或 SSE 消息，必须停止并再次确认。

---

## 1. 2026-07-16 新增决策

### 1.1 已确认事项

| ID | 决策 | 实施含义 |
| --- | --- | --- |
| R-01 | 阶段 3 起允许使用仅供自动化集成测试的 Docker Compose | 只启动 MySQL、RabbitMQ、Redis、MinIO；开发应用仍使用 `/venv` 和当前启动方式，不把应用迁入容器，不启动 `run.py` |
| R-02 | analysis、report、weaponry 不设置业务积压数量上限 | 不因正常待处理任务数量返回 503/429；待处理事实必须持久化，执行并发仍按 AnythingLLM/模型资源限流，Worker 持续取出处理 |
| R-03 | `/llm/reassign` 的外部 `false`、目标 workspace 创建失败和本地条件更新失败均算业务失败 | 阶段 1E 执行补偿并返回已确认的 HTTP 500 结构，不再把“未抛异常”当作远端迁移成功 |
| R-04 | 同一业务键采用回调投递 Guard | 新任务先创建则旧回调 stale；旧回调已取得发送权或结果未知时，新提交立即返回 409，不占用 Web 请求线程轮询；`delivery_outcome_unknown` 直至确认旧 Worker 已停止/隔离并完成人工解除审计 |
| R-05 | 报告源文件在 Worker 执行时下载 | HTTP 202 只表示任务已可靠受理；排队期间 URL 内容变化时，以 Worker 实际下载到的版本为本次输入 |
| R-06 | MySQL 第一版统一使用同步 SQLAlchemy Repository | Flask/Celery 直接调用；阶段 8 FastAPI 用受控数据库线程池调用短事务；只有压测证明必要时才增加异步 Adapter |
| R-07 | 当前不建设 URL 安全策略 | 生产被认定为本地离线环境；1C 不增加主机白名单、DNS 重绑定或重定向校验，不把 URL Policy 作为上线门禁。若部署拓扑或信任边界改变，必须重新评审 |
| R-08 | MinIO 具体保留天数暂不确定 | 阶段 1C～3 先建立对象类别、引用和删除资格字段；未确认前不启用自动 TTL，不得按对象创建时间删除排队或恢复中的任务数据 |
| R-09 | report RAG 返回 `None`/空字符串仍按成功处理 | 保留既有空 HTML、`status="1"` 和 `msg="生成成功"`；只增加内部日志/指标，不新增公开字段，也不豁免交互审计硬门禁 |
| R-10 | report 三类异常容器/元素统一严格拒绝 | 顶层非对象、任一非对象 params、filePathList 非字符串或空白元素均在受理前返回已确认 HTTP 400；当前 Flask 路径已实现，不增删参数 |
| R-11 | reportId 数字部分最多 128 位，两个可选文本沿用兼容转换 | JSON 整数/整数字符串仍按整数值统一业务键；正负号不计入、前导零计入上限。`templateDesc`/`requirement` 缺失为 `""`，历史非字符串值先转换为字符串；接口参数集合和输出类型不变 |
| R-12 | weaponry 采用稳定 score-or-rank Selection | 不建设独立 reranker、不设置绝对相关性阈值或正式精度指标；合法分数用于稳定排名，供应商明确无分数时使用稳定正整数 rank；保留来源、正文质量和同文档精确去重门禁，Evidence 全文不截断 |
| R-13 | Schema v2 直接取代 Schema v1 | 当前不存在历史 weaponry execution、历史数据或旧 Worker；v2 移除 reranker/anchor/绝对阈值并冻结 score/rank、排序、正文处理和 Artifact profile。直接删除 v1 Encoder/Decoder、版本常量、专属策略字段和夹具，不建设双读、迁移或分阶段兼容发布；公开接口与数据库表不变 |
| R-14 | 文档处理独立为平级阶段 1H | 在 `app/modules/document_processing` 建立 MHTML/MinerU/OCR/Office、Artifact/lineage 和任务级资源边界；Translator 只负责语言转换。1D 可用带来源/哈希/指纹的历史清洗 Artifact，原始 MHTML 忠实重建不再阻塞 1D-3B |

### 1.2 “无业务积压上限”的准确边界

“不设置积压上限”不等于物理存储无限，也不等于允许无界线程、无界进程内队列或无界
模型并发。目标语义是：

1. API 成功返回前，任务和不可变请求快照已经进入数据库事实；阶段 4 后还必须与 Outbox
   在同一事务提交。
2. 本地兼容 Dispatcher 和未来 RabbitMQ 消息只用于唤醒/派发，数据库任务事实是恢复
   依据；不得把全部积压只放在 Python 内存。
3. 不配置基于“待处理任务数量”的业务拒绝阈值，也不配置 RabbitMQ `x-max-length` 一类
   会因正常积压丢弃或拒绝消息的策略。
4. Worker 并发、AnythingLLM 会话数、GPU/模型并发和下载并发仍是有界资源；积压依次
   等待，资源释放后继续处理。
5. 数据库、磁盘或 Broker 已无法完成可靠写入时，系统不能返回 202。这属于基础设施失败，
   不是正常积压限流；必须告警并保留既有错误结构。
6. 队列间优先级和公平性在阶段 5 L3 结合真实任务耗时确定；当前不承诺三个业务之间的
   全局严格 FIFO，但同一队列内必须有稳定、可审计的领取顺序。

### 1.3 保留周期延期的临时安全口径

- 持久对象默认不自动过期，`eligible_for_deletion_at`/`expires_at` 为空。
- queued、running、retrying、死信待处理、待回调、恢复中和人工处理中的对象始终受保护。
- 保留期不得从对象 `created_at` 直接起算；只能在任务终态、无租约、无重试、无 Outbox、
  无回调恢复和无其他引用后计算删除资格。
- 只有已确认可重建、持久产物已提交且没有执行器继续使用的任务私有 scratch 才可立即
  清理。崩溃或提交结果不明时进入隔离/待清理状态，不直接删除。
- 阶段 3 Schema 预留 `retention_class`、删除资格时间、删除状态、尝试次数和审计字段。
- 在启用第一条非测试自动删除规则前必须确认具体期限。回调载荷若设置期限，会限制
  `/llm/check-task` 的可补发窗口，届时必须先确认是否需要同步接口文档。

### 1.4 阶段 1C 全面审查后的新增门禁

1. 最终报告进入终态所有权后，清理器仍须复核对象存在性、大小和 SHA-256；缺失或篡改
   必须保持 `cleanup_pending/failed` 并告警，不能因“位于 retain 集合”而误报完成。
2. AnythingLLM 写请求超时、连接中断、5xx 或成功响应协议损坏时，必须区分“明确失败”
   与“供应商副作用结果未知”。Workspace 创建可按任务唯一名称精确查回；无法唯一查回、
   或全局文档上传后拿不到 location 时，先审计再隔离，禁止猜测性清理或盲目重放。
3. 外部清理仅允许编排幂等 DELETE；每个生命周期事件必须在下一项删除前以 CAS 持久化，
   清理租约包含 token、开始时间和逐步心跳。过期后先审计已持久事件，再以连续序号重放；
   最后一个“删除成功但事件未落库”窗口依赖供应商 404 幂等收敛。
4. 1C-6 必须装配 `sweep(limit)` 的启动扫描和周期扫描。该门禁已完成：每批有硬上限，
   单任务失败不能阻断同批其他任务，扫描结果输出 cleaned/pending/quarantined/failed 分类指标。
5. `running` execution 在阶段 2 建立 Task Attempt、Step、Checkpoint、lease/fencing 以及
   attempt 级审计幂等键前，禁止通过 SQL 或扫描器直接重置为 `accepted`。1C-6 已按此只恢复
   从未领取的 `accepted`；对遗留 `running` 只告警、盘点和人工隔离。
6. 128 位 `reportId` 只在供应商名称超限时使用稳定摘要，公开值和数据库业务键不截断；
   回调历史文件采用原子排他创建，50 个并发写入不得互相覆盖。

---

## 2. 滚动细化和变更控制

### 2.1 文档层级

| 层级 | 负责内容 | 冻结时点 |
| --- | --- | --- |
| L1 总计划 | 阶段依赖、总体架构、生产目标和最终门禁 | 已落档，重大方向变化才修订 |
| 本文 | 子波次、进入/退出条件、到期决策和回滚边界 | 每完成一个阶段复盘一次 |
| L3 文件级设计 | 文件、类/端口、事务、状态机、失败矩阵、测试、日志和回滚步骤 | 对应阶段编码前 |
| 执行记录 | 实际变更、测试结果、偏差、遗留风险和生产边界 | 每个子波次完成后 |

### 2.2 每个 L3 的强制章节

1. 范围与非目标。
2. 当前代码和数据事实。
3. 已确认接口契约与停止条件。
4. 目标调用链、文件变更表和依赖方向。
5. DTO、Port、Repository、Unit of Work 和错误分类。
6. 并发、幂等、事务、队列、latest-wins 和资源隔离。
7. 失败矩阵、补偿、恢复、日志与指标。
8. 单元、契约、并发、集成和故障注入测试。
9. 灰度、回滚、旧路径删除证据和完成门禁。

### 2.3 不允许跨阶段偷跑的事项

- 1C～1H 不能宣称已经具备 MySQL、Outbox、RabbitMQ、跨实例或多节点可靠能力。
- 阶段 3 不能通过“数据库已共享”宣称消息不会丢失；数据库与 Broker 一致性属于阶段 4。
- 阶段 5 不能只因 Celery 任务能运行就宣称可靠；必须通过 ACK、重复消费、Worker 崩溃、
  DLQ、latest-wins 和外部副作用故障注入。
- 阶段 8 不能借 FastAPI 迁移改变公开参数、错误、WebSocket/SSE 帧或任务完成语义。
- 阶段 10 未完成真实稳态与故障压测前，离线 Fake 的 50 线程测试不能作为生产容量结论。

---

## 3. 总体依赖路径

```text
1C report ─────────┐
1D weaponry ───────┤
1E reassign ───────┼─→ 1G 业务结构收口 → 2 统一任务与进程内隔离 → 3 MySQL/MinIO
1F analysis ───────┤                    → 4 Outbox → 5 RabbitMQ/Celery → 6 逐业务可靠切换
1H document process┘                                           ↓
                                  7 跨实例通知 → 8 FastAPI
                                                    ↓
                                  9 chat 闭环 → 10 容量/故障验收 → 11 生产上线
```

主依赖原则：先稳定业务内核和事务边界，再替换数据库、消息、通知和 Web 框架。FastAPI
继续位于可靠队列之后，避免在迁移 Web 框架时同时重写业务和持久化语义。

---

## 4. 阶段 1C～1H：剩余业务结构统一

### 4.1 1C 报告生成

详细文件级步骤见 `260716-阶段1C报告生成文件级实施设计.md`。

| 子波次 | 状态 | 主要交付 | 退出门禁 |
| --- | --- | --- | --- |
| 1C-0 | **已完成** | 当前/目标契约、并发与回调黄金样例；已确认严格请求校验收口 | 未授权行为全部保持，已批准 202 空体、409 和三类 HTTP 400 单独标识 |
| 1C-1 | **已完成** | Report DTO、领域错误、HTML/回调纯规则 | 无 Flask/SQL/HTTP Client 依赖，纯规则测试通过 |
| 1C-2 | **已完成** | Report Task/File/Artifact/RAG/Audit/Callback/Progress/Dispatcher Port、Submit/Run、严格 Fake | Application 只依赖领域与 Port；无 Flask/SQLite/真实 I/O；URL Policy 未引入 |
| 1C-3 | **已完成并补强** | SQLite 追加 execution、Guard 表、原子受理、不可变请求快照、按 TaskId 领取和条件写；审查后补充 stale 收敛、结果投影分离及 Guard fencing 核心 | 内部 50 同键提交为 1 accepted/49 conflict、50 callback acquire 为 1 owner/49 busy；无锁错误、孤儿行、半提交、旧写覆盖或迟到租约覆盖；公开 202/409 已在 1C-6 切换 |
| 1C-4 | **已完成** | `RunReportTask(task_id)` 所需任务级 Artifact、执行时下载/规范化/Word、多文档 AnythingLLM RAG 和原子 Audit Adapter；审计 Schema v3 无损保存 trace/call 身份 | 50 任务目录隔离；多文档保序和部分失败 trace 完整；审计事务失败禁止成功终态/callback 并保留现场；生产路由未切换 |
| 1C-5 | **已完成并经两轮审查补强** | 回调 Guard、发送前权威复核、精确 HTTP outcome、带旧 Worker 停止确认的人工解除审计、Artifact 完整性、AnythingLLM 未知副作用隔离、逐事件 cleanup 检查点/心跳租约和有界恢复扫描 | stale 在 HTTP 前被拦截；unknown 冻结；最终报告缺失/篡改不误报；清理中断按幂等 DELETE 连续审计恢复；50 个并发回调历史不覆盖；同步提交不等待旧回调 |
| 1C-6 | **已完成并经全面审查补强** | SQLite 持久积压 + Event 常量空间唤醒；一条报告执行 Worker、两条隔离维护线程、稳定事务 FIFO、毒任务/坏资源持久冷却、可中断许可等待、真实关闭语义、跨进程单实例锁、`sweep(limit)`、60/90 秒清理边界、组合根和薄路由切换 | 50 个 accepted 只使用一条执行 Worker且零内存积压项；重型执行不阻塞恢复/诊断；毒任务不热循环；第二进程拒绝取得锁；路由无报告线程、无正常积压拒绝、无双执行/双回调；running 只观测不重置；当前开发分支 202 空体/409 已生效，尚未部署生产 |
| 1C-7 | **已完成** | 契约、并发、架构、故障和扩大回归的阶段关闭；遗留 Worker 三类引用证据及永久 AST 门禁 | 358 项合并定向、74 模块/872 项安全回归、240 文件编译、契约 JSON 和差异检查通过；无新增失败类别，阶段 2～6 输入清单已落档 |

1C-5 的实际代码、故障补强、测试证据和生产未切换边界见
`../更新记录/260716-阶段1C-5Callback Guard与资源恢复闭环执行记录.md`；第二轮全面审查
修复及 1C-6/阶段 2～6 新增硬门禁见
`../更新记录/260716-阶段1C第二轮全面审查风险修复执行记录.md`。
1C-6 的 Dispatcher、组合根、路由切换及生产未部署边界见
`../更新记录/260717-阶段1C-6Dispatcher组合根与路由切换执行记录.md`；全面审查后的生命周期、
调度饥饿、按键 Progress 锁、跨进程门禁修复和 871 项安全回归见
`../更新记录/260717-阶段1C-6全面审查风险修复执行记录.md`。
阶段 1C 最终关闭、遗留 Worker 引用证据、永久 AST 门禁和阶段 2～6 输入清单见
`../更新记录/260717-阶段1C-7阶段关闭验收执行记录.md`。

### 4.2 1D 武器谱

详细文件级设计见 `260717-阶段1D武器谱文件级实施设计.md`。该设计已经把公开路由切换
延后到 Callback Guard、资源恢复和 Dispatcher 均完成的 1D-6，避免中间波次暴露不完整链路。
2026-07-17 已确认五项契约口径：空范围受理时冻结、`architectureId` 规范化为 64 位正整数、
`status` 可选忽略且非法字段结构统一 400、删除模式 1、Callback sending/unknown 时新提交 409。
术语规则辅助在 1D 中改造为可整体删除的 Provider；关闭路径不得产生任何术语资源 I/O。
第三轮全面复盘已冻结七项内部来源/证据隔离硬门禁：Retrieval Query 与 Extraction Prompt 分离、
回调 `rows` 与实际模型 Evidence 逐项一致、无历史来源会话、禁止父 Thread/目标文档二次 RAG、
非空回答上下文强校验、不以 `content` 是否相等代替来源身份校验，以及 Candidate 必须经过
确定性 score-or-rank Evidence Selection。当前批准策略不设置绝对相关性阈值、不使用独立
reranker：合法分数用于稳定排名，供应商明确无分数时使用稳定 rank；协议损坏的批次不进入 rows，
全部候选被拒绝时跳过模型；
8/16 只表示供应商 Candidate 批次，所有通过门禁的 Selected Evidence 完整保留，不设置单条、
总量或单文档数量/字符配额。上述内容不改变任何公开接口或回调参数。

1D-0R 已纠正旧导弹负例和动力预期词遗漏。获批的临时隔离清洗副本真实索引复跑已完成，五个正例
预期事实均排质量门禁后第 1，参考文献型候选为零，第四次权威观测的查询级暂定间隔为
`0.017830`；四次执行的临时 workspace/全局文档均已删除，既有资源快照保持
`d1b3e76148958843`。修复真实 `passage` 元数据包装后，两次复跑候选集合一致，但 Top-1 最大漂移
约 `0.001722`。语料仍只有一份业务
文档，输入不是已丢失的原始 MHTML；该事实继续作为历史质量风险记录，但完整 Candidate 标注、
独立 reranker、绝对阈值和正式精度指标已按 R-12 取消。原始文件处理迁入 1H，不再阻塞
1D-3B；Schema v2 领域/Codec、最小多文档结构黄金样例和生产 Adapter 离线实现已经完成。
供应商协议验证原计划作为 1D-6 切流前硬门禁；当前开发分支已切换源码但未部署，因此保留为
实际环境启用前和 1D-7 阶段关闭硬门禁，不用 Fake Transport 结果替代。

| 子波次 | 状态 | 主要交付 | 关键门禁 |
| --- | --- | --- | --- |
| 1D-0 | **历史资产完成；原阈值门禁已被 R-12 取代** | 当前/目标契约、INPUT/TABLE/来源/回调黄金样例、术语双路径、A/B/Thread/上下文/供应商能力/故障矩阵和离线 Selection Oracle | 原始 score 无法形成可靠绝对阈值的负面证据继续保留；禁止把夹具 `0.82` 或 workspace `0.25` 当作生产阈值，但不再要求另找阈值 |
| 1D-0R | **历史修复与临时隔离闭环完成** | 地面真值纠正、专用 Query、MHTML/Chunk 去噪、只读工具和随机临时资源上传/嵌入/查询/补偿清理 | 临时清洗副本与既有资源零变更证据保留；原始处理和 Translator 解耦移交 1H，不再阻塞 1D-3B |
| 1D-1 | **已完成并完成 v2 定向修订** | 四层骨架、深冻结 DTO、字段/表格/行合并/来源去重/回调纯规则、Candidate/Selected Evidence、Extraction Prompt 类型边界及模式 1 删除 | Query/Prompt、`fieldDescription`、全文 Evidence 和稳定选择保留；生产 reranker/anchor/绝对阈值分支已删除，score/rank 双路径已通过测试 |
| 1D-2 | **已完成并由唯一 v2 Codec 取代开发期 v1** | 请求适配、Document Scope Port、execution 级文档/字段快照和严格 Codec | v2 精确键集合、严格往返、无活动旧 execution 和完整回归通过；不建设 v1 双读或迁移 |
| 1D-3A | **已完成** | Retrieval/Extraction/AuxiliaryGuidance/Translation/Audit/Callback/Resource/Dispatcher 的供应商无关 Port、不可变 DTO 和严格 Fake | Retrieval 只返回 Candidate、Extraction 只接收 Selected Evidence；审计前置；owned/shared、CAS、cleanup lease/fencing；20 项新增、190 项定向、87 模块/1034 项安全回归及 50 线程结构隔离通过 |
| 1D-3B | **离线实现已完成** | AnythingLLM Retrieval/Extraction、NoAuxiliary/Terms、Translation、Audit 和 Resource 生产 I/O Adapter | Candidate score/rank/指纹可审计，非法/冲突分数不伪装缺失或转 0；每来源无历史会话；Evidence/rows 强校验；永久 workspace 零写入；创建结果未知不盲重试；真实供应商/Callback/资源恢复/投递仍属于 1D-6 门禁 |
| 1D-4 | **已完成并经全面审查补强** | `SubmitWeaponryTask` / `RunWeaponryTask(task_id)`、字段执行器、严格 Task/Progress Fake、INPUT/TABLE/失败黄金 Callback，以及 Query/哨兵/Audit/资源/409/文档身份补强 | 只按 TaskId；零 Selected Evidence 不调用 Extraction/Translation；每来源只使用最终 rows 对应 Evidence；Query 无隐藏长度/语义词门禁；历史 Audit 不重放；cleanup intent 先行；终态 tracking 可恢复；50 任务隔离和慢 I/O 无 SQLite 长事务均通过 |
| 1D-5 | **已完成** | 通用持久化 Dispatcher/进程锁、Report 薄包装、Weaponry 单执行 Worker、严格配置、固定策略、隔离维护任务与离线组合根 | 50 个 accepted 只形成持久行和一条执行 Worker；稳定 FIFO、毒任务冷却、运行中只观察、stop/close/readiness/fatal、共享 limiter/进程锁和容量/零结果/契约错误分项观测通过；Report/容器回归通过，生产路由未切换 |
| 1D-6 | **代码与安全门禁完成；完整生产证明待指纹** | Callback Guard、同步 check-task 恢复、资源补偿/保守隔离和公开路由/202 空体切换 | 50 并发回调唯一发送；3xx 拒绝、stale 零网络、unknown 冻结；Audit pending、409/超时和检查点丢失不盲重放；终态 tracking/cleanup pending 有界退避恢复；localhost 已只读验证 8/8 score 与来源身份，空 workspace 模型行为待生成完整证明 |
| 1D-7 | **开发分支代码与验收已关闭；production attestation 待四类指纹** | 永久 AST/配置门禁、I01～I07、全量安全回归、遗留 Worker/Terms 引用清单和后续阶段输入 | 正式路由不回流旧 Worker/线程/Client；无完整 Prompt 召回、Candidate 绕过 Selection、非法分数转 0/伪装缺失、无稳定 rank、父 Thread、目标文档二次 RAG 或 Evidence/rows 分叉；Schema v2 证明缺失/过期/篡改/漂移或生产必需配置缺失时 fail-fast/readiness=false |

2026-07-20 已关闭 1D-7 前的代码型阻塞：Creation Intent 在远端 create 前落库，维护循环
对崩溃 pending 只查回/隔离不重放；Callback/清理租约覆盖连接与读取；内部 readiness 同时
检查 Report/Weaponry Dispatcher 和真实供应商证明；missing execution 的 tracking 资源一次性
隔离；术语/翻译降级进入内部诊断。当前剩余阻塞只有受控真实环境生成有效 production
attestation，不以 Fake 或配置存在代替。

1D-3A 的 Port/Fake 证据见
`../更新记录/260718-阶段1D-3A供应商无关端口与严格Fake执行记录.md`；1D-3B 的唯一 Schema v2、
生产 Adapter、故障补偿和离线验证见
`../更新记录/260718-阶段1D-3B生产IO适配与Schema-v2执行记录.md`。1D-4 Application 的实现、
故障矩阵和验证见 `../更新记录/260718-阶段1D-4应用用例与严格Fake执行记录.md`；全面审查补强见
`../更新记录/260719-阶段1D-4全面审查问题修复执行记录.md`；Dispatcher/配置/离线组合根证据见
`../更新记录/260719-阶段1D-5通用Dispatcher配置与离线组合根执行记录.md`；1D-6 的 Callback、资源、
生产装配、路由切换和验证证据见
`../更新记录/260719-阶段1D-6CallbackGuard资源恢复与公开路由切换执行记录.md`；1D-7 的永久门禁、
遗留清单和离线关闭见 `../更新记录/260720-阶段1D-7关闭验收执行记录.md`。后续阶段可以继续开发；
冻结四类真实能力指纹后仍须运行隔离验证器补齐 production attestation；不再等待绝对阈值或高精度 profile。

### 4.3 1E 分类变更同步 Saga

| 子波次 | 主要交付 | 关键门禁 |
| --- | --- | --- |
| 1E-0 | 当前/已批准目标契约和故障矩阵 | 保持同步；不新增任务 ID、状态查询或参数 |
| 1E-1 | `DocumentReassignmentService`、Saga 步骤与幂等键 | 每一步可识别未开始/成功/失败/需补偿 |
| 1E-2 | Knowledge Port、Document Repository、Unit of Work | 业务层不直接使用 SQLite/AnythingLLM Client |
| 1E-3 | 目标 workspace 准备与文档挂载 | 任一 `false` 或缺失目标引用视为失败，不进入本地提交 |
| 1E-4 | 本地条件更新 | `fileName + oldArchitectureId` 条件更新且 rowcount 必须为 1 |
| 1E-5 | 旧资源清理、反向补偿与人工恢复审计 | 补偿失败不得伪装成功，保留可恢复事实 |
| 1E-6 | 路由切换与回归 | 成功 200；上述失败统一进入已确认 HTTP 500 结构 |

`/llm/reassign` 的最大同步时限仍未知。只要现有同步契约可保持，1E 可先实施；若真实故障
注入证明无法在上游超时内完成，必须停止并单独确认，不能自行改成 202 或新增查询参数。

### 4.4 1F 文件分析二次收口

| 子波次 | 主要交付 | 关键门禁 |
| --- | --- | --- |
| 1F-0 | 单文件/批量、分类、数据标准和回调黄金样例 | 不重新解释已稳定领域规则 |
| 1F-1 | 解析、匹配、映射纯领域规则迁移 | 无外部 I/O，既有规则测试等价 |
| 1F-2 | File/Translation/Callback/Progress Port | 去除全局可变进度回调和跨任务共享状态 |
| 1F-3 | `RunAnalysisTask(task_id)` | 每个文件执行只读取自己的快照和目录；禁止路由或业务函数启动 daemon/thread |
| 1F-4 | 有序批次协调器 | 请求顺序、每文件终态和批次失败隔离保持；`fileName` 作为唯一逻辑任务键 |
| 1F-5 | 原子受理、持久化积压 Dispatcher、薄路由和 202 空体 | `/llm/analysis` 必须先用 `create_if_allowed` 同事务判定同键；accepted 后只唤醒 Dispatcher，不创建线程；活动任务返回既有 409，不因正常积压返回 503 |
| 1F-6 | RAG/知识库资源租约收口 | 临时资源、永久资源和失败补偿边界清晰 |
| 1F-7 | Callback latest-wins、50 任务隔离与扩大回归 | file 回调在 HTTP 前按 `fileName + expected TaskId` 复核；旧执行 stale/skipped、无网络调用；输入、进度、临时文件、翻译和回调不串扰 |

> 2026-07-20 复核说明：上述 analysis/file 问题不在 1D 运行时代码中提前修复；本轮只将其
> 固化为 1F 的显式交付和门禁。实施时不得增删接口参数，既有 `/llm/analysis` 202 空体和
> 409 契约保持不变。

### 4.5 1H 共享文档处理与 Translator 解耦

详细文件级设计见 `260718-阶段1H共享文档处理模块文件级实施设计.md`。1H 与 1D 平级，
不再嵌入 1D-3B；它可以与 1D/1E/1F 的后续实现并行，但 1G 的最终遗留删除必须等待 1H-5。

| 子波次 | 主要交付 | 关键门禁 |
| --- | --- | --- |
| 1H-0 | translator/utils/report/analysis/RAG 调用清单、当前/目标行为和自建格式黄金语料 | 不访问生产文档/workspace；冻结降级、临时目录、配置和资源所有权 |
| 1H-1 | `document_processing` Domain/Application/Ports、不可变 Artifact/Profile/Lineage 与严格 Fake | 新模块不导入 Translator/Flask/具体 Converter；相同 task/step/profile 确定性 |
| 1H-2 | 独立 MHTML MIME/主内容纯规则、浏览器转换 Adapter 和历史 Artifact provenance | 现有 workspace 零修改；`legacy-normalized-artifact-v1` 不伪装原始输入 |
| 1H-3 | MinerU、内置 OCR、Office/PDF/TXT/MD Adapter 和重型许可 | 50 在途任务目录/状态隔离，实际转换有界；结果未知不盲重试 |
| 1H-4 | utils、report、analysis/RAG、translation 调用点迁移及 Translator 兼容 Facade | Translator 只负责语言转换；同一请求没有新旧双执行；公开契约不变 |
| 1H-5 | 扩大回归、AST 门禁、旧 Handler/Converter 三类引用证据 | 新转换代码禁止进入 Translator/utils；阶段 3 只替换 MinIO Adapter |

1D 可在记录来源、SHA-256 和处理指纹的前提下使用历史清洗 Artifact，因此 1H 不阻塞
1D-3B/1D-6；但新的生产文件处理入口在 1H 关闭后不得继续由 Translator 拥有转换生命周期。

### 4.6 1G 组合根、Debug 与遗留清理

1. 建立不依赖 Flask `current_app` 的 Bootstrap；Flask 只安装已构造的应用依赖。
2. Debug 路由只调用 Query Service/Debug Adapter，不直接读数据库文件。
3. 正式路由只保留协议解析、应用用例调用和 Presenter。
4. 对旧 `report_service.py`、`weaponry_service.py`、`llm_service.py`、Translator 文件转换器、旧 Client 和工具调用做
   静态引用盘点；只有运行路径、测试和配置三类证据均证明无调用后才能删除。
5. 阶段 1 完成后输出模块数据所有权、依赖图、遗留 Adapter 清单和阶段 2 输入清单；1H-5
   未形成三类引用证据前，不删除 Translator 旧文件处理实现。

---

## 5. 阶段 2：统一任务语义与进程内隔离

### 2-0 统一模型

- 冻结 Task、Attempt、Step、Event、Input Snapshot、Artifact Reference、Callback Delivery
  和公开投影之间的关系。
- chat 保持 ChatRun 领域事实；通用 Task 只表达可调度执行、attempt、租约和队列状态。
- 明确终态不可逆、重试创建新 attempt、同业务键活动唯一和 latest-wins 条件写。
- 为每次模型/外部副作用执行建立 attempt 级审计幂等键；不得继续使用仅含 TaskId 的键去
  覆盖或复用一次非确定性重跑结果。

### 2-1 本地兼容执行器

- 将 1C/1D/1F 的业务兼容 Dispatcher 收敛为统一固定 Worker 数执行器。
- 待处理任务保存在数据库；内存只保存有界唤醒信号，可丢唤醒但不可丢任务事实。
- 启动和周期扫描恢复 accepted/queued 任务；停止时有界等待并记录未完成任务。
- 不设置业务积压数量上限；FIFO 键、扫描游标和最老等待时间必须可观测。

### 2-2 隔离、租约与 fencing 预演

- 每个任务拥有独立目录、依赖实例、日志上下文和取消令牌。
- 引入本地 attempt、lease owner、lease expiry、fencing token 和心跳的可测试语义。
- 旧 attempt 的进度、结果、产物和回调写入必须被 expected task/attempt/fencing 条件拒绝。
- 在 Reaper 可以重领 `running` 前，先持久化 Step/Checkpoint：至少区分“未产生外部
  副作用”“副作用引用已登记”“结果不确定”“终态已提交”。只有可证明可重试的步骤才
  创建新 attempt；不能用通用 `running → accepted` 更新代替恢复判定。

### 2-3 故障与验收

- 覆盖重复派发、Worker 异常、进程停止、超时、取消、失联任务回收和清理失败。
- 50 个 Fake 重任务同时在途，执行并发受限但全部最终被处理，且无输入/进度/文件串扰。
- 本阶段仍是单实例 SQLite 兼容能力，不作为生产可靠队列结论。

---

## 6. 阶段 3：共享 MySQL、同步 SQLAlchemy Repository 与 MinIO

### 3-0 集成测试环境

- 新增测试专用 Compose，只包含 MySQL、RabbitMQ、Redis、MinIO；固定镜像版本和健康检查。
- 应用测试由 `/venv` 解释器从宿主机运行；测试不得启动 `run.py`。
- 每次测试使用独立 database/schema、bucket prefix、queue/vhost 或命名空间并可重复清理。

### 3-1 SQLAlchemy/Alembic 基线

- 单一 SQLAlchemy 2 Metadata 描述生产表；Alembic 从空库升级，不在应用启动时隐式改表。
- 第一版统一使用同步 Engine/Session/Repository；每个请求或 Worker 任务独占 Session，
  禁止跨线程、跨进程或异步任务共享。
- Unit of Work 控制一次业务事务的 commit/rollback；Repository 不自行提交，不执行网络 I/O。
- `reportId` 业务键使用规范化十进制文本，不能因 MySQL `BIGINT` 缩小为 64 位；Schema
  必须容纳可选负号与最多 128 位数字，并由 Web/领域双层执行已批准上限。

### 3-2 Schema 和 Repository 等价

- 建立 tasks、attempts、inputs、events、artifacts、callback recovery/delivery、chat、knowledge
  等表、外键、唯一约束和索引。
- SQLite 兼容实现和 MySQL 实现运行同一套 Repository 契约、并发唯一性和事务回滚测试。
- 使用空库和版本化测试种子；历史 SQLite 测试数据不迁移。
- 将当前兼容期聚合在 `LLMTaskService` 的任务、回调 Guard、资源恢复和审计能力拆为窄
  Repository/Unit of Work；Application/Adapter 不再依赖一个数千行具体服务类。拆分过程
  必须先跑 SQLite/MySQL 双实现契约测试，不能在路由中直接散落 SQLAlchemy Session。

### 3-3 MinIO Adapter

- 为输入、任务 scratch、中间产物、最终报告和不可变回调载荷定义对象 key/metadata/checksum。
- 将 1D weaponry 仅冻结身份/引用的文档快照升级为 execution-owned 内容 Artifact：受理后保存
  不可变对象引用、size、SHA-256 和所有权，Worker 不再依赖可被替换的单机路径或同名文档。
- 为 1H DocumentProcessing 的 Artifact/Profile/Lineage 实现同步 SQLAlchemy Repository 和 MinIO
  ArtifactStore Adapter；处理 Application/Port 不因存储替换而改变。
- 数据库保存对象引用和删除资格，MinIO 不作为业务状态真相。
- 具体 7/30/180 天等期限保持未启用；自动删除前必须另行确认。
- `retain` 必须对 MinIO 对象执行存在性、size、checksum/ETag 语义校验；校验失败与本地
  Artifact 一样保持 pending 并告警。数据库记录为所有权真相，不能只因对象 key 存在就
  宣称完整，也不能依赖单机本地路径作为多实例共享事实。

### 3-4 单写切换与演练

- 按模块切换唯一写路径，禁止长期 SQLite/MySQL 双写。
- 验证连接池耗尽、死锁、事务回滚、唯一冲突、MinIO 暂停和对象写成功/数据库提交失败。
- 完成空库升级、备份恢复、Alembic 升降级和回切演练。

---

## 7. 阶段 4：事务 Outbox 与可靠派发

### 4-0 事件契约

- 消息只携带 task ID、attempt/command ID、schema version、trace ID 和最小路由信息。
- 不把完整请求、密钥、文件正文或公开响应对象复制进消息。
- Consumer 必须兼容重复消息并拒绝未知 schema version。

### 4-1 同事务事实

- 任务受理、不可变输入和 Outbox 在同一 MySQL Unit of Work 提交。
- 后台 callback recovery request 与 Outbox 在同一事务创建/复用；MySQL 未提交不得把该
  后台命令视为可靠登记。`/llm/check-task` 仍在请求内同步执行必要恢复，其 HTTP 200 不以
  Broker 或后台 Outbox 成功为前提，也不新增公开结果字段。
- Report 活动任务唯一、callback unknown 冻结和 Outbox 登记均使用数据库条件/约束，不能
  依赖进程锁作为最终正确性依据。

### 4-2 Relay

- Relay 用短事务批量领取 Outbox，发布时启用 publisher confirm。
- 数据库领取与 Broker 发布之间允许重复、不允许静默丢失；确认后条件标记 published。
- Broker 暂停时保留数据库积压，并监控最老事件年龄、失败率和重试次数。

### 4-3 故障注入

- 提交前崩溃、提交后发布前崩溃、发布成功但确认丢失、确认后标记前崩溃均需验证。
- 重复发布不得重复创建外部资源或绕过 callback Guard。

---

## 8. 阶段 5：RabbitMQ/Celery Worker 与可靠消费

### 5-0 队列拓扑

- analysis、report、weaponry、callback、cleanup 使用可独立限流和观测的路由。
- 不设置正常业务积压数量上限或溢出丢弃；实际磁盘水位、RabbitMQ flow control 和资源
  告警属于基础设施保护。
- 每类队列定义 durable、persistent message、DLX/DLQ、prefetch、优先级和重放权限。

### 5-1 薄 Celery Task

- Celery Task 只校验信封、建立日志上下文并调用 `RunXxxTask(task_id)`。
- 使用 late ACK；只有业务事实安全提交后 ACK。Worker 丢失、超时和重投必须创建/复用
  受控 attempt，不能直接重开终态。

### 5-2 资源隔离

- AnythingLLM、模型、下载、预处理和 callback 分别配置并发许可与超时。
- analysis/report 当前共享的重型上传资源继续按真实安全容量限流；队列可以积压，不能
  为追求吞吐放大下游雪崩。

### 5-3 Callback Worker

- 投递前在同业务键串行化边界内复核 latest task ID。
- 自动恢复 Worker 与 `/llm/check-task` 的同步恢复必须调用同一应用服务、使用同一
  execution 级 Callback Guard，并分别记录 `background`/`check_task` 触发来源；禁止形成
  两套发送状态机或独立发送权。
- 仅明确未送达错误有限重试；非 2xx 进入人工/DLQ；发送后结果未知标记
  `delivery_outcome_unknown`，不自动重试并冻结同业务键新提交。
- 人工解除必须是内部运维命令，记录操作者、原因、前后状态和审计时间；不新增公开接口参数。

### 5-4 故障验收

- 覆盖重复消息、Worker kill -9、ACK 丢失、Broker 重启、DB 短暂失败、AnythingLLM 超时、
  MinIO 暂停、callback unknown 和 DLQ 重放。
- 验证积压可持续增长并在资源恢复后持续排空，且 Web 进程内存不随积压线性增长。

---

## 9. 阶段 6：逐业务切换到可靠队列

切换顺序：callback 后台兜底 → cleanup/compensation → analysis → report → weaponry。

每类业务必须独立完成：

1. 新旧路径黄金样例和副作用对比。
2. 确认只有一个 Dispatcher/Consumer 拥有执行权，禁止双执行者。
3. 小流量切换、队列/任务/回调/产物观测、故障回滚演练。
4. 回滚只切组合根绑定；已进入新队列的任务由新 Consumer 排空，不能让旧执行器重复接管。
5. 稳定观察后删除对应本地任务执行路径；甲方规定的 check-task 同步恢复不在删除范围。

check-task 不再安排“由同步切为异步”的生产切换。报告类型当前已在请求内使用共享 Guard
完成必要恢复；阶段 4～6 只增加 MySQL recovery request + Outbox + callback Worker 后台
兜底。同步请求和后台消息并发命中同一 execution 时最多一方取得发送租约，旧任务在新任务
提交后必须判定 stale 并跳过。既有 HTTP 200 空响应体、400/404、请求参数和回调载荷不变。

---

## 10. 阶段 7：跨实例 Progress 与事件通知

### 7-0 事实与唤醒分离

- MySQL 保存任务/ChatRun 进度和有序事件真相；Redis 只发送轻量唤醒，不承担唯一事实。
- Redis 消息丢失时，连接端按游标/版本定期回查 MySQL，不产生永久漏通知。

### 7-1 进程级订阅聚合

- 同一进程对同一业务键只维持一份 Redis 订阅/监听，再扇出到多个本地连接。
- 每连接继续使用有界合并缓冲和单写入原则；慢连接只影响自身。

### 7-2 契约保持

- `/llm/progress` 不新增事件 ID、游标或 ack，不恢复已删除的显式 action。
- Chat SSE 保持事件名称、顺序和终态格式；内部断线恢复不能泄露 task/run ID。

---

## 11. 阶段 8：迁移 FastAPI Web 适配层

| 子波次 | 内容 | 门禁 |
| --- | --- | --- |
| 8-0 | Bootstrap、lifespan、配置和异常映射 | 与 Flask 共用同一 Application/Port/Repository 契约 |
| 8-1 | 普通 JSON HTTP 路由 | 请求、状态码、空响应体、错误 JSON 完全等价 |
| 8-2 | WebSocket Progress | 错误后保持连接、无 ack、释放和慢连接隔离等价 |
| 8-3 | Chat SSE | 响应头、事件行、终态、断连和清理等价 |
| 8-4 | 双栈灰度 | 同一时刻每个任务类型仍只有一个受理/执行所有者 |
| 8-5 | Flask 下线 | 静态引用、运行配置、回滚窗口和观察期证据齐全 |

同步 SQLAlchemy Repository 在 FastAPI 中只能通过受控线程池执行短事务，不能直接阻塞
事件循环。阶段 8 压测若证明数据库线程池是瓶颈，再为 Web 查询路径增加 AsyncSession
Adapter；同步 Celery Repository 保持不变，两者共享 Schema 和 Repository 语义，不共享 Session。

---

## 12. 阶段 9：Chat 可靠派发与多实例闭环

1. ChatRun 受理事实、用户 pending 消息、输入快照和 Outbox 同事务。
2. Worker 从 task/run 引用恢复，不依赖原 Web 连接；事件持续写 MySQL。
3. SSE 从事件账本追赶并由 Redis 唤醒；断线不改变 Worker 所有权。
4. abort 跨实例通知、lease/fencing 和终态条件写完整闭环。
5. delete/title 产生的清理任务进入可靠队列；同步公开语义保持，补偿可审计。
6. Chat 文件与产物统一使用 MinIO；不得删除尚被会话、run、消息或清理任务引用的对象。

---

## 13. 阶段 10：容量、稳态与故障验收

### 10-0 冻结环境和 SLO

- 记录 CPU、内存、GPU、磁盘、网络、MySQL/RabbitMQ/Redis/MinIO/AnythingLLM 版本和配置。
- 冻结短请求成功率、P95/P99、最大可接受排队时间、积压恢复时间、回调恢复时间和压测时长。
- 若保留周期仍未确定，容量模型必须按“不自动删除”最坏情况计算；启用 TTL 前另行确认。

### 10-1 分层负载

- 至少 50 个在途任务；重型 AnythingLLM/模型任务按资源限流排队并最终全部处理。
- 至少 50 条 WebSocket/SSE 长连接，同时保持不少于 50 个短请求的稳定并发。
- 分别测 Web 受理能力、数据库/Outbox、Broker、Worker、外部模型和积压恢复，不能只给总吞吐。

### 10-2 稳态和资源泄漏

- 长时间运行并观察内存、线程、文件描述符、连接池、临时文件、对象存储和队列趋势。
- 验证慢连接、超大积压、Worker 扩缩和持续 callback 失败不会拖垮无关业务。

### 10-3 故障矩阵

- 注入应用、Worker、MySQL、RabbitMQ、Redis、MinIO、AnythingLLM 和网络分区/超时故障。
- 不允许任务静默丢失、旧执行覆盖、重复终态、不可解释积压或未经审计的人工修复。

---

## 14. 阶段 11：生产上线与旧路径退出

1. Docker Compose 固定镜像 digest、资源限制、健康检查、启动顺序和数据卷。
2. 完成 MySQL/MinIO 备份恢复、RabbitMQ 定义备份、密钥轮换、日志脱敏和监控告警。
3. URL Policy 按 R-07 不作为当前门禁；若实际生产不再是受控本地离线环境，必须在上线前
   重新评审并另立安全实施项。
4. 按接口/任务类型灰度，定义观察窗口、自动/人工回滚条件和未完成任务处置方式。
5. 确认新写入不需要回切后，依次下线 Flask、SQLite 生产写路径、本地 Dispatcher 和无调用 Adapter。
6. 输出生产 Runbook：积压、DLQ、callback unknown、人工解除、备份恢复、扩容和故障升级。

---

## 15. 到期决策日历

| 事项 | 当前状态 | 最晚门槛 |
| --- | --- | --- |
| 1C 文件级设计 | 本文配套 L3 已创建 | 1C 编码前评审 |
| AnythingLLM 拓扑、速率与可观测接口 | 未确定 | 阶段 5 队列/配额 L3 前 |
| 队列间优先级和公平性 | 未确定；不影响“无积压数量上限” | 阶段 5 L3 前 |
| `/llm/reassign` 最长同步时限 | 未知，先保持同步 | 1E 故障基线证明不满足时立即确认 |
| MySQL 具体 Schema/索引/隔离级别 | 未确定 | 阶段 3 L3 前 |
| RabbitMQ/Celery 版本、队列类型、prefetch | 未确定 | 阶段 5 L3 前 |
| MinIO 具体保留期 | 批准延期；当前不自动删除 | 启用首条非测试删除规则前；最迟阶段 10 容量冻结时确认或按永久保留验收 |
| 回调载荷删除后的 check-task 恢复窗口 | 未确定 | 任何 callback payload TTL 启用前，且可能需要接口文档确认 |
| report RAG 空结果是否算成功 | **已确认**：继续按空 HTML 成功回调，只增加内部可观测性 | 已关闭；1C 契约测试固定 |
| check-task 200 异步完成点措辞 | 已有目标，待最终文字确认 | 阶段 6 生产路由切换前 |
| 数值 SLO 和压测时长 | 批准延期 | 首次完整集成环境初测；阶段 10 硬门禁 |
| Docker Compose 版本、资源、备份和责任人 | 未确定 | 阶段 11 上线前 |

---

## 16. 全局完成判据

- 业务层不依赖 Flask/FastAPI、Celery、SQLAlchemy 实现、Redis、RabbitMQ 或 AnythingLLM Client。
- 所有异步任务以持久化 task ID 恢复；Web 不创建业务后台线程。
- 正常积压不触发业务数量阈值拒绝，且积压不会造成 Web 进程内存线性增长。
- 任务、输入、Outbox、回调、进度和产物的事务/引用关系可审计、可恢复。
- 至少 50 在途任务、50 长连接和 50 短请求稳定并发通过真实环境验收。
- 无任务串扰、静默丢失、重复终态、旧回调覆盖和未记录的人工修复。
- 所有已批准公开契约通过自动化黄金样例；没有新增或删除任何前后端接口参数。

# 其余业务分层统一改造实施计划

## 0. 文档信息

| 项目 | 内容 |
| --- | --- |
| 编写日期 | 2026-07-15 |
| 适用基线 | `main@0023c47` |
| 文档性质 | `260715-低耦合高并发任务隔离与可靠队列改造总计划.md` 阶段 1 的专项实施计划 |
| 上游计划 | `260703-AnythingLLM低耦合改造总计划.md`、`260707-文件对话功能改造计划.md` |
| 执行级设计 | `260715-统一任务最小契约与Progress语义预设计.md`、`260715-阶段1A-1B文件级实施设计.md`、`260716-阶段1C报告生成文件级实施设计.md`、`260717-阶段1D武器谱文件级实施设计.md`、`260718-阶段1H共享文档处理模块文件级实施设计.md` |
| 文档状态 | 实施中；阶段 0 已有条件关闭，波次 1A、1B、1C 和 1D-0/0R～1D-7 的开发分支代码与验收已完成。weaponry 的真实 Callback Guard、同步 check-task 恢复、资源恢复、生产组合根、公开 202 空体薄路由、Creation Intent、HTTP 租约、永久 AST/配置门禁及遗留清单均已落地；关闭后又补强真实 Source URL 身份、共享 FIFO limiter、Schema v2 有时效证明和人工处置审计。localhost 只读 score/来源身份已验证 8/8，但四类生产指纹未冻结、完整 production attestation 未生成，readiness 必须保持 false。共享文档处理平级 1H 尚未实施。报告和 weaponry 的 check-task 均已迁移为各自业务模块内共享 Guard 的同步恢复；已批准的 200 空响应体尚未切换，阶段 4～6 增加后台可靠队列兜底 |
| 接口影响 | 已确认 report 活动任务及 callback outcome unknown 冻结返回 409、三类受理成功 202 空响应体、check-task 成功 200 空响应体、check-task/Progress 严格 `params` 元素校验、Progress 显式 action 错误后保持连接且无 ack、reportId 入站接受 JSON 整数/十进制整数字符串且数字部分最多 128 位（正负号不计入、前导零计入）、`templateDesc`/`requirement` 兼容字符串化、report 顶层非对象/任一非对象 params/filePathList 非字符串或空白元素按已确认文本返回 400，以及 reassign 的远端 `false`/目标创建失败/本地条件更新失败按既有 500 结构处理；参数集合、输出字段类型、错误字段结构、回调、SSE 和 chat 契约不变 |
| 数据迁移 | 本计划只建立 Repository/Unit of Work 边界；SQLite 历史测试数据无需迁移，阶段 3 初始化空 MySQL Schema |
| check-task 队列决策 | **2026-07-17 修订**：甲方规定永久保留请求内同步恢复；可靠队列作为后台兜底。同步请求和 callback Worker 共用同一 Guard、expected TaskId、lease/fencing 与 outcome，不建设第二套并行发送链 |
| 执行限制 | 不启动 `run.py`；未经明确授权不连接真实 AnythingLLM、模型、回调或其他后台服务。1D-0R 已按授权四次创建、嵌入并清理随机临时 AnythingLLM 资源，既有 workspace/文档快照未变；没有批准覆盖或修改现有资源 |

> **2026-07-25 集成前置更新：** `refactor/integration` 已完成 M0～M9 的工程交接并创建
> PR #79，当前等待独立审核和维护者实际合入 `main`。阶段 1E 已完成；下一项**开发**工作为
> 1F“Analysis 二次高内聚收口”。1F 不得在该 PR 尚未实际合入时把集成分支视为新的主线基线；
> 合入后须重新冻结 `main` 提交、复核权威接口文档，并形成 1F 文件级实施设计后再改代码。
> 集成的 M9 证据见 `../更新记录/260725-main与refactor并发重构分支集成执行记录.md`。

本文用于把 AnythingLLM 低耦合改造和文件对话改造形成的结构能力推广到项目其余接口与内部功能。若实现可能影响 `docs/接口文档/` 的公开内容，必须立即停止并取得确认；**任何情况下都不得擅自增删前后端请求参数**。2026-07-17 对 check-task 的修订只改变内部基础设施路线：保持甲方规定的同步副作用，同时增加后台可靠兜底；不改变参数、状态码、响应字段或回调载荷。

---

## 1. 在现有计划体系中的位置

三份计划分别解决不同维度的问题：

| 计划 | 负责解决的问题 | 本计划如何承接 |
| --- | --- | --- |
| `260703-AnythingLLM低耦合改造总计划.md` | AnythingLLM Transport、Client、Port、Gateway、Factory 和供应商边界 | 复用已经完成的端口与网关；继续完成 report、weaponry、reassign 等遗留业务收口 |
| `260707-文件对话功能改造计划.md` | chat 业务的应用用例、领域状态、持久化、租约、Presenter 和可替换调度边界 | 把 chat 作为“业务模块内部高内聚”的参考实现，不复制其单实例基础设施限制 |
| `260715-低耦合高并发任务隔离与可靠队列改造总计划.md` | 统一安排业务解耦、任务隔离、MySQL、Outbox、RabbitMQ/Celery、FastAPI 和多实例 | 本计划是其阶段 1 的详细子计划；本计划完成后，阶段 2 才统一任务语义 |

本计划不是第四条并行改造路线。它的作用是完成总计划阶段 1，使后续数据库、队列和 FastAPI 只需替换适配器，而不再重写业务。

---

## 2. 当前范围与剩余结构债务

### 2.1 正式接口范围

| 接口 | 当前结构状态 | 本计划目标 |
| --- | --- | --- |
| `POST /llm/analysis` | AnythingLLM 已通过 RAG Port/Gateway 隔离，但服务仍混合任务、文件、翻译、进度、回调和结果映射 | 保留现有 Gateway，二次拆分应用编排、领域规则和共享能力 |
| `POST /llm/generate-report` | 路由启动线程；报告服务直接创建旧 `AnythingLLMClient`，同时负责下载、Prompt、生成和回调；当前重复 `reportId` 会覆盖最新投影 | 拆为报告提交/执行用例、领域格式规则和多文档 RAG/File/Callback Port；活动 `reportId` 重复提交按已确认契约返回 409 |
| `POST /llm/weaponry` | 路由校验复杂；服务直接依赖旧 Client、知识库数据库、配置、进度和翻译 | 拆分检索编排、字段/表格领域规则、来源映射与外部能力适配 |
| `POST /llm/check-task` | 路由理解不同业务键、任务状态和回调状态，并可能补偿重放终态回调 | 每个业务模块提供同步 Recovery Source/用例，统一进入共享 Callback Guard；后台 `RequestCallbackRecoveryService`、Outbox 与 RabbitMQ Worker 仅作可靠兜底 |
| `WS /llm/progress` | WebSocket 路由直接订阅进程内 `LLMProgressHub` 并拼装快照，另含无外部需求依据的显式 action/ack | 建立无 action 的 Progress Snapshot/Subscription Port 与协议 Presenter，连接关闭释放订阅 |
| `POST /llm/reassign` | 路由直接编排知识库文档移动、本地数据库更新和失败恢复 | 建立同步 `DocumentReassignmentService`、Unit of Work 和可补偿 Saga |
| `/llm/chat*` | 已完成业务分层和单实例能力门禁 | 不重做业务结构；只在总计划后续阶段替换持久化、Dispatcher 和通知适配器 |

`/llm/callback` 在接口文档中描述的是服务端向外部系统发送的回调契约，当前不是需要新增的入站 Flask 路由。本计划只重构内部回调发送能力，不改变既有回调载荷。

### 2.2 调试接口范围

`/debug/api/callback`、`/debug/api/chat/bootstrap`、`/debug/callback`、`/debug/chat` 属于调试适配器。它们不进入任务领域或生产业务模块，但仍需遵守以下边界：

- 只能调用公开的 Query Service 或专用 Debug Query Adapter。
- 不得绕过 Repository 直接读取生产数据库文件。
- 不得在生产环境因 FastAPI 迁移而意外开放。
- 调试页面模板不参与领域模型和任务状态机。

### 2.3 需要共同收口的内部能力

| 当前能力 | 主要问题 | 目标边界 |
| --- | --- | --- |
| `LLMTaskService` | 同时承担任务写入、查询、交互审计、生命周期事件和回调重放 | `tasks` 模块内的 Command/Query Service、Repository 和审计端口 |
| `DatabaseService` | SQLite Schema、知识文档、Workspace 映射与业务操作集中在一个类 | Knowledge Document Repository、Workspace Mapping Repository 和 Unit of Work |
| `LLMProgressHub` | 进程内实现直接暴露给路由和业务服务 | `ProgressPublisher`、`ProgressQuery`、`ProgressSubscriber` 端口；当前 Hub 只是单实例适配器 |
| `callback_client.py` | 业务代码直接执行 HTTP 和本地历史落盘 | `CallbackDeliveryPort`；阶段 4～6 由 Outbox 和 callback Worker 接管 |
| 文件下载与产物 | URL 下载、临时路径、报告产物和文件命名混在业务流程 | `SourceFilePort`、`ArtifactStorePort` 和任务级资源命名空间 |
| OCR/MinerU/格式处理 | 具体实现、全局环境变量和业务选择逻辑交织 | `DocumentPreprocessorPort` 与任务作用域配置 |
| 翻译服务 | 模块级单例和可变进度回调存在任务串扰风险 | `TranslationPort`；进度报告器按任务显式传入 |
| Prompt | 共享文件持续累积不同业务 Prompt | Prompt 构造器回到所属业务模块；只共享纯格式工具 |

---

## 3. 目录结构方案比较与正式决策

### 3.1 可选方案

| 方案 | 优点 | 主要风险 | 结论 |
| --- | --- | --- | --- |
| 传统技术分层：controllers/services/repositories/models | 简单直观、迁移成本较低 | 一个业务散落多个全局目录，容易继续形成巨型 Service | 不采用为目标结构 |
| 全局 Clean Architecture：domain/application/ports/infrastructure | 依赖方向清晰、测试方便 | 业务增多后全局 domain/application 容易成为新的大杂烩 | 只吸收依赖规则，不采用纯全局目录 |
| 纯按业务聚合：chat/report/weaponry | 业务高内聚、修改范围清晰 | 各模块可能重复连接池、队列、日志和外部 Client | 作为基础，但必须增加统一分层与平台边界 |
| 垂直切片：submit/query/execute | 单个用例交付快、适合命令查询 | 领域规则和基础设施若无约束容易重复 | 作为模块 application 内部组织方式 |
| 插件化 | 动态扩展能力强 | 生命周期、兼容协议和插件治理成本过高 | 当前不采用 |
| 微服务 | 部署和故障隔离强 | 分布式事务、网络契约、部署和运维成本过早放大 | 当前不采用；保留未来按模块拆分可能 |
| **模块化单体 + 业务聚合 + 模块内分层 + Ports/Adapters** | 同时获得高内聚、低耦合、渐进迁移和未来可拆分性 | 需要架构测试持续约束模块边界 | **正式采用** |

### 3.2 正式架构决策

本项目采用以下组合：

1. 以**模块化单体（Modular Monolith）**作为部署和代码组织基础。
2. 以 analysis、report、weaponry、reassign、chat、tasks 作为业务模块。
3. 每个模块内部使用 domain、application、ports、adapters 分层。
4. application 内按 command/query/execute 等垂直用例切片。
5. AnythingLLM、数据库、消息队列、Redis、存储、翻译和文档处理通过 Port/Adapter 隔离。
6. Web、Celery Worker 和调度器都是入站适配器，不拥有业务规则。
7. 当前保持单体部署；只有模块边界、数据所有权和运维能力成熟后才评估拆微服务。

### 3.3 为什么不能直接复制 chat 目录

chat 已经证明业务聚合方式可行，但目标结构不能机械复制其当前 SQLite、locking 和 persistence 细节：

- chat 的领域状态机和应用用例可以作为参考。
- chat 当前单实例 SQLite、内联 Dispatcher 和轮询通知只是过渡适配器，不是所有模块的基础设施模板。
- 每个模块拥有自己的 Repository 协议和数据映射；数据库 Engine、Session、迁移、消息连接等平台能力统一管理。
- 不要求立即把现有 `services/chat/` 物理移动到新目录；先建立依赖边界，再在测试保护下渐进整理。

---

## 4. 目标目录与放置规则

### 4.1 目标目录

```text
app/
├── modules/
│   ├── analysis/
│   │   ├── domain/                  # 解析规则、结果映射、领域错误
│   │   ├── application/
│   │   │   ├── commands/            # 提交、取消等写用例
│   │   │   ├── queries/             # 业务查询用例
│   │   │   ├── executors/           # 只按持久化任务 ID 执行
│   │   │   └── dto/                 # 框架无关 DTO
│   │   ├── ports/                   # 本模块拥有的能力需求
│   │   └── adapters/                # 本模块专属 Repository/映射适配
│   ├── report/
│   ├── weaponry/
│   ├── reassign/
│   ├── chat/                        # 由现有 services/chat 渐进演进
│   ├── document_processing/         # MHTML/MinerU/OCR/Office 与 Artifact 谱系
│   └── tasks/                       # 统一任务领域，不等同于 Celery
├── shared/
│   ├── domain/                      # 极少量稳定值对象和领域事件基类
│   └── application/                 # Clock、ID Generator 等通用协议
├── ports/                           # 迁移期保留的跨业务稳定端口
├── integrations/
│   ├── anythingllm/                 # 已有 Transport/Gateway/Factory
│   ├── callback/
│   └── translation/
├── infrastructure/
│   ├── database/                    # Engine、Session、Alembic 和通用事务设施
│   ├── messaging/                   # Outbox、RabbitMQ、Celery、Redis
│   ├── storage/                     # 本地/MinIO
│   └── observability/               # 日志、指标和 Trace
├── adapters/
│   ├── web/
│   │   ├── flask/                   # 过渡入口
│   │   └── fastapi/                 # 目标入口
│   └── workers/
│       └── celery/                  # Celery task 薄适配器
└── bootstrap/                       # 配置、依赖装配和进程生命周期
```

### 4.2 放置规则

- 只服务一个业务的规则、DTO、Port 和 Repository 映射放入该业务模块。
- 数据库连接池、消息连接、日志初始化和对象存储客户端等技术底座放入 infrastructure。
- AnythingLLM 原始 HTTP、路径、字段和错误只允许出现在 `integrations/anythingllm`。
- MHTML/MinerU/OCR/Office 等文件转换生命周期只能由 `modules/document_processing` 拥有；
  Translator 可以调用其 Application/Port，但不得被其反向依赖。
- `shared` 只能保存经过至少两个业务验证且语义完全一致的稳定能力，禁止成为新的 `utils`。
- 模块之间不能直接导入对方 adapters 或数据表实现；通过应用接口、只读查询契约或领域事件协作。
- Web Presenter 属于入站协议边界；业务模块不得返回 Flask Response、FastAPI Response 或 SSE 文本。
- Celery task 只反序列化内部 ID、调用应用服务并分类异常，不复制业务流程。

### 4.3 迁移期映射

| 当前路径 | 目标归属 | 迁移说明 |
| --- | --- | --- |
| `app/services/llm_service/analysis_service.py` | `modules/analysis` | 先抽编排和纯规则，不重写已稳定的 AnythingLLM Gateway |
| `app/services/llm_service/report_service.py` | `modules/report` | 拆提交、执行、领域格式和端口 |
| `app/services/llm_service/weaponry_service.py` | `modules/weaponry` | 按检索、字段、表格、来源和执行器分步拆分 |
| `app/services/llm_service/task_service.py` | `modules/tasks` | 先建立端口和 Facade，再分离命令、查询、审计和回调记录 |
| `app/services/chat/` | `modules/chat` 目标边界 | 暂不机械移动；后续只在独立阶段调整物理路径 |
| `app/services/translator/document_handler.py`、`mhtml_handler.py`、`mhtml2pdf.py`、`MinerUConverter.py` | `modules/document_processing` + Translator 兼容 Facade | 1H 先迁转换与 Artifact 生命周期，再让 Translator 只编排语言转换和双语输出 |
| `app/services/core/progress_hub.py` | Progress 基础设施适配器 | 通过端口使用，阶段 7 替换为 MySQL + Redis 通知 |
| `app/services/utils/*` | 所属业务模块或 integrations/infrastructure | 逐个证明归属，禁止整体复制到新 `shared/utils` |
| `app/blueprints/llm.py` | `adapters/web/flask` | 按路由逐项变薄；迁移期可保留兼容导入 |
| `app/container.py` | `bootstrap` | 组合根从 Flask `current_app` 读取方式中解耦 |

---

## 5. 模块与共享端口边界

### 5.1 tasks 模块

tasks 是跨业务共享的稳定领域，而不是 Celery 包装层。建议包含：

- `TaskCommandService`：创建任务、保存不可变输入、请求取消、推进受控状态。
- `TaskReadService`：按现有业务键读取兼容任务投影，不产生副作用。
- `RecoverXxxCallbackSynchronously`：组合业务 Recovery Source 与 Callback Guard Port，保留请求内同步副作用；Web 层不直接持有 HTTP/Repository 细节。
- `RequestCallbackRecoveryService`：保留 1B 已完成的可靠命令边界，供后续后台 Outbox 兜底，不替换同步入口。
- `TaskRepository`、`TaskInputRepository`、`TaskEventRepository`。
- `TaskDispatcher`：只接受内部任务 ID 和任务类型。
- `TaskStatusPresenter`：按 file/report/weaponry 的既有公开值分别映射。
- Interaction Audit 与 Callback Delivery 采用独立 Repository/Service，不继续堆入一个 God Service。

### 5.2 进度能力

进度拆分为三个方向：

- `ProgressPublisher`：业务执行器发布内部进度事件。
- `ProgressQuery`：查询数据库中的最新快照。
- `ProgressSubscriber`：WebSocket/SSE 获得跨实例唤醒。

当前 `LLMProgressHub` 仅实现单实例版本。后续 MySQL 保存事实，Redis 只发通知。

### 5.3 回调能力

- 业务模块只构造领域结果，Callback Presenter 负责保持现有回调载荷。
- `CallbackDeliveryPort` 负责发送，不允许业务模块直接调用 `requests.post`。
- 回调 task ID、attempt、触发来源、投递阶段、内部 outcome、结果摘要和下次重试时间持久化。
- 接收方幂等和结果查询能力无法确认：仅明确未送达错误允许有限自动重试；发送后超时/中断标记 `delivery_outcome_unknown`，非 2xx 进入人工/死信处理，二者均不由 Worker 自动重试。
- `/llm/check-task` 继续作为显式同步补发入口；每次实际外发与后台 Worker attempt 都独立审计触发来源，但必须竞争同一 Guard。
- 不建设无 Guard 的有界/无界同步并行、本地线程或内存队列。阶段 4～6 完成 Outbox + RabbitMQ + callback Worker 后，两条触发路径长期并存但只有一个发送权。

### 5.4 文件、预处理和产物

- `SourceFilePort`：从现有 URL 或路径取得任务输入。
- `DocumentPreprocessorPort`：由独立 `modules/document_processing` 提供 OCR、MinerU、MHTML、
  Office/PDF/TXT/MD 处理；返回不可变 ArtifactRef/Profile/Lineage，不返回任意宿主路径。
- `ArtifactStorePort`：报告和中间产物的提交、查询与清理。
- 所有方法使用任务作用域路径/对象引用，不返回打开的句柄或进程私有临时对象。
- 当前本地文件实现属于阶段 1～2 Adapter；阶段 3 只把 ArtifactStore 切换为 MinIO。详细分波次
  见 `260718-阶段1H共享文档处理模块文件级实施设计.md`。

### 5.5 Translation 与 RAG

- `TranslationPort` 接受显式请求和任务级 Progress Reporter，不使用全局可变回调。
- 已完成的 `DocumentRagPort`、`KnowledgeIndexPort`、`ChatConversationPort` 和 AnythingLLM Gateway 继续复用。
- report 和 weaponry 如需要不同语义，应定义业务拥有的 Port，不把供应商响应 DTO 直接传入领域层。

### 5.6 禁止的伪抽象

不得新增以下类型的通用入口：

- `GenericLLMService.run(payload)`。
- 接受任意 `dict` 并按 `businessType` 分支的应用服务。
- 同时承担任务、审计、回调、进度和队列的超级 Repository。
- 所有模块共同依赖的 `shared/utils.py`。
- 为“少写几个类”而让领域层直接导入 AnythingLLM、SQLAlchemy、Celery 或 Web 框架。

---

## 6. 接口到应用用例的目标映射

| 接口/入口 | 入站用例 | 后台/领域用例 | 公开 Presenter |
| --- | --- | --- | --- |
| `/llm/analysis` | `SubmitAnalysisTask` | `RunAnalysisTask(task_id)` | 成功 202 空响应；错误/Callback Presenter |
| `/llm/generate-report` | `SubmitReportTask` | `RunReportTask(task_id)` | 成功 202 空响应；活动任务 409；错误/Callback Presenter |
| `/llm/weaponry` | `SubmitWeaponryTask` | `RunWeaponryTask(task_id)` | 成功 202 空响应；错误/Callback Presenter |
| `/llm/check-task` | `RecoverXxxCallbackSynchronously` | 请求内同步尝试一次；后台 Outbox/Worker 兜底；两者共用 latest-wins Guard，不创建新的业务解析 execution | 保持既有成功/400/404 Presenter；不新增内部 outcome 字段 |
| `/llm/progress` | `SubscribeTaskProgress` | Progress Publisher/Snapshot | 无 action 的既有进度消息 Presenter |
| `/llm/reassign` | `ReassignDocument` | 同步 Saga，不进入异步任务队列 | Reassign Response Presenter |
| `/llm/chat*` | 现有 Chat Application Services | 现有 Chat Run/Cleanup Executor | 现有 Chat SSE/JSON Presenter |
| Debug | Debug Query Services | 无生产任务 | HTML/JSON Debug Presenter |

异步业务统一使用以下边界：

```text
Flask/FastAPI Adapter
  → SubmitXxxTask
      → 持久化任务和输入快照
      → TaskDispatcher.dispatch(task_id)
  → 按现有接口立即返回

Local Executor/Celery Adapter
  → RunXxxTask(task_id)
      → 重新读取输入和执行权
      → 调用领域规则与外部 Port
      → 持久化进度、结果、产物和回调事实
```

`/llm/reassign` 不采用上述异步返回方式。它必须在当前请求内完成同步 Saga，并按既有接口返回。

---

## 7. 阶段 1 的实施波次

1A～1B 已由 `260715-阶段1A-1B文件级实施设计.md` 细化到文件和测试级。1C 已由
`260716-阶段1C报告生成文件级实施设计.md` 细化；1D～1G 继续在开工前按同一模板补充
独立 L3，并完成到期决策、调用链、端口、失败矩阵、日志指标、契约测试、回滚和完成门禁
评审。这样既保持总路线稳定，也允许后续设计吸收前一波次的实测结果。

### 波次 1A：全接口契约基线与模块骨架

> 当前进度：**波次 1A 已于 2026-07-16 完成**。阶段 0 黄金资产继续冻结全部正式接口的已确认目标；1A-1 对首个 check-task/Progress 切片补齐当前与目标分层断言，1A-2 建立模块骨架和架构门禁，1A-3 实现框架无关 DTO/Port/应用服务及 Fake 契约。其余接口仍须在各自波次开工前按同一模板补齐文件级当前实现基线，不能把“1A 完成”解释为生产路由已经迁移。执行证据见 `../更新记录/260715-阶段1A-1接口契约基线执行记录.md`、`../更新记录/260715-阶段1A-2模块骨架与架构边界执行记录.md` 与 `../更新记录/260716-阶段1A-3内部任务契约执行记录.md`。

**工作内容**

- 为 analysis、report、weaponry、check-task、progress、reassign 分别建立“当前实现基线”和“已批准目标”请求、响应、状态码、回调及 WebSocket 黄金样例。
- 创建模块边界和最小包结构，不搬迁业务实现。
- 建立领域错误到现有 HTTP 错误的集中映射。
- 建立架构导入测试，禁止 modules/domain/application 导入 Flask、FastAPI、Celery 和具体数据库。
- 明确模块数据所有权、业务键和内部任务 ID 的关系。

**门禁**

- [x] 只新增内部类型、Port、应用服务、测试和文档时，当前实现基线继续通过；待实施的目标黄金样例没有被误报为当前行为。
- [x] 本波次未改变运行路径；只宣称已具备可独立测试的内部契约，不宣称接口已迁移或具备共享存储、可靠队列、跨实例能力。

### 波次 1B：任务状态检查命令边界与进度控制面

> 当前进度：**1B-1 与 1B-2 均已于 2026-07-16 完成**。1B-1 已实现批量原子
> `CallbackRecoveryCommandPort`、`RequestCallbackRecoveryService`、框架无关 Presenter
> 和 Fake 契约；该可靠命令边界现作为未来后台兜底资产。1B-2 已将 `/llm/progress` 接入类型化应用服务、
> 线程安全单实例 Adapter、连接级有界缓冲和单写入路由。仍未实现共享数据库、Outbox、
> RabbitMQ、Worker 或 Redis 跨实例通知。执行证据见
> `../更新记录/260716-阶段1B-1可靠恢复命令边界执行记录.md` 与
> `../更新记录/260716-阶段1B-2Progress控制面迁移执行记录.md`。

**工作内容**

- 保留 1A 已完成的 Task DTO、Task Read 和应用服务资产；1B 的 `CallbackRecoveryCommandPort`/`RequestCallbackRecovery` 继续验证后台命令 eligible、去重、stale、失败和批量顺序。同步网络 Adapter 由各业务波次基于共享 Guard 实现，不回填到 tasks 通用模块。
- check-task/Progress 的 `params` 任一元素不是对象时拒绝整次请求/消息，不过滤后部分处理。
- 以内部 execution ID 定义未来 message/Outbox 契约和 latest-wins 门禁，防止旧执行轮次覆盖或补发新执行轮次结果；实际 Guard、delivery 审计和 Worker 在阶段 4～6 实现。
- 从 `/llm/progress` 提取无 action 订阅解析、快照查询、订阅端口和 WebSocket Presenter，并删除显式 subscribe/query/unsubscribe 与 ack 的公开处理分支；收到 action 时返回 error 消息并保持连接。
- Task Read 可用只读 Facade 适配当前 `LLMTaskService`；Progress 继续适配当前 `LLMProgressHub`。报告类型 check-task 已在 1C 切到业务同步恢复用例，其他业务随垂直切片迁移。

**门禁**

- [x] 单项/批量任务状态检查的可靠命令登记已形成框架无关契约和 Fake 测试；1B 不宣称 HTTP 200 异步语义已进入生产。生产切换门禁移至阶段 6，现有 400/404 错误必须保持。
- [x] WebSocket 只接受不含 action 的订阅；显式 action 或非对象 params 元素使整条消息失败，返回 error 消息并保持连接，不输出 ack；初始快照、后续进度和连接关闭清理保持。
- [x] `RequestCallbackRecovery` 可在无 Flask/request context 下测试，Task Read 与恢复命令的创建、活动命令复用、无需恢复、过期跳过和持久化失败路径均可独立验证。
- [x] 单实例 Progress 发布与订阅共享同一 Hub；任务线程只向有界缓冲投递，WebSocket 只由连接路由线程写入，50 键并发与资源释放竞争已有离线测试。

### 波次 1C：报告生成垂直切片

> 文件级实施以 `260716-阶段1C报告生成文件级实施设计.md` 为准。该设计已经纳入
> 无业务积压数量上限、执行时下载源文件、同步 Repository 目标、callback Guard、
> 数值保留期延期和当前不建设 URL Policy 等 2026-07-16 决策。1C-0～1C-7 已完成并关闭；
> 当前开发分支已完成 Dispatcher、组合根与公开路由切换、遗留 Worker AST 隔离和阶段输入
> 清单，尚未部署生产。

**工作内容**

- 建立 `SubmitReportTask`、`RunReportTask`、Report DTO 和领域错误。
- 在提交事务中原子检查同一 `reportId` 的活动任务；命中时返回既有 `{"error":"任务正在处理中"}` 和 HTTP 409，不创建新任务、不发布进度、不启动执行器。
- 抽离模板下载、Word 文本提取、多文档 RAG、HTML 规范化、产物和回调端口。
- 把线程启动收口到 `TaskDispatcher` 兼容适配器。
- 待处理任务必须持久化；兼容 Dispatcher 只保留常量空间的有界唤醒并扫描数据库，
  不因正常积压返回 503，也不使用无界等待线程/内存任务队列。
- 报告源文件在 Worker 执行时下载；202 不表示文件字节已经固定。
- 使用 callback Guard 串行化同业务键提交/外发；`delivery_outcome_unknown` 冻结新提交并
  返回既有 409，直至内部人工解除。
- 保留当前任务状态、进度和回调载荷。
- 受理成功返回 HTTP 202 空响应体；错误响应保持目标接口文档定义。

**门禁**

- 报告应用服务不再创建旧 `AnythingLLMClient` 或读取 Flask 配置。
- 执行器只通过 task ID 恢复输入。
- Fake RAG/File/Callback 下可完成成功、失败和清理测试。
- 并发提交相同 `reportId` 时最多一个请求受理为 202，其余请求返回 409；数据库唯一性/条件写负责消除“先查后写”竞态。
- 执行快照使用追加式 execution 事实，所有公开投影、进度、结果和 callback 写入携带
  expected execution ID；旧 execution 不得覆盖新任务。
- 正常积压只增加持久化任务事实，不增加等待线程或无界 Python Queue 元素。

### 波次 1D：武器谱垂直切片

> 文件级实施以 `260717-阶段1D武器谱文件级实施设计.md` 为准。空 `filePathList` 受理时冻结、
> architectureId 64 位正整数规范化、`status` 可选忽略与非法字段结构 400、删除模式 1、旧
> callback 与新任务冲突时 409 五项口径已于 2026-07-17 确认并同步接口文档。1D-1～1D-5
> 不切换公开路由；Callback Guard、资源恢复和 Dispatcher 完成后，统一在 1D-6 切换 202 空体，
> 避免中间状态进入实际运行链路。
> 第三轮复盘已把 A/B 文件 `content` 串扰修复和确定性证据筛选纳入 1D：检索 Query 与抽取
> Prompt 分离，Retrieval Candidate 必须经过冻结的 score-or-rank Evidence Selection。当前批准
> 策略不设置绝对相关性阈值、不使用独立 reranker；合法分数参与稳定排名，供应商明确无分数时
> 使用稳定正整数 rank，协议损坏的批次不进入 rows，零 Selected Evidence 时不调用模型；来源级
> 模型调用只使用最终回调 `rows` 对应 Evidence 和无历史上下文，禁止回退共享父 Thread 或执行
> 任何目标文档二次 RAG；非空回答必须通过 Evidence/上下文校验。以上均为内部正确性修复，
> 不改变接口参数。
>
> 1D-0R 已纠正旧导弹负例标签；纠正后，农业负例分数仍高于部分正例，原始 score 直接门禁继续
> 被拒绝。当前已落地专用 Retrieval Query、MHTML/Chunk 去噪、额外信号 Selection 纯规则和
> fail-fast 只读复校准，并已使用随机临时资源完成历史清洗副本的真实索引重建、复校准与清理。
> 该输入不是可追溯原始 MHTML；此事实继续作为历史风险证据，但完整 Candidate 标注、独立
> reranker、绝对阈值和正式精度指标已经取消。测试夹具 `0.82`、旧 reranker 分支 `0.8` 与
> workspace 默认 `0.25` 均不得进入生产快照。原始文件处理与 Translator 解耦迁入平级 1H，
> 不再阻塞 1D-3B；Schema v2、最小多文档结构黄金样例和生产 Adapter 离线实现已经完成，
> 真实 score/rank、来源身份和空 workspace 模型行为原计划作为 1D-6 源码切流前门禁；当前开发
> 分支已经完成源码切换但尚未部署，1D-7 也已完成开发分支离线关闭，因此该项保留为实际环境
> 启用前 production attestation 门禁，不阻塞后续阶段代码开发。
>
> 1D-1 已完成四层骨架、不可变领域 DTO、来源/TABLE/回调纯规则、Candidate/Selected Evidence/
> Prompt 类型隔离及模式 1 删除；遗留模式 2 只通过兼容转发复用新规则，生产路由未切换。
>
> 1D-2 已完成未绑定路由的请求/响应适配、Document Scope、execution 完整策略快照和当时的
> Schema v1 Codec；该内容只作为历史实施事实。1D-3B 已按“无历史数据、无旧 Worker”口径
> 直接删除 v1 Encoder/Decoder、版本常量、专属策略字段和夹具，以 v2 作为唯一版本，未建设
> 双读、迁移或兼容发布顺序。1D-3A
> 已完成 Retrieval/Extraction/Auxiliary/Translation/Audit/Callback/
> Resource/Dispatcher 供应商无关 Port 与严格 Fake，并以 50 线程证明 scope/session/call/resource
> 结构隔离。1D-3B 已补齐真实 Adapter 代码和 Fake Transport 验证；1D-4 已在这些抽象上完成
> Submit/Run/字段 Application，并验证 50 个共享 Port 在途任务隔离和慢检索期间 SQLite 可写，
> 全面审查又补齐 Query 无隐藏限制、Audit 三态、cleanup intent 先行、终态 tracking 恢复和
> 409 unknown；1D-5 已完成通用 Dispatcher、严格配置和离线组合根；1D-6 已完成 Callback Guard、
> 同步 check-task 恢复、资源有界恢复、生产组合根和公开 202 空体薄路由；1D-7 已完成永久门禁、
> I01～I07 证据、遗留清单和离线关闭。不得把 Fake/临时 SQLite 结果解释为真实 AnythingLLM 质量
> 或生产并发结论。后续阶段可以继续开发；服务可用且四类指纹明确后，仍须补齐真实供应商协议、
> 来源身份和 Provided-Evidence 模型行为 attestation，校验通过前不得标记为 production ready。

**工作内容**

- 先拆纯函数和领域规则：字段定义、表格列、响应解析、行合并、来源去重、精炼 Retrieval Query、
  单文档 Extraction Prompt 和公开载荷映射；两类对象在类型层不可互换。
- 在 Retrieval 与 Extraction 之间增加纯 `select_evidence`：Candidate/Selected Evidence 类型不可
  混用，按冻结 profile 完成来源、score/rank 协议、正文质量、同文档精确去重和稳定排序；不
  设置绝对阈值或 reranker。8/16 只表示供应商 Candidate 批次。所有通过门禁的正文完整进入
  Selected Evidence，不设置单条、总量或单文档数量/字符配额。
- 再拆 Retrieval Application Service、选中文档快照和可拔除的 AuxiliaryGuidance Port；术语
  规则只作为一个 Adapter，关闭时不得发生术语目录、workspace、embedding 或检索 I/O。
- `fieldDescription` 保持 INPUT/TABLE 目标检索与最终抽取两阶段生效，术语辅助不得覆盖它。
- 每个来源级调用使用全新无历史上下文；Thread/Conversation 创建失败不得回退父会话。最终
  Extraction 不访问任务/分类文档 workspace，实际 Evidence 必须与 `analyseDataSource.rows`
  逐项一致；额外、缺失或越界上下文使整次回答作废。
- Schema v2 Selection profile 必须携带 AnythingLLM/embedding、Query、文档处理指纹、score/rank
  协议和稳定排序并写入 execution 快照；非法分数不得伪装成缺失或静默转 0，rank-only 必须有
  稳定正整数 rank。旧 execution 重试不得读取新的运行时默认值。
- 删除已废弃的模式 1，只保留按来源文件聚合多个 Chunk 的单一提取策略。
- 复用/扩展供应商无关 Retrieval/Knowledge Port，消除业务层直接 Client、Session、URL 和配置访问。
- 建立 `SubmitWeaponryTask` 与 `RunWeaponryTask`。
- 新执行把字段模板和文档范围写入 execution 级不可变输入，不再依赖按业务键替换的旧选文快照。
- 任务执行不再临时删改来源类别永久 workspace；任务级 workspace/thread 进入可恢复资源记录。
- weaponry check-task 同步恢复与正常 Worker 共用 latest-wins、lease/fencing 和 Callback Guard。
- 受理成功返回 HTTP 202 空响应体；错误响应与回调载荷不变。

**门禁**

- 每次只迁移一类字段或一个执行步骤，禁止一次性重写整个大文件。
- INPUT、TABLE、来源映射、选中文档和回调与模式 2 黄金样例一致；模式 1 不可再由配置选择。
- 术语辅助关闭路径外部术语调用为零；将来删除术语 Adapter 不修改核心任务或公开接口。
- 重复执行不会复用其他任务的选中文档、Translation 回调或临时资源。
- 公开路由切换前必须完成 50 同键原子受理、50 回调唯一发送、资源恢复和 Dispatcher 生命周期验收。

### 波次 1E：分类节点变更同步 Saga

**工作内容**

- 建立 `DocumentReassignmentService` 和清晰步骤：校验本地记录、准备目标集合、执行知识索引操作、提交本地映射、清理旧资源。
- 每一步记录幂等键、外部引用和补偿状态。
- 通过 KnowledgeIndex Port 和 Repository/Unit of Work 执行，不在路由直接创建 Client。
- AnythingLLM 删除/挂载返回 `false`、目标 workspace 创建失败或本地条件更新 rowcount
  不为 1 均按业务失败处理，执行补偿并返回已确认的既有 HTTP 500 结构。

**门禁**

- 对外仍为同步接口，不新增任务 ID 或异步状态。
- 成功、记录不存在、分类不一致、旧 ID 转换失败和外部失败响应与已批准目标契约一致；
  不再把“外部调用未抛异常但返回 false”误判为成功。
- 任一补偿无法完成时保留明确审计和恢复入口，不能伪装成功。
- 1E-7 已完成：观察、检查点收敛、补偿和终态提交的实际逻辑已从大型恢复 Facade 下沉到独立协作器，
  callback wrapper 不再存在。可靠队列或多实例恢复接入必须复用该唯一 Facade/协作器链路，并继续满足
  lease/fencing、短事务外网络 I/O、供应商侧幂等/串行化和容量验收门禁。

### 波次 1F：analysis 二次高内聚收口

**开工前置**

- PR #79 已经通过审核并实际合入 `main`；以合入后的 `main` 提交重新冻结源码、接口文档和测试基线。
- 先形成 1F 的文件级实施设计：列出 `llm_analysis` 路由、`analysis_service`、任务/进度/回调/
  文件预处理/Translation 依赖的当前调用图、待迁移边界、删除条件和负向架构测试；未完成设计不得
  直接拆分大型服务文件。
- `docs/接口文档/` 仍是唯一公开契约依据。1F 默认只重构内部结构；任何可能改变请求参数、响应/
  Callback/SSE/WebSocket 字段、Header 或状态码的需求，必须暂停并先取得确认。
- 迁移期间继续明确 Analysis 仍是单实例兼容的迁移期批量后台线程；不能把本波次表述为可靠队列、
  多实例任务恢复或生产吞吐验收。

**工作内容**

- 保留已经完成的 Document RAG Gateway、审计和知识索引边界。
- 将纯解析/匹配/映射规则移入 analysis/domain。
- 将任务状态、进度、文件预处理、Translation、Callback 和资源清理改为显式端口。
- 建立 `SubmitAnalysisTask` 与 `RunAnalysisTask`，保持当前批量和单文件行为。
- `/llm/analysis` 受理必须通过持久 Repository 的 `create_if_allowed` 在同一事务内完成
  “同 `fileName` 是否允许新执行”的判定与 execution 创建；提交后只唤醒 Dispatcher，
  路由和业务函数均不得创建 daemon/thread。
- file 回调必须复用通用 Callback Guard：外发前以 `fileName + expected TaskId` 复核
  latest；旧执行判定为 stale/skipped 且网络调用次数为零，未知投递继续冻结同键。
- 单文件或批量成功受理均返回 HTTP 202 空响应体；请求顺序、错误响应与回调载荷不变。

**门禁**

- 不重新解释已经稳定的 architecture/data-standard 业务规则。
- 既有离线 Fake、RAG trace、回调和任务状态测试全部通过。
- 删除全局可变 Progress Callback 和任务间共享的非线程安全对象。
- 50 个同 `fileName` 并发受理只能产生一个 accepted；不得因正常积压返回 503，也不得
  在进程重启后依赖内存线程列表恢复任务。

### 波次 1H：共享文档处理与 Translator 解耦

> 文件级实施以 `260718-阶段1H共享文档处理模块文件级实施设计.md` 为准。1H 与 1D 平级，
> 可以和 1D/1E/1F 后续波次并行；1G 的最终旧路径删除必须等待 1H-5 引用证据。

**工作内容**

- 建立 `modules/document_processing` 的 Domain/Application/Ports/Adapters，统一 MHTML、MinerU、
  OCR、Office/PDF/TXT/MD 和 Artifact/lineage。
- 将处理 Request、Result、Profile、ArtifactRef、SHA-256 和父子谱系变为不可变 DTO。
- 每个 task/step 使用独立目录和幂等 Artifact key；重型 MinerU/OCR 受共享许可限制，正常积压
  保留在任务事实中。
- 先迁 `mhtml_normalizer.py`、`ocr_preprocessor.py`，再迁 report、analysis/RAG 和 Translation
  调用点；兼容期 `DocumentTranslator` 单向调用新模块后再执行语言转换。
- 本地 Artifact Adapter 保持当前环境；阶段 3 只替换 MinIO Adapter。

**门禁**

- DocumentProcessing 不导入 Translator，业务 Application 不导入具体 Converter。
- 预处理路径 Translation 调用次数严格为零；50 个 task 目录、状态、Artifact 和进度不串扰。
- `legacy-normalized-artifact-v1` 保留来源、哈希和处理指纹，不能标记为原始 MHTML。
- 新旧实现不能双执行；公开接口、回调和 Progress 参数零变化。

### 波次 1G：Debug、组合根与遗留路径清理

> 文件级实施以 `260801-阶段1G组合根Debug与遗留清理文件级实施设计.md` 为准。该设计按照
> 1G-0～1G-6 分离契约/引用基线、Debug 边界、框架无关组合根、正式蓝图审查、遗留测试迁移、
> 条件物理删除和阶段关闭；引用清零、仓库外调用、运行配置或回滚观察期任一未确认时不得删除。

**工作内容**

- Debug 路由只调用 Query Service 或专用 Debug Adapter。
- 把依赖创建从 Flask `current_app` 访问方式中分离，形成框架无关 Bootstrap。
- 蓝图只保留协议解析、用例调用和 Presenter。
- 静态核对 `llm_service`、旧 `AnythingLLMClient` 和 `utils` 的剩余调用。
- 静态核对 Translator 旧文件处理器和 Converter；1H-5 的生产、测试、配置三类引用未清零前
  不得删除。

**门禁**

- 所有正式接口和 Debug 接口回归通过。
- Web 路由不创建生产后台线程、不直接访问 SQLite、不直接编排 AnythingLLM。
- 删除旧路径前有静态搜索、测试和运行配置三类证据。

---

## 8. 单个用例的标准迁移步骤

每个路由或后台用例必须按以下顺序执行：

1. **冻结契约**：记录请求、响应、状态码、回调和日志中的必要业务语义。
2. **建立 DTO**：把 Web `dict` 转换为框架无关、不可变或受校验的 Command/Query DTO。
3. **提取纯规则**：先移动无 I/O 的领域规则并补单元测试。
4. **定义 Port**：由业务模块表达需要什么能力，不由基础设施反向决定接口。
5. **实现兼容 Adapter**：包装当前 SQLite、Hub、Client 或工具，避免同时重写所有技术实现。
6. **建立应用用例**：显式声明事务边界、任务状态和补偿策略。
7. **切换组合根**：只切换当前路由，不影响其他业务；check-task 的每个业务分支可在对应
   垂直切片接入共享 Guard 的同步恢复，但已批准空响应/严格校验目标必须另设契约波次；
   阶段 6 只切换后台可靠 Adapter，不删除同步副作用。
8. **差异验证**：新旧实现使用相同输入和 Fake，比较公开输出和副作用。
9. **稳定后清理**：确认无调用方后删除旧编排；不保留长期双写。

若任一步发现除 2026-07-15 已确认定向调整之外还必须修改公开参数或接口文档，立即停止，不得通过兼容猜测继续实现。

---

## 9. 与总计划后续阶段的衔接

| 总计划阶段 | 本计划提供的前置成果 | 后续阶段只做什么 |
| --- | --- | --- |
| 阶段 2：统一任务语义 | Submit/Run 分离、Task/Progress/Callback Port、任务级 DTO | 增加 task/attempt/step/event、租约、fencing、本地隔离 |
| 阶段 3：MySQL | Repository 和 Unit of Work 边界 | 替换 SQLite Adapter、执行 Alembic 空库初始化和测试种子验证，不迁移 SQLite 历史测试数据 |
| 阶段 4：Outbox | check-task 同步 Guard 与后台 recovery command 均有明确边界 | 建立 delivery/recovery/Outbox 表，让终态后台兜底与同步入口共享 expected TaskId 和 Guard |
| 阶段 5：RabbitMQ/Celery | Worker-ready 的 `RunXxxTask(task_id)` 与 callback recovery command | 实现 callback 队列、消息契约、Worker、ACK/DLQ、目标级限流和 Celery 薄适配器 |
| 阶段 6：任务切换 | 业务逻辑已经完成分层 | 切换后台任务 Dispatcher 与 callback Worker；check-task 同步入口继续存在，只替换其 MySQL Guard/Recovery Source Adapter |
| 阶段 7：跨实例通知 | Progress Port 与 Presenter 已稳定 | 用 MySQL + Redis 实现，不改 WebSocket 公开协议 |
| 阶段 8：FastAPI | Web 无关的 Command/Query/Presenter | 用 FastAPI Adapter 替换 Flask Adapter |
| 阶段 9：chat 多实例 | chat 保持独立领域状态机 | 复用统一任务、Outbox、队列、存储和通知基础设施 |

阶段 1 与阶段 6 的职责不得混淆：阶段 1 是**业务代码结构迁移**；阶段 6 是**可靠执行适配器切换**。若阶段 6 仍需从蓝图中提取业务流程，说明阶段 1 尚未完成。

---

## 10. 接口兼容与停止条件

### 10.1 必须保持

- 所有路径、方法、请求参数、参数位置、类型、必填性和默认行为。
- 除已批准的成功空响应体、report 409、严格 `params` 元素校验和 Progress 显式 action 错误后保持连接/无 ack 外，保持现有 HTTP 状态码、JSON 字段、字段类型和错误语义。
- file/report/weaponry 各自已有的公开任务状态值。
- `/llm/check-task` 的请求内同步恢复副作用、成功响应及 400/404 错误；后台队列不得改变返回时点或增加公开字段。
- `/llm/progress` 无 action 的请求、进度响应、订阅和关闭语义；显式 action/非对象 params 返回 error 后保持连接，不输出 ack。
- `/llm/reassign` 同步完成/失败语义。
- 所有既有回调字段与数据来源结构。
- `/llm/chat*` 已冻结的 JSON、SSE、状态码和 `chatId` 正整数规则。

### 10.2 必须立即停止并确认

- 需要新增、删除、重命名或改变任一前后端接口参数。
- 需要改变公开状态值、状态码、错误体、回调字段或 WebSocket/SSE 消息。
- 现有接口无法表达队列过载、结果未知或迁移失败。
- 同步 check-task 与后台 Worker 无法共用同一 MySQL Guard，或实现被迫引入第二套发送权、本地线程池/进程内队列。
- 发现旧数据无法无损映射，或必须清空非开发环境数据。
- `/llm/reassign` 可靠实现无法保持同步契约。
- 模块间数据所有权存在冲突，必须跨模块直接写表。

接口文档已经明确 check-task 可能在返回前同步尝试回调。2026-07-17 的基础设施修订与该契约一致，本轮不修改接口参数或响应结构；未来若要删除同步时点，必须重新取得甲方确认并同步接口文档。

---

## 11. 测试、日志与代码质量

### 11.1 测试要求

- Domain：纯规则、状态转换、来源映射、表格合并和补偿决策。
- Application：使用 Fake Port 验证编排、事务、错误分类和幂等。
- Adapter：SQLite/未来 MySQL Repository 契约，AnythingLLM/Callback/File Transport 映射。
- Web：路径、参数、状态码、JSON、WebSocket 和回调黄金样例。
- Architecture：禁止 domain/application 导入 Web、Celery、SQLAlchemy 实现和旧 Client。
- Concurrency：至少 50 个 Fake 任务验证进度、Translation、临时目录和结果无串扰。

当前可使用的离线回归命令：

```bash
/venv/bin/python -B -m unittest \
  tests.test_routes \
  tests.test_progress_and_check_task \
  tests.test_task_service \
  tests.test_report_service \
  tests.test_weaponry_service \
  tests.test_translation_service \
  tests.test_dependency_container -q

/venv/bin/python -B -m unittest discover -s tests -p "test_chat*.py" -q

git diff --check
```

若当前开发环境对 `/venv` 使用不同路径映射，应先确认解释器位置，不能改用来源不明的全局 Python。不得用可能触发 `run.py` 或真实后台服务的全量命令替代定向离线测试。

### 11.2 注释要求

- 领域规则注释解释业务原因、约束和边界，不复述代码。
- 事务、租约、幂等、重试、补偿和状态映射必须有通俗中文注释。
- Port 说明调用方保证、实现方保证、错误分类和线程/任务安全要求。
- 过渡 Adapter 明确其能力限制以及未来替换阶段，禁止把单实例能力描述成可靠队列。

### 11.3 日志要求

关键日志至少包含 `trace_id`、内部 `task_id`、业务键、任务类型、attempt、step 和实例 ID。必须记录状态变化、外部调用耗时、重试/补偿决策和最终结果；不得记录密钥、Authorization 或敏感正文。

---

## 12. 风险与控制

| 风险 | 控制措施 |
| --- | --- |
| 一次性移动大文件造成行为回归 | 先契约测试，再按纯规则、Port、用例、Adapter 小步迁移 |
| 把 shared 变成新杂物目录 | 共享能力必须至少两个业务语义一致，并通过架构评审 |
| 抽象过早导致通用 `dict` 服务 | DTO 和 Port 由具体业务语义驱动，禁止 businessType 大分支 |
| 同步 check-task 与后台队列形成双发送 | 两条路径共用同一业务 Guard/lease/fencing；业务波次实现同步用例，阶段 4～6 只增加后台触发 Adapter |
| 旧/新执行器重复处理 | 每次只切一个路由/任务类型，单一 Dispatcher 拥有执行权 |
| Translation/进度全局状态串扰 | 任务作用域依赖与 Progress Reporter，并发 Fake 测试 |
| 文档转换继续寄生在 Translator 或 utils | 独立 1H 模块、单向依赖、兼容 Facade 和永久 AST 导入门禁 |
| reassign 外部写成功、本地写失败 | 持久化 Saga、幂等步骤、结果探测和补偿审计 |
| 模块化单体退化为跨模块直接写表 | Repository 数据所有权、导入边界测试和代码评审门禁 |
| 物理目录整理扩大 diff | 先改依赖方向，最后单独做机械移动并保持测试通过 |

---

## 13. 完成定义

本专项计划完成必须同时满足：

- analysis、report、weaponry、reassign、tasks、chat 和 document_processing 具有清晰模块边界。
- `/llm/check-task` 已形成 Task Read、业务同步 Recovery 与后台 Recovery Command 边界；报告类型已迁移，其他类型随业务波次迁移；`/llm/progress` 通过 Progress Port 工作。
- Flask 蓝图不直接创建旧 AnythingLLM Client、不访问 SQLite、不编排后台流程。
- report、weaponry、analysis 均具备 Submit/Run 分离，执行入口只接收持久化任务 ID。
- reassign 通过同步 Application Service 和可补偿 Saga 完成。
- Task、Progress、Callback、File、Artifact、DocumentProcessing、Translation 均有显式 Port 和兼容 Adapter。
- Domain/Application 可在无 Flask context、无真实 AnythingLLM 的环境中测试。
- 模块间不直接导入对方 Adapter 或跨模块写表。
- 已确认的成功空响应体、report 409、严格 `params` 元素校验和 Progress action 错误后保持连接/无 ack 均已在对应波次实现并通过契约测试；其余正式接口、回调、WebSocket 和 chat 契约无变化，且未增删任何请求参数。
- 旧 `llm_service`、`utils`、Translator 文件转换器和 Blueprint 遗留编排均有明确归属；无调用路径后再删除。
- 每个波次均有修改文件、验证结果、回滚点和剩余风险记录。

---

## 14. 推荐首个实施切片

首个切片建议执行波次 1A + 1B，而不是立即拆 `weaponry_service.py`：

1. 固化其余正式接口的黑盒契约。
2. 建立 modules/tasks 最小边界和架构导入测试。
3. 为 Task Read 和 `LLMProgressHub` 提供必要 Facade；同步 callback 网络恢复由业务 Port 隔离。
4. 保留 `/llm/check-task` 的框架无关应用边界；可靠 command registration 作为未来后台兜底。
5. 把 `/llm/progress` 的快照和订阅提取为 Port/Presenter。
6. 通过“当前实现基线 + 已批准目标黄金样例”证明差异严格受控，不影响未授权契约。

该切片以控制面边界设计和 Progress 迁移为主，固定 Task Read、后台 Callback Recovery Command 和 Progress 契约。同步 check-task 由 report → weaponry → analysis 等业务垂直切片逐项接入共享 Guard；阶段 4～6 再增加可靠后台兜底，不删除同步入口。

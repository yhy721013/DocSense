# 阶段 1G-6 关闭验收与阶段 2 交接执行记录

> - 执行日期：2026-08-01
> - 执行状态：已完成，阶段 1G 关闭门禁通过
> - 前置门禁：1G-0～1G-5 均已完成；1G-5 四项外部条件已由负责人确认
> - 公开契约：`docs/接口文档/` 全程只读，未增删任何前后端接口参数
> - 验证环境：项目 `venv`、临时 SQLite、Fake/临时目录；未运行 `run.py`，未连接真实后台服务

## 1. 目标与关闭结论

1G-6 不增加业务功能。它把阶段 1 已形成的模块边界、数据库表所有权、遗留处置、回滚点和阶段 2
输入固化为可复核资产，并依次通过文档门禁、九组专项门禁、安全全仓、编译、引用、Git 文本和
接口零差异终检。

关闭结论：阶段 1 的单实例离线结构门禁已通过，可以进入阶段 2 的统一任务与可靠执行建模；本记录
不把 SQLite、本地 Dispatcher、进程内 activity marker/limiter、文件锁或本地 Hub 表述为可靠队列、
分布式 lease/fencing、多实例一致性、生产容量或 exactly-once。

## 2. 分阶段执行结果

### 2.1 1G-6A：关闭事实复核

- 分支为 `feat/weaponry-chat`，HEAD 为 `808f3a109c1e56df32ac7abfde8ef625b846a2dd`；
- 工作树中的修改与连续 1G-0～1G-5 改造一致，没有发现来源未知的重叠修改；
- `docs/接口文档/` 为零差异；
- 引用检查器对 11 组候选均给出 `deletionReady=true`、阻塞引用 0、未知动态导入 0；
- 309 条剩余引用全部属于历史文档、Manifest 定义或永久门禁字符串；
- 现行 Dispatcher、Callback、资源、SQLite 表与遗留 Adapter 的单实例边界与设计一致。

未发现需要修改公开契约、生产数据、部署拓扑或 1G-5 删除结论的新事项，因此进入 1G-6B。

### 2.2 1G-6B：所有权矩阵与永久门禁

新增：

- `docs/重构记录/阶段0资产/260801-阶段1关闭模块所有权与遗留适配矩阵.md`；
- `tests/test_stage1g_closeout.py`。

矩阵覆盖：

1. Web → Application → Port ← Adapter 的当前依赖方向；
2. Analysis、Report、Weaponry、Reassign、Tasks、DocumentProcessing、Translation、Debug、Chat、
   AnythingLLM Integration 和共享兼容持久化的所有权；
3. 生产源码中全部静态 SQLite 表及其权威写入者、读取者、当前一致性边界和退出阶段；
4. 保留的 Legacy/Local Adapter、保留理由、禁止误读和退出条件；
5. 1G-5 十一组删除候选、1G 各波次证据和文件级回滚点；
6. 阶段 2 Task/Attempt/Step/Event、Codec、lease/fencing/checkpoint、Callback/资源、
   DocumentProcessing Profile 与阶段 3/5/6 输入；
7. 数据库、真实供应商、多实例、可靠队列和容量的负责人门禁。

首轮测试准确发现两个资产规则问题：已删除 Translator 目录只剩被忽略的 `__pycache__`，以及静态
SQL 正则把注释中的 `IF` 误判为表名。整改后，门禁只拒绝真实源码/资产回流，并逐名记录 Chat v5
事务内迁移临时表。关闭分组执行 44 项，失败 0、错误 0、跳过 0。

### 2.3 1G-6C：当前说明与索引同步

同步了：

- 根 `README.md` 的当前依赖方向、1G-5 删除状态和模块目录；
- `app/modules/README.md` 的八个现行模块状态；
- `app/modules/debug/README.md` 的部署访问控制边界；
- `app/services/llm_service/README.md` 的共享兼容持久化定位；
- `tests/README.md` 与 `docs/重构记录/阶段0资产/README.md` 的关闭资产索引；
- 重构记录、更新记录索引和 1G 文件级设计的关闭状态。

文档/契约/架构分组共执行 48 项，失败 0、错误 0、跳过 5。五项跳过均因仓库不提供被
`.gitignore` 排除的个人联调夹具，不影响当前说明、契约或架构门禁。

### 2.4 1G-6D：九组关闭验收

九组严格按顺序执行，前一组通过后才进入下一组：

| 序号 | 分组 | 执行 | 失败 | 错误 | 跳过 |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | Debug、Container、Route、Architecture | 136 | 0 | 0 | 0 |
| 2 | Analysis | 322 | 0 | 0 | 0 |
| 3 | Report、Tasks、Callback | 392 | 0 | 0 | 0 |
| 4 | Weaponry | 314 | 0 | 0 | 0 |
| 5 | Reassign | 179 | 0 | 0 | 0 |
| 6 | Chat、Progress | 331 | 0 | 0 | 0 |
| 7 | DocumentProcessing、Translation | 168 | 0 | 0 | 3 |
| 8 | AnythingLLM Integration | 226 | 0 | 0 | 0 |
| 9 | 安全全仓 | 2,105 | 0 | 0 | 3 |

前八组允许交叉覆盖，目的是让故障可按业务定位，不能把合计数解释成唯一测试数。第 7/9 组的三项
跳过为两个 macOS 真实进程组用例和当前 Windows 不允许创建真实符号链接的用例；对应算法仍有
跨平台 Fake/静态测试覆盖。

安全全仓先动态发现 2,118 项，再按完整测试 ID 精确排除 13 项，实际执行 2,105 项：

- 7 项可能启动本地 Shell、测试文件服务、目录脚本或面向本机应用；
- 1 项在 Windows 无法可靠表达 POSIX `0640` 权限位；
- 5 项依赖被 `.gitignore` 排除的个人请求夹具。

测试中的 ERROR/CRITICAL 日志均来自预期故障注入，例如远端写入结果未知、受管资源身份不一致、
转换超时和清理失败；`unittest` 的 failure/error 统计均为 0。

### 2.5 1G-6E：最终静态与范围终检

- 关闭资产、引用检查器、契约和架构终检执行 50 项，失败 0、错误 0、跳过 0；
- 引用检查器扫描 555 个 Python 文件和 316 个文本文件，得到 11/11 候选删除就绪、阻塞引用 0、
  未知动态导入 0、`inventoryComplete=true`；309 条非阻塞引用分类保持稳定；
- `venv\Scripts\python.exe -B -m compileall -q app tests scripts` 通过；
- `git diff --check` 通过，仅有工作树 LF/CRLF 转换提示；
- `docs/接口文档/` 差异文件数为 0，`run.py` 差异文件数为 0；
- 分支/HEAD 仍为 `feat/weaponry-chat` / `808f3a109c1e56df32ac7abfde8ef625b846a2dd`；
- 没有创建提交、暂存文件、修改生产数据库或清理用户工作树。

## 3. 文件范围

1G-6 新增或直接修改的范围为：

- `docs/重构记录/阶段0资产/260801-阶段1关闭模块所有权与遗留适配矩阵.md`；
- `tests/test_stage1g_closeout.py`；
- `README.md`；
- `app/modules/README.md`；
- `app/modules/debug/README.md`；
- `app/services/llm_service/README.md`；
- `tests/README.md`；
- `docs/重构记录/阶段0资产/README.md`；
- `docs/重构记录/README.md`；
- `docs/更新记录/README.md`；
- `docs/重构记录/260801-阶段1G组合根Debug与遗留清理文件级实施设计.md`；
- 本执行记录。

未修改 `docs/接口文档/`、`run.py`、数据库 Schema、生产 SQLite、AnythingLLM 供应商协议、公开请求、
响应、Callback、SSE 或 WebSocket 参数。

## 4. 阶段 2 交接重点

阶段 2 必须以上述所有权矩阵为入口，至少解决：

1. Analysis/Report/Weaponry 的版本化不可变输入 Codec、业务键、批次/dispatch 顺序和 `Run(TaskId)`；
2. 统一 Task/Attempt/Step/Event、领取、心跳、checkpoint、Reaper、lease/fencing 与重派规则；
3. 当前 Dispatcher 的 FIFO、扫描、poison cooldown、stop 和 maintenance 语义到可靠执行模型的映射；
4. Callback Guard、delivery attempt、`outcome_unknown`、显式授权重试、资源事实与 latest-wins；
5. DocumentProcessing Profile 冻结字段、Artifact/Lineage、外部操作 unknown 和阶段 3 存储迁移输入；
6. `LLMTaskService`、Chat、Reassign、Knowledge Index、RAG Lease 的表与事务边界；
7. 进程内 limiter/activity/file lock/Hub 的单实例限制，以及阶段 5 Broker ACK/DLQ/Outbox 和阶段 6
   多实例演练所需 owner gate。

## 5. 回滚与发布边界

- 1G-6 只增加/同步文档和永久资产测试，可按文件回退，不需要数据库回滚；
- 1G-5 已删除对象如需恢复，只能从已知代码版本恢复单个有明确调用方、所有者和退出期限的对象；
- 禁止恢复整个旧包、在线双跑新旧 Worker 或绕过永久引用/架构门禁；
- 本次未执行真实生产数据库、真实 AnythingLLM/MinerU/LibreOffice/模型、生产回调、RabbitMQ、
  多实例和 50+ 生产容量验收，不能标记为 production ready。

## 6. 最终结论

1G-0～1G-6 已按顺序完成并通过各自关闭门禁。阶段 1 的模块化单体结构、正式 Web 边界、Debug
查询边界、框架无关组合根、遗留测试价值、物理删除证据、模块/表所有权和阶段 2 输入均已有当前
文档与自动化资产。没有发现仍需在进入阶段 2 前商讨的 1G 范围内事项。

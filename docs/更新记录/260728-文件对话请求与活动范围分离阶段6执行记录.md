# 文件对话请求与活动范围分离阶段 6 执行记录

## 1. 开发 Chat 数据清理

清理前解析的实际配置为：

```text
DOCSENSE_RUNTIME_DIR=C:\.me\envs\DocSenseEnv
DOCSENSE_CHAT_DB 未单独覆盖
Chat DB=C:\.me\envs\DocSenseEnv\chat_sessions.sqlite3
```

清理前确认 DocSense 默认端口 5001 未监听。随后只处理以下三个精确目标：

```text
C:\.me\envs\DocSenseEnv\chat_sessions.sqlite3
C:\.me\envs\DocSenseEnv\chat_sessions.sqlite3-wal
C:\.me\envs\DocSenseEnv\chat_sessions.sqlite3-shm
```

未删除 `C:\.me\envs\DocSenseEnv` 目录，也未处理知识库、任务、审计或其他数据库。

首次 PowerShell 清理命令中，sidecar 数组表达式把第二项错误解析为工作区下的 `-wal`。
路径边界校验按设计立即中止；在此之前已删除唯一实际存在的 Chat 主库。随后逐一核对三个精确
目标均不存在，确认没有越界删除。

开发 Chat 数据无保留要求，本次清理不可恢复。

## 2. 空库重建

未启动 `run.py`，仅使用项目虚拟环境构造 `ChatStore` 初始化空库。重建后的只读核验结果：

```text
schema_versions=[1, 2, 3, 4]
chat_sessions=0
chat_scope_revisions=0
chat_scope_heads=0
```

## 3. 定向门禁汇总

本轮各阶段已通过：

- 204 项 Chat 全量发现；
- 59 项 AnythingLLM Chat Gateway、依赖容器与架构边界；
- 52 项 Scope 契约、公开契约、history、title 与 debug 组合；
- 1,000 个 Effective Scope 成员不扩大空 Requested history；
- 同会话 50 首次并发和 50 显式替换并发唯一受理；
- 不同会话隔离、事务回滚、run-id 离线恢复和累计绑定范围隔离。

## 4. 安全全仓回归

动态发现 `tests` 下 `test*.py` 共 1,879 项，执行前逐项打印并核对以下 13 项排除：

1. `tests.test_local_scripts.LocalScriptTests.test_analysis_shell_script_posts_fixture_to_expected_path`
2. `tests.test_local_scripts.LocalScriptTests.test_check_task_shell_script_posts_fixture_to_expected_path`
3. `tests.test_local_scripts.LocalScriptTests.test_progress_shell_script_reads_progress_snapshot_from_local_app`
4. `tests.test_local_scripts.LocalScriptTests.test_report_shell_script_posts_fixture_to_expected_path`
5. `tests.test_local_scripts.LocalScriptTests.test_start_test_file_server_serves_fixture_file`
6. `tests.test_local_scripts.LocalScriptTests.test_weaponry_directory_script_dry_run_writes_manifest`
7. `tests.test_local_scripts.LocalScriptTests.test_weaponry_shell_script_posts_fixture_to_expected_path`
8. `tests.test_migrate_analysis_security.AnalysisSecurityMigrationTests.test_apply_is_idempotent_and_preserves_callback_metadata_and_audit`
9. `tests.test_test_assets.LLMTestAssetsTests.test_analysis_request_fixture_has_required_fields`
10. `tests.test_test_assets.LLMTestAssetsTests.test_check_task_fixture_can_query_multiple_files`
11. `tests.test_test_assets.LLMTestAssetsTests.test_check_task_weaponry_fixture_matches_request_architecture_id`
12. `tests.test_test_assets.LLMTestAssetsTests.test_report_request_fixture_has_required_fields`
13. `tests.test_test_assets.LLMTestAssetsTests.test_weaponry_request_fixture_uses_ship_fields`

排除原因保持既有安全口径：前 7 项会启动本地脚本、服务或发出本地网络请求；第 8 项依赖
Windows 无法稳定表达的 POSIX 权限位；后 5 项依赖被 `.gitignore` 排除的个人联调夹具。

其余用例按模块分为 8 个不重叠组，组大小为：

```text
233, 234, 234, 234, 233, 233, 233, 232
```

结果：

| 发现 | 排除 | 执行 | 失败 | 错误 | 跳过 | Expected failure | Unexpected success |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1,879 | 13 | 1,866 | 0 | 0 | 0 | 0 | 0 |

全仓回归使用随机临时 `DOCSENSE_RUNTIME_DIR`，结束后已校验并删除临时目录。输出中的
`ERROR`、`CRITICAL` 等来自预期故障注入，不代表 unittest 失败。

第一次启动安全全仓命令时，外层工具超时误设为 1 秒，进程被终止且未产生有效结果；遗留的
唯一临时目录经绝对路径校验后删除。上表只统计随后完整执行成功的正式结果。

## 5. 门禁结论

阶段 6 验收通过，可以进入阶段 7。当前没有新增待确认事项。以上证据仅证明 Windows、
SQLite 单实例、Fake/临时数据库下的离线行为，不代表真实 AnythingLLM、真实模型、多实例、
可靠队列或生产高并发就绪。

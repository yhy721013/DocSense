# 阶段 1F-0：文件分析契约黄金与现状资产执行记录

## 1. 范围与契约结论

本次只完成阶段 1F-0 的冻结和验证工作：新增离线契约/算法黄金资产、测试和实施记录，
为后续把文件分析收口为垂直业务切片建立可执行基线。

本次未修改 app/ 下的生产源码、SQLite Schema、后台线程、Container 生命周期或公开路由；
未启动 run.py，也未连接真实 AnythingLLM、模型服务或回调服务。

未修改 docs/接口文档/，未增加、删除、改名或改变任何前后端接口参数、JSON 字段、
HTTP 状态码、Header、SSE/WS 格式或同步语义。文件处理和报告生成接口文档的
SHA-256 为 CFE6D4C6CE4A224305337F78EE9616E00D5E114093740F574BBEF0B05AF97394，
由新增黄金测试校验。

## 2. 已完成改造

- 新增 tests/contracts/stage1f0_analysis_contracts.json，冻结 POST /llm/analysis 的
  64 MiB 请求限制、32 项批量上限、202 严格空体、既有 400/409/413/503/500 兼容行为，
  并明确成功响应不得泄露 task、execution、batch、lease 或 run 内部标识。
- 冻结 file 类型 check-task 的 200 空体与单项不存在 404、Progress 的 fileName/progress
  消息形状，以及文件 callback 的成功/失败载荷。
- 冻结范围默认值、领域树指纹和拓扑、召回候选排序、分类/抽取 Prompt、身份重选 Prompt、
  默认策略和字段映射/回退。长 Prompt 与默认范围采用稳定 SHA-256，结构化结果逐字段比较。
- 记录当前旧链的阶段顺序、外部副作用点和引用清单：路由校验、旧任务预查、原子受理、
  初始 Progress、daemon Thread、单/批执行、下载/预处理、RAG、知识入库、翻译、状态与回调。
  后续 1F-1 起移动这些职责时，必须显式更新清单，不能静默遗漏。
- 新增 tests/test_analysis_contract_assets.py。测试均使用临时 SQLite、显式注入的离线容器、
  Fake 和线程替身；不依赖开发机 .runtime 数据。

## 3. 验证结果

| 验证范围 | 结果 |
| --- | --- |
| 新增 1F-0 资产 | 8 项通过，0 failure、0 error。 |
| Analysis 纯规则、Prompt、领域树、召回、双阶段、范围保护、身份重选、配置，加新增资产 | 267 项通过；其中原计划基线 259 项保持通过。 |
| 路由、容器、TaskService、RAG/Knowledge Gateway、架构门禁与阶段 0 契约 | 201 项通过。 |
| 1F-0 定向合计 | 468 项通过，0 failure、0 error。 |
| 安全全仓回归 | 动态发现 1,633 项；逐项排除 13 项后运行 1,620 项，0 failure、0 error、0 skipped，分三批累计约 140 秒。 |

安全全仓明确排除项及原因：

1. test_local_scripts.LocalScriptTests 的 7 项会启动本地 Shell、静态服务或 run.py；
2. test_test_assets.LLMTestAssetsTests 的 5 项依赖被 .gitignore 排除的个人联调夹具；
3. test_migrate_analysis_security.AnalysisSecurityMigrationTests 中的 1 项断言 POSIX
   0640 权限位，Windows 文件系统无法稳定表达。

测试输出中的网络、权限、回调、资源和供应商异常均来自临时目录、严格 Fake 或显式故障注入；
它们是断言分支，不代表真实服务启动或真实外部请求发送。

## 4. 静态检查与剩余边界

已执行 git diff --check，未发现空白错误。新增资产还校验接口文档摘要、公开响应中禁止的内部
标识，以及旧路由/执行器引用。

本次不改变当前 SQLite single_instance 运行模式，也不宣称已经具备可靠任务队列、多实例部署、
运行中任务自动接管、数据库最终一致性或真实 50 个重任务吞吐能力。上述目标仍由 1F 后续波次和
阶段 2 至 6 的架构演进完成；1F-1 前不得基于本次离线结果切换生产执行链。

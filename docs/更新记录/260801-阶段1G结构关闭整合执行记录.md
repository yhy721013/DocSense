# 阶段 1G 结构关闭整合执行记录

## 1. 整合说明

本文整合阶段 1G-0～1G-6 的契约基线、Debug 分层、组合根收口、正式蓝图审查、测试价值迁移、条件物理删除和阶段 2 交接记录。

- 阶段状态：1G 已关闭，阶段 1 模块化单体结构完成收口。
- 公开契约：未修改 `docs/接口文档/`，未改变 HTTP、Callback、SSE 或 WebSocket 语义。
- 删除边界：只删除经引用扫描、测试价值迁移和负责人门禁确认的遗留实现。

## 2. 阶段里程碑

| 波次 | 主要结果 |
| --- | --- |
| 1G-0 | 冻结 15 条正式路由、4 条 Debug 路由、回归基线和 11 组删除候选。 |
| 1G-1 | 建立 Debug Query/Port/Adapter/Presenter 与不可变结果，四条 Debug 路由收口为薄入口。 |
| 1G-2 | 将 `app/container.py` 收口为框架无关组合根，把 Flask 依赖访问集中到唯一 Adapter。 |
| 1G-3 | 将 Weaponry 冻结/策略/受理和 check-task 预校验/同步恢复等编排下沉到 Application。 |
| 1G-4 | 将旧测试中的 200 条有效断言逐项迁移到现行 Domain/Application/Adapter/Presenter 门禁。 |
| 1G-5 | 按候选批次删除旧执行器、旧 Facade、供应商包装和方法级兼容入口。 |
| 1G-6 | 固化模块/数据库表所有权、依赖方向、保留适配、回滚点和阶段 2 输入，完成九组关闭验收。 |

## 3. 最终结构事实

1. `app/container.py` 是框架无关生产组合根；Flask 只通过依赖访问 Adapter 获取已装配服务。
2. 正式 Blueprint 只承担请求解析、Application 调用和 Presenter 输出，不直接构造数据库、供应商客户端、执行线程或恢复器。
3. `/debug/*` 是只读 Query 边界，但仍需在生产网络/反向代理层限制访问；应用内只读不等于可公开暴露。
4. 每个静态 SQLite 表都有唯一模块 owner；跨模块只通过 Port/Facade 访问，不允许绕过所有权直接写表。
5. 遗留代码必须先完成生产引用、测试价值、配置/文档引用三类盘点，再决定迁移、保留或物理删除。
6. 历史兼容入口只在明确调用方仍存在时保留，不能成为第二生产执行链。

## 4. 验证与删除证据

- 11/11 删除候选通过引用就绪检查；
- 旧测试 200 条断言完成方法级价值映射，迁移后核心语义和关闭门禁通过；
- 最终安全全仓动态发现 2,118 项，精确排除 13 项，执行 2,105 项；failure 0、error 0、skip 3；
- 关闭资产、引用检查器、契约和架构终检 50 项通过；
- `compileall` 和 `git diff --check` 通过。

跳过项为 macOS 真实进程组和 Windows 符号链接平台能力门禁。物理删除的历史源码仍可通过 Git 追溯。

## 5. 阶段 2 交接

- 统一 Task/Attempt/Step/Event、heartbeat、lease/fencing、checkpoint 和 Reaper。
- 将本地 Dispatcher 的持久扫描、FIFO、poison cooldown、stop 和 maintenance 语义映射到可靠执行模型。
- 建立共享数据库迁移、Outbox/可靠队列、跨实例通知和数据库一致性门禁。
- 保持组合根、模块所有权和薄路由边界，不在接入队列时重新耦合 Web、业务和供应商实现。

本阶段没有运行真实生产数据库、AnythingLLM、MinerU、LibreOffice、模型、生产 Callback 或 RabbitMQ，因此不能据此宣称多实例、可靠队列或生产容量已经完成。


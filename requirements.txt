# 1. 基础 Web 与网络通信
Flask
flask-sock
python-dotenv
requests
httpx
# 2. 文档解析与处理
PyMuPDF
Markdown
# 已废弃的文档处理库
# python-docx
# pdfplumber
# pdf2docx
# reportlab
# 3. 翻译服务
argostranslate
# 4. MinerU 及其硬件加速底层 (目标机器环境：CUDA XX.X)
# --extra-index-url https://download.pytorch.org/whl/cu121
# 明确锁定带有 CUDA 后缀的版本
# torch==2.4.1+cu121
# torchvision==0.19.1+cu121
mineru[core]
# 5. RAG 增强依赖（BM25 + Reranker）
rank-bm25>=0.2.2
sentence-transformers>=2.3.0
# 6. 中文分词（可选，用于 BM25 关键词提取优化，未安装时降级为字符级切分）
jieba
# 模板目录说明

本目录保存 Flask 调试页面使用的 HTML 模板。模板只承担人工排查和本地演示，不属于 `/llm/chat*` 的正式接口契约或文件对话领域状态机。

## 子目录说明

| 路径 | 作用 |
| --- | --- |
| `debug/` | 回调与文件对话的调试页面模板。 |

## 维护规则

- 调试页面可以展示经过脱敏和约束的本地诊断数据，但不能成为生产接口的替代品。
- 不得以模板变更为由修改文件对话 HTTP/SSE 的请求参数、事件名称或响应结构。
